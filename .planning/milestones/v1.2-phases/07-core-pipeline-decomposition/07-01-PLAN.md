---
phase: 07-core-pipeline-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Motiv.FluentFactory.Generator/Diagnostics/FluentDiagnostics.cs
  - src/Motiv.FluentFactory.Generator/Analysis/FluentConstructorContextFactory.cs
  - src/Motiv.FluentFactory.Generator/FluentFactoryGenerator.cs
  - src/Motiv.FluentFactory.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs
  - src/Motiv.FluentFactory.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs
  - src/Motiv.FluentFactory.Generator/Model/SymbolExtensions.cs
  - src/Motiv.FluentFactory.Generator/Model/FluentConstructorValidator.cs
  - src/Motiv.FluentFactory.Generator/Model/FluentModelFactory.cs
autonomous: true
requirements:
  - DECOMP-02
  - XCUT-01
  - XCUT-02

must_haves:
  truths:
    - "FluentFactoryGenerator contains only Initialize() pipeline setup and Execute() (~70 lines)"
    - "All 10 diagnostic descriptors live in FluentDiagnostics static class"
    - "Constructor context creation, metadata extraction, and de-duplication live in FluentConstructorContextFactory"
    - "All existing tests pass with identical generated output"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator/Diagnostics/FluentDiagnostics.cs"
      provides: "All 10 DiagnosticDescriptor static fields"
      contains: "public static class FluentDiagnostics"
    - path: "src/Motiv.FluentFactory.Generator/Analysis/FluentConstructorContextFactory.cs"
      provides: "Context creation, metadata extraction, de-duplication methods"
      contains: "FluentConstructorContextFactory"
    - path: "src/Motiv.FluentFactory.Generator/FluentFactoryGenerator.cs"
      provides: "Clean IIncrementalGenerator entry point"
      min_lines: 30
  key_links:
    - from: "src/Motiv.FluentFactory.Generator/FluentFactoryGenerator.cs"
      to: "FluentConstructorContextFactory"
      via: "static method calls in pipeline"
      pattern: "FluentConstructorContextFactory\\."
    - from: "src/Motiv.FluentFactory.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs"
      to: "FluentDiagnostics"
      via: "diagnostic descriptor reference"
      pattern: "FluentDiagnostics\\.UnreachableConstructor"
    - from: "src/Motiv.FluentFactory.Generator/Model/FluentConstructorValidator.cs"
      to: "FluentDiagnostics"
      via: "diagnostic descriptor references"
      pattern: "FluentDiagnostics\\."
---

<objective>
Decompose FluentFactoryGenerator (376 lines) into focused types by extracting diagnostic descriptors to FluentDiagnostics and pipeline helper methods to FluentConstructorContextFactory.

Purpose: FluentFactoryGenerator currently mixes diagnostic data definitions, constructor context factory logic, and generator pipeline orchestration. After extraction, it becomes a clean ~70-line IIncrementalGenerator entry point.

Output: FluentDiagnostics.cs (static class with 10 descriptors), FluentConstructorContextFactory.cs (static class with context creation + de-duplication), simplified FluentFactoryGenerator.cs
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-pipeline-decomposition/07-CONTEXT.md
@.planning/phases/07-core-pipeline-decomposition/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/Motiv.FluentFactory.Generator/FluentFactoryGenerator.cs:
- 10 public static readonly DiagnosticDescriptor fields (lines 21-132): UnreachableConstructor, ContainsSupersededFluentMethodTemplate, IncompatibleFluentMethodTemplate, AllFluentMethodTemplatesIncompatible, FluentMethodTemplateAttributeNotStatic, FluentMethodTemplateSuperseded, InvalidCreateMethodName, DuplicateCreateMethodName, FluentConstructorTargetTypeMissingFluentFactory, CreateMethodNameWithNoCreateMethod
- private const string Category = "FluentFactory" (line 16)
- private static methods to extract: CreateConstructorContexts (lines 238-301), GetFluentFactoryMetadata (lines 303-337), ConvertToFluentFactoryGeneratorOptions (lines 339-375), DeDuplicateFluentConstructors (lines 195-200), ChooseOverridingConstructors (lines 202-219)
- Local function to extract: CreateContainingTypeFluentConstructorContexts (lines 282-300)

Files referencing FluentFactoryGenerator diagnostic fields (must update to FluentDiagnostics):
- Diagnostics/UnreachableConstructorAnalyzer.cs line 41: FluentFactoryGenerator.UnreachableConstructor
- Diagnostics/IgnoredMultiMethodWarningFactory.cs lines 45, 57: ContainsSupersededFluentMethodTemplate, FluentMethodTemplateSuperseded
- Model/SymbolExtensions.cs lines 79, 90: FluentMethodTemplateAttributeNotStatic, IncompatibleFluentMethodTemplate
- Model/FluentConstructorValidator.cs lines 27, 46, 75, 98: FluentConstructorTargetTypeMissingFluentFactory, InvalidCreateMethodName, DuplicateCreateMethodName, CreateMethodNameWithNoCreateMethod
- Model/FluentModelFactory.cs line 315: AllFluentMethodTemplatesIncompatible
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract FluentDiagnostics and FluentConstructorContextFactory from FluentFactoryGenerator</name>
  <files>
    src/Motiv.FluentFactory.Generator/Diagnostics/FluentDiagnostics.cs,
    src/Motiv.FluentFactory.Generator/Analysis/FluentConstructorContextFactory.cs,
    src/Motiv.FluentFactory.Generator/FluentFactoryGenerator.cs
  </files>
  <action>
    **Step 1: Create FluentDiagnostics.cs** in Diagnostics/ directory.
    - Public static class in namespace `Motiv.FluentFactory.Generator.Diagnostics`
    - Move `private const string Category = "FluentFactory"` here
    - Move all 10 `public static readonly DiagnosticDescriptor` fields from FluentFactoryGenerator (lines 21-132) here, preserving exact field names, values, and XML doc comments
    - Add XML doc header to the class: "Diagnostic descriptors for the fluent factory source generator."

    **Step 2: Create FluentConstructorContextFactory.cs** in Analysis/ directory.
    - Internal static class in namespace `Motiv.FluentFactory.Generator.Analysis`
    - Move these static methods from FluentFactoryGenerator:
      - `CreateConstructorContexts` (lines 238-301) including the local function `CreateContainingTypeFluentConstructorContexts`
      - `GetFluentFactoryMetadata` (lines 303-337)
      - `ConvertToFluentFactoryGeneratorOptions` (lines 339-375)
      - `DeDuplicateFluentConstructors` (lines 195-200)
      - `ChooseOverridingConstructors` (lines 202-219)
    - All methods remain static. Keep their exact signatures, logic, and XML doc comments (add docs if missing)
    - Add necessary using statements for the types these methods reference (FluentConstructorContext, FluentFactoryMetadata, FluentFactoryGeneratorOptions, etc.)

    **Step 3: Simplify FluentFactoryGenerator.cs**
    - Remove the 10 diagnostic descriptor fields and Category constant
    - Remove CreateConstructorContexts, GetFluentFactoryMetadata, ConvertToFluentFactoryGeneratorOptions, DeDuplicateFluentConstructors, ChooseOverridingConstructors
    - Update Initialize() to call `FluentConstructorContextFactory.CreateConstructorContexts(...)` and `FluentConstructorContextFactory.DeDuplicateFluentConstructors(...)`
    - Add using for `Motiv.FluentFactory.Generator.Diagnostics` and `Motiv.FluentFactory.Generator.Analysis` as needed
    - The remaining file should contain only: the [Generator] class declaration, Initialize() with pipeline setup, and Execute()
    - Verify the file is roughly 50-80 lines after extraction

    Follow established codebase patterns: primary constructor injection style where applicable, LINQ-heavy queries, modern C# features (collection expressions, pattern matching).
  </action>
  <verify>
    <automated>dotnet build src/Motiv.FluentFactory.Generator -v q</automated>
  </verify>
  <done>FluentFactoryGenerator.cs contains only Initialize() pipeline and Execute(). FluentDiagnostics.cs has all 10 descriptors. FluentConstructorContextFactory.cs has all context creation and de-duplication logic. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update all diagnostic descriptor references and run full test suite</name>
  <files>
    src/Motiv.FluentFactory.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs,
    src/Motiv.FluentFactory.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs,
    src/Motiv.FluentFactory.Generator/Model/SymbolExtensions.cs,
    src/Motiv.FluentFactory.Generator/Model/FluentConstructorValidator.cs,
    src/Motiv.FluentFactory.Generator/Model/FluentModelFactory.cs
  </files>
  <action>
    Update all files that reference `FluentFactoryGenerator.{DiagnosticName}` to use `FluentDiagnostics.{DiagnosticName}` instead:

    1. **UnreachableConstructorAnalyzer.cs** (line 41): Change `FluentFactoryGenerator.UnreachableConstructor` to `FluentDiagnostics.UnreachableConstructor`. Update using from `Motiv.FluentFactory.Generator` to add `Motiv.FluentFactory.Generator.Diagnostics` (may already have it since the file is in Diagnostics/).

    2. **IgnoredMultiMethodWarningFactory.cs** (lines 45, 57): Change `FluentFactoryGenerator.ContainsSupersededFluentMethodTemplate` to `FluentDiagnostics.ContainsSupersededFluentMethodTemplate` and `FluentFactoryGenerator.FluentMethodTemplateSuperseded` to `FluentDiagnostics.FluentMethodTemplateSuperseded`. Add using if needed.

    3. **Model/SymbolExtensions.cs** (lines 79, 90): Change `FluentFactoryGenerator.FluentMethodTemplateAttributeNotStatic` to `FluentDiagnostics.FluentMethodTemplateAttributeNotStatic` and `FluentFactoryGenerator.IncompatibleFluentMethodTemplate` to `FluentDiagnostics.IncompatibleFluentMethodTemplate`. Replace `using Motiv.FluentFactory.Generator` (if used only for diagnostics) with `using Motiv.FluentFactory.Generator.Diagnostics`, or add the Diagnostics using.

    4. **Model/FluentConstructorValidator.cs** (lines 27, 46, 75, 98): Change all 4 references to use `FluentDiagnostics.` prefix. Add `using Motiv.FluentFactory.Generator.Diagnostics`.

    5. **Model/FluentModelFactory.cs** (line 315): Change `FluentFactoryGenerator.AllFluentMethodTemplatesIncompatible` to `FluentDiagnostics.AllFluentMethodTemplatesIncompatible`. Add `using Motiv.FluentFactory.Generator.Diagnostics` and remove `using Motiv.FluentFactory.Generator` if it was only used for diagnostics (check if other references to that namespace remain).

    After all updates, do a full build to catch any missed references, then run the complete test suite.
  </action>
  <verify>
    <automated>dotnet test src/Motiv.FluentFactory.Generator.Tests -v q</automated>
  </verify>
  <done>All 10 diagnostic references updated from FluentFactoryGenerator to FluentDiagnostics. Zero compile errors. All existing tests pass with identical generated output.</done>
</task>

</tasks>

<verification>
- `dotnet build src/Motiv.FluentFactory.Generator -v q` compiles without errors
- `dotnet test src/Motiv.FluentFactory.Generator.Tests -v q` all tests pass
- FluentFactoryGenerator.cs is ~50-80 lines (only Initialize + Execute)
- FluentDiagnostics.cs contains all 10 DiagnosticDescriptor fields
- FluentConstructorContextFactory.cs contains all context creation and de-duplication methods
- No file references `FluentFactoryGenerator.{DiagnosticName}` (grep confirms zero matches)
</verification>

<success_criteria>
- FluentFactoryGenerator is a clean ~70-line IIncrementalGenerator entry point with only Initialize() and Execute()
- FluentDiagnostics is a public static class with all 10 diagnostic descriptors
- FluentConstructorContextFactory is an internal static class with all context creation + de-duplication logic
- All existing tests pass (XCUT-01)
- Generated output is identical (XCUT-02, verified by test suite)
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-pipeline-decomposition/07-01-SUMMARY.md`
</output>
