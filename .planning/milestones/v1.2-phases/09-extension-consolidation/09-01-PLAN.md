---
phase: 09-extension-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Motiv.FluentFactory.Generator/Generation/TypeParameterExtensions.cs
  - src/Motiv.FluentFactory.Generator/Generation/SymbolExtensions.cs
  - src/Motiv.FluentFactory.Generator/Model/FluentModelExtensions.cs
  - src/Motiv.FluentFactory.Generator/StringExtensions.cs
autonomous: true
requirements: [EXT-01, EXT-02, XCUT-01, XCUT-02]

must_haves:
  truths:
    - "Type parameter methods (GetGenericTypeParameters, GetGenericTypeArguments, GetGenericTypeParameterSyntaxList, ToTypeParameterSyntax, Union, Except) live in TypeParameterExtensions in Generation/"
    - "Symbol display/analysis methods (ToGlobalDisplayString, ToFullDisplayString, ToUnqualifiedDisplayString, IsOpenGenericType, IsPartial, CanBeCustomStep, IsAssignable, ReplaceTypeParameters, AccessibilityToSyntaxKind, HasAttribute, GetAttributes) live in SymbolExtensions in Generation/"
    - "Fluent domain methods (ToDisplayString, FindUnreachableConstructors, GetGenericTypeArguments on IFluentReturn, GetFluentMethodName, GetMultipleFluentMethodSymbols, GetFluentMethodPriority, GetLocationAtIndex, GetAttribute for TypeName) live in FluentModelExtensions in Model/"
    - "All string utilities (Capitalize, ToCamelCase, ToParameterFieldName, ToIdentifier, ToFileName) live in one StringExtensions at project root"
    - "All files use the shared namespace Motiv.FluentFactory.Generator"
    - "Old source files (Model/SymbolExtensions.cs, Model/FluentMethodExtensions.cs, Model/FluentReturnExtensions.cs, Generation/StringExtensions.cs) are deleted"
    - "All existing tests pass with identical generated output"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator/Generation/TypeParameterExtensions.cs"
      provides: "Type parameter extraction, filtering, conversion extensions"
      contains: "class TypeParameterExtensions"
    - path: "src/Motiv.FluentFactory.Generator/Generation/SymbolExtensions.cs"
      provides: "Symbol display formatting, type analysis, accessibility, attribute helpers"
      contains: "class SymbolExtensions"
    - path: "src/Motiv.FluentFactory.Generator/Model/FluentModelExtensions.cs"
      provides: "Fluent method display, return helpers, unreachable constructors, fluent attribute helpers"
      contains: "class FluentModelExtensions"
    - path: "src/Motiv.FluentFactory.Generator/StringExtensions.cs"
      provides: "All string utility extensions merged"
      contains: "class StringExtensions"
  key_links:
    - from: "src/Motiv.FluentFactory.Generator/Generation/TypeParameterExtensions.cs"
      to: "Generation/SymbolExtensions.cs"
      via: "ToGlobalDisplayString call in ToTypeParameterSyntax"
      pattern: "ToGlobalDisplayString"
    - from: "src/Motiv.FluentFactory.Generator/Model/FluentModelExtensions.cs"
      to: "Generation/SymbolExtensions.cs"
      via: "ToFullDisplayString, IsOpenGenericType, IsAssignable calls"
      pattern: "ToFullDisplayString|IsOpenGenericType|IsAssignable"
    - from: "src/Motiv.FluentFactory.Generator/Model/FluentModelExtensions.cs"
      to: "StringExtensions.cs"
      via: "Capitalize call in GetFluentMethodName"
      pattern: "Capitalize"
---

<objective>
Consolidate all 7 extension files into 5 concern-based files: TypeParameterExtensions, SymbolExtensions, FluentModelExtensions, StringExtensions, and EnumerableExtensions (unchanged). Delete the 4 old source files and update all consumer using directives.

Purpose: Eliminate the duplicate SymbolExtensions across Model and Generation namespaces, and organize extension methods by domain concern rather than horizontal layer.
Output: 4 new/rewritten files, 4 deleted files, all consumer usings updated, tests green.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extension-consolidation/09-CONTEXT.md

@src/Motiv.FluentFactory.Generator/Generation/SymbolExtensions.cs
@src/Motiv.FluentFactory.Generator/Model/SymbolExtensions.cs
@src/Motiv.FluentFactory.Generator/Model/FluentMethodExtensions.cs
@src/Motiv.FluentFactory.Generator/Model/FluentReturnExtensions.cs
@src/Motiv.FluentFactory.Generator/StringExtensions.cs
@src/Motiv.FluentFactory.Generator/Generation/StringExtensions.cs
@src/Motiv.FluentFactory.Generator/EnumerableExtensions.cs

<interfaces>
<!-- Key types consumed by extension methods that the executor needs to understand -->

From Model/FluentMethodExtensions.cs — depends on:
- IFluentMethod (Model/Methods/) — has Name, TypeParameters, MethodParameters, Return properties
- MultiMethod (Model/Methods/) — subtype with ParameterConverter, SiblingMultiMethods
- IFluentReturn (Model/Steps/) — has CandidateConstructors, GenericConstructorParameters
- TargetTypeReturn (Model/Steps/) — subtype with Constructor property
- FluentType (Model/) — wrapper around ITypeParameterSymbol

From Model/SymbolExtensions.cs — depends on:
- TypeName (root) — string constants for attribute full names
- FluentDiagnostics (Diagnostics/) — diagnostic descriptors

Consumer using directives to update (files importing Model or Generation namespace for extension methods):
- Files with `using Motiv.FluentFactory.Generator.Model;` — may need updating
- Files with `using Motiv.FluentFactory.Generator.Generation;` — may need updating
- After consolidation to shared namespace `Motiv.FluentFactory.Generator`, many cross-namespace usings can be removed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeParameterExtensions and rewrite SymbolExtensions in Generation/</name>
  <files>
    src/Motiv.FluentFactory.Generator/Generation/TypeParameterExtensions.cs
    src/Motiv.FluentFactory.Generator/Generation/SymbolExtensions.cs
  </files>
  <action>
Split the current Generation/SymbolExtensions.cs into two files, both using namespace `Motiv.FluentFactory.Generator`:

**TypeParameterExtensions.cs** (new file in Generation/):
Move these methods from Generation/SymbolExtensions.cs:
- `GetGenericTypeParameterSyntaxList` (both overloads)
- `GetGenericTypeParameters` (both overloads — the IEnumerable and ITypeSymbol versions)
- `ToTypeParameterSyntax`
- `GetGenericTypeArguments` + private `GenericTypeArgumentsInternal`
- `Union`
- `Except`

Also move from current Generation/SymbolExtensions.cs into TypeParameterExtensions:
- None extra — Union and Except are already in Generation/SymbolExtensions.cs

Wait — checking context: Union and Except are in Generation/SymbolExtensions.cs lines 206-227. Yes, move those.

**SymbolExtensions.cs** (rewrite in Generation/):
Keep these methods from Generation/SymbolExtensions.cs:
- `TypeNameOnlyFormat` (private static field)
- `GlobalQualifiedFormat` (private static field)
- `ToGlobalDisplayString`
- `IsOpenGenericType` + private `ContainsUnboundGenericTypes`
- `IsAssignable`
- `AccessibilityToSyntaxKind`
- `ToUnqualifiedDisplayString`

Add these methods from Model/SymbolExtensions.cs (generic symbol helpers, NOT fluent-specific):
- `FullFormat` (private static field — rename or keep, check for conflicts with FluentModelExtensions which also has a FullFormat)
- `IsPartial`
- `ToFullDisplayString`
- `CanBeCustomStep`
- `ReplaceTypeParameters`
- `HasAttribute` (both overloads — the TypeName version and the generic version)
- `GetAttributes` (both overloads — the TypeName version and the generic version)

Change namespace to `Motiv.FluentFactory.Generator` for both files.
Add XML doc headers to both classes per CLAUDE.md conventions.

IMPORTANT: The `FullFormat` field exists in both Model/SymbolExtensions.cs and Model/FluentMethodExtensions.cs with slightly different options. The one from Model/SymbolExtensions.cs (used by ToFullDisplayString) goes into SymbolExtensions. The one from FluentMethodExtensions goes into FluentModelExtensions (Task 2). They have different settings so they must remain separate.
  </action>
  <verify>
    <automated>dotnet build src/Motiv.FluentFactory.Generator/Motiv.FluentFactory.Generator.csproj --no-incremental 2>&amp;1 | tail -5</automated>
  </verify>
  <done>TypeParameterExtensions.cs exists with all type parameter methods. SymbolExtensions.cs contains display/analysis/attribute methods. Both use namespace Motiv.FluentFactory.Generator. Project may not compile yet (consumers still reference old namespaces, old files still exist).</done>
</task>

<task type="auto">
  <name>Task 2: Create FluentModelExtensions and merge StringExtensions</name>
  <files>
    src/Motiv.FluentFactory.Generator/Model/FluentModelExtensions.cs
    src/Motiv.FluentFactory.Generator/StringExtensions.cs
  </files>
  <action>
**FluentModelExtensions.cs** (new file in Model/):
Namespace: `Motiv.FluentFactory.Generator`

Merge all methods from:
1. Model/FluentMethodExtensions.cs — all methods:
   - `FullFormat` private static field (the one with IncludeVariance)
   - `ToDisplayString(this IFluentMethod)`
   - private `SerializeFluentMethod`
   - `FindUnreachableConstructors`

2. Model/FluentReturnExtensions.cs — all methods:
   - `GetGenericTypeArguments(this IFluentReturn, IDictionary...)`

3. Fluent-specific methods from Model/SymbolExtensions.cs:
   - `GetFluentMethodName(this IParameterSymbol)`
   - `GetMultipleFluentMethodSymbols(this Compilation, IParameterSymbol)`
   - `GetFluentMethodPriority(this IParameterSymbol)`
   - `GetLocationAtIndex(this AttributeData, int)`
   - `GetAttribute(this IParameterSymbol, TypeName)` — this is the fluent-specific attribute helper that takes TypeName

Note on GetAttribute: This method is used by both fluent-specific methods (GetFluentMethodName, GetFluentMethodPriority) and generic methods (HasAttribute with TypeName). Since it takes a TypeName parameter (a domain type), it belongs in FluentModelExtensions. The generic HasAttribute/GetAttributes methods in SymbolExtensions will need to call GetAttribute — but actually HasAttribute(TypeName) calls GetAttributes(TypeName) not GetAttribute. So GetAttribute(IParameterSymbol, TypeName) is fluent-specific (only used with FluentMethodAttribute etc.) and goes here.

Keep required using directives for IFluentMethod, MultiMethod, IFluentReturn, TargetTypeReturn, FluentType, FluentDiagnostics, TypeName.

Add XML doc headers per CLAUDE.md conventions.

**StringExtensions.cs** (rewrite at project root):
Namespace: `Motiv.FluentFactory.Generator`

Merge both StringExtensions files:
From Generation/StringExtensions.cs:
- `FullyQualifiedFormat` (private static field)
- `Capitalize`
- `ToCamelCase`
- `ToParameterFieldName`
- `ToIdentifier`

From root StringExtensions.cs:
- `ToFileName`

Add XML doc headers per CLAUDE.md conventions.
  </action>
  <verify>
    <automated>dotnet build src/Motiv.FluentFactory.Generator/Motiv.FluentFactory.Generator.csproj --no-incremental 2>&amp;1 | tail -5</automated>
  </verify>
  <done>FluentModelExtensions.cs exists with all fluent domain methods. StringExtensions.cs contains all string utilities merged. Both use namespace Motiv.FluentFactory.Generator. Project may not compile yet (old files still exist, consumers not updated).</done>
</task>

<task type="auto">
  <name>Task 3: Delete old files, update consumer usings, verify tests pass</name>
  <files>
    src/Motiv.FluentFactory.Generator/Model/SymbolExtensions.cs
    src/Motiv.FluentFactory.Generator/Model/FluentMethodExtensions.cs
    src/Motiv.FluentFactory.Generator/Model/FluentReturnExtensions.cs
    src/Motiv.FluentFactory.Generator/Generation/StringExtensions.cs
  </files>
  <action>
1. Delete the 4 old source files:
   - `src/Motiv.FluentFactory.Generator/Model/SymbolExtensions.cs`
   - `src/Motiv.FluentFactory.Generator/Model/FluentMethodExtensions.cs`
   - `src/Motiv.FluentFactory.Generator/Model/FluentReturnExtensions.cs`
   - `src/Motiv.FluentFactory.Generator/Generation/StringExtensions.cs`

2. Update consumer using directives across the project. Since all extension classes now use namespace `Motiv.FluentFactory.Generator`:
   - Files that had `using Motiv.FluentFactory.Generator.Generation;` ONLY for extension methods may be able to remove that using (but only if they don't also reference other Generation types — check each file)
   - Files that had `using Motiv.FluentFactory.Generator.Model;` ONLY for extension methods may be able to remove that using (but only if they don't also reference other Model types — check each file)
   - Files already in namespace `Motiv.FluentFactory.Generator` need no using changes for extensions

3. Attempt `dotnet build` — fix any compilation errors from:
   - Missing usings (add `using Motiv.FluentFactory.Generator;` where needed)
   - Ambiguous references (if any method name collisions occur after namespace unification)
   - The `DistinctBy` in FluentReturnExtensions.cs calls the LINQ DistinctBy — ensure this still resolves (it might conflict with EnumerableExtensions.DistinctBy now that they share a namespace). If ambiguity occurs, qualify the call or remove the custom DistinctBy if .NET version supports it natively.

4. Run full test suite: `dotnet test` from solution root
5. Verify generated output is identical by checking no generated .g.cs files have changed (git diff on the Generated/ folder)

IMPORTANT: Do NOT remove `using Motiv.FluentFactory.Generator.Model;` or `using Motiv.FluentFactory.Generator.Generation;` from files that reference OTHER types in those namespaces (not just extensions). Only remove usings that are now unnecessary.
  </action>
  <verify>
    <automated>dotnet test C:/Dev/Motiv.FluentFactory/Motiv.FluentFactory.sln --no-restore --verbosity minimal 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All 4 old extension files deleted. All consumer files compile with correct using directives. Full test suite passes. Generated .g.cs output is identical (git diff shows no changes in Generated/ folder). No duplicate SymbolExtensions class exists.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Motiv.FluentFactory.Generator/Motiv.FluentFactory.Generator.csproj` succeeds with no errors
2. `dotnet test Motiv.FluentFactory.sln` — all tests pass
3. `git diff src/Motiv.FluentFactory.Example/Generated/` shows no changes (generated output identical)
4. Only one `SymbolExtensions` class exists (in Generation/)
5. No file named `FluentMethodExtensions.cs` or `FluentReturnExtensions.cs` exists
6. `grep -r "namespace Motiv.FluentFactory.Generator.Model" src/Motiv.FluentFactory.Generator/Model/FluentModelExtensions.cs` returns nothing (uses shared namespace)
</verification>

<success_criteria>
- Single SymbolExtensions in Generation/ (EXT-01 satisfied)
- Extension methods grouped by concern: TypeParameterExtensions, SymbolExtensions, FluentModelExtensions, StringExtensions, EnumerableExtensions (EXT-02 satisfied)
- All tests pass with identical generated output (XCUT-01, XCUT-02 satisfied)
- 4 old files deleted, 4 new/rewritten files in place
</success_criteria>

<output>
After completion, create `.planning/phases/09-extension-consolidation/09-01-SUMMARY.md`
</output>
