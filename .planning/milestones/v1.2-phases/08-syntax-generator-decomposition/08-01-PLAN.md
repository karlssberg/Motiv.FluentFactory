---
phase: 08-syntax-generator-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Motiv.FluentFactory.Generator/Generation/Shared/TypeParameterConstraintBuilder.cs
  - src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentStepMethodDeclaration.cs
  - src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs
  - src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/RootTypeDeclaration.cs
  - src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/FluentStepDeclaration.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs
autonomous: true
requirements: [SYNTAX-01, SYNTAX-02, XCUT-01, XCUT-02]

must_haves:
  truths:
    - "Constraint-building logic exists in exactly one place (TypeParameterConstraintBuilder)"
    - "All type constraints use global::-qualified names via ToGlobalDisplayString()"
    - "All 4 consumer files delegate constraint building to the shared builder"
    - "All existing tests pass (with updated expectations for the qualification bug fix)"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator/Generation/Shared/TypeParameterConstraintBuilder.cs"
      provides: "Shared constraint clause builder"
      contains: "TypeParameterConstraintBuilder"
    - path: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs"
      provides: "Updated test expectations for global:: qualification"
      contains: "global::System.IComparable"
  key_links:
    - from: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentStepMethodDeclaration.cs"
      to: "TypeParameterConstraintBuilder.Create"
      via: "method call replacing inline constraint loop"
      pattern: "TypeParameterConstraintBuilder\\.Create"
    - from: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs"
      to: "TypeParameterConstraintBuilder.Create"
      via: "method call replacing inline constraint loop"
      pattern: "TypeParameterConstraintBuilder\\.Create"
    - from: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/RootTypeDeclaration.cs"
      to: "TypeParameterConstraintBuilder.Create"
      via: "method call replacing inline constraint loop"
      pattern: "TypeParameterConstraintBuilder\\.Create"
    - from: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/FluentStepDeclaration.cs"
      to: "TypeParameterConstraintBuilder.Create"
      via: "method call replacing inline constraint loop"
      pattern: "TypeParameterConstraintBuilder\\.Create"
---

<objective>
Extract TypeParameterConstraintBuilder to Generation/Shared/ and replace constraint-building logic in all 4 consumer files, fixing the constraint qualification bug.

Purpose: Eliminates 4x duplicated constraint-building code and fixes an inconsistency where method-level type parameter constraints use `GlobalNamespaceStyle.Omitted` (producing `System.X`) instead of `ToGlobalDisplayString()` (producing `global::System.X`), aligning with Phase 6's decision that all type references use `global::`.

Output: TypeParameterConstraintBuilder.cs shared class, updated 4 consumer files, updated test expectations.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-syntax-generator-decomposition/08-CONTEXT.md
@.planning/phases/08-syntax-generator-decomposition/08-RESEARCH.md

@src/Motiv.FluentFactory.Generator/Generation/Shared/AggressiveInliningAttributeSyntax.cs
@src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentStepMethodDeclaration.cs
@src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs
@src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/RootTypeDeclaration.cs
@src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/FluentStepDeclaration.cs
@src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs

<interfaces>
<!-- Existing shared pattern to follow (AggressiveInliningAttributeSyntax.cs) -->
From Generation/Shared/AggressiveInliningAttributeSyntax.cs:
```csharp
namespace Motiv.FluentFactory.Generator.Generation.Shared;
internal static class AggressiveInliningAttributeSyntax
{
    public static AttributeSyntax Create() => ...;
}
```

<!-- Constraint ordering in method declarations (FluentStepMethodDeclaration, FluentRootFactoryMethodDeclaration) -->
<!-- Order: value type, reference type, constructor, type constraints -->
<!-- These produce method-level where clauses that tests validate -->

<!-- Constraint ordering in struct declarations (RootTypeDeclaration, FluentStepDeclaration) -->
<!-- Order: reference type, value type, type constraints, constructor -->
<!-- These produce struct-level where clauses that tests validate -->
<!-- IMPORTANT: The two orderings differ. Unify to one canonical order and update tests if needed. -->

<!-- The bug (in both FluentStepMethodDeclaration and FluentRootFactoryMethodDeclaration): -->
```csharp
// Method type parameter block uses Omitted (no global::):
var typeName = constraintType.ToDisplayString(SymbolDisplayFormat.FullyQualifiedFormat
    .WithGlobalNamespaceStyle(SymbolDisplayGlobalNamespaceStyle.Omitted));
// Should use: constraintType.ToGlobalDisplayString()
```

<!-- Test expectations to update (FluentFactoryGeneratorGenericTests.cs): -->
<!-- Line ~833: "where T : struct, System.IComparable<T>" -> "where T : struct, global::System.IComparable<T>" -->
<!-- Line ~931: "where T : struct, System.IComparable<T>" -> "where T : struct, global::System.IComparable<T>" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeParameterConstraintBuilder and replace in all 4 consumers</name>
  <files>
    src/Motiv.FluentFactory.Generator/Generation/Shared/TypeParameterConstraintBuilder.cs,
    src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentStepMethodDeclaration.cs,
    src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs,
    src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/RootTypeDeclaration.cs,
    src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/FluentStepDeclaration.cs
  </files>
  <action>
    1. Create `Generation/Shared/TypeParameterConstraintBuilder.cs`:
       - `internal static class TypeParameterConstraintBuilder` in namespace `Motiv.FluentFactory.Generator.Generation.Shared`
       - Single public static method: `ImmutableArray<TypeParameterConstraintClauseSyntax> Create(ImmutableArray<ITypeParameterSymbol> typeParameters)`
       - Uses `static Microsoft.CodeAnalysis.CSharp.SyntaxFactory`
       - For each type parameter with any constraints, builds a `TypeParameterConstraintClauseSyntax`
       - ALL constraint types use `constraintType.ToGlobalDisplayString()` (fixes the qualification bug)
       - Add XML doc header to the Create method
       - CRITICAL ORDERING DECISION: The 4 consumers currently have TWO different constraint orderings. RootTypeDeclaration/FluentStepDeclaration use: reference, value, types, constructor. FluentStepMethodDeclaration/FluentRootFactoryMethodDeclaration use: value, reference, constructor, types. The builder must pick ONE canonical order. Check test snapshots to determine which order is validated by more tests. Use that order and update any tests that break due to reordering. Recommendation from research: try the method declaration order (value, reference, constructor, types) first since method declarations are the most tested path.

    2. Replace constraint logic in FluentStepMethodDeclaration.cs:
       - Remove the entire `GetConstraintClauses` private method (lines 133-220)
       - In `CreateMethodDeclaration`, where it currently calls `GetConstraintClauses(method, ambientTypeParameters)`:
         - Collect the relevant type parameters into an `ImmutableArray<ITypeParameterSymbol>`:
           - If `ambientTypeParameters.IsEmpty` and `method.Return` is `TargetTypeReturn` with generic containing type: include `targetTypeReturn.Constructor.ContainingType.OriginalDefinition.TypeParameters`
           - Always include `method.TypeParameters.Select(tp => tp.TypeParameterSymbol)`
         - Call `TypeParameterConstraintBuilder.Create(combinedTypeParameters)`
       - Add `using Motiv.FluentFactory.Generator.Generation.Shared;` if not already present
       - The public `Create` method signatures MUST NOT change

    3. Replace constraint logic in FluentRootFactoryMethodDeclaration.cs:
       - Remove the entire `GetConstraintClauses` private method (lines 83-173)
       - In `Create`, where it currently calls `GetConstraintClauses(method, rootType)`:
         - Collect relevant type parameters similarly:
           - If `rootType?.IsGenericType != true` and `method.Return` is `TargetTypeReturn`: include target type parameters
           - Include `method.TypeParameters.Select(tp => tp.TypeParameterSymbol)`
         - Call `TypeParameterConstraintBuilder.Create(combinedTypeParameters)`
       - The public `Create` method signature MUST NOT change

    4. Replace constraint logic in RootTypeDeclaration.cs:
       - Replace the `CreateTypeParameterConstraints(INamedTypeSymbol rootType)` method body:
         - Convert `rootType.TypeParameters` to `ImmutableArray<ITypeParameterSymbol>` (they already are via `INamedTypeSymbol.TypeParameters`)
         - Call `TypeParameterConstraintBuilder.Create(rootType.TypeParameters)` (note: `TypeParameters` is already `ImmutableArray<ITypeParameterSymbol>`)
         - Return `List(result)` to convert from `ImmutableArray` to `SyntaxList`
       - Keep the early return guard for non-generic/empty type parameters

    5. Replace constraint logic in FluentStepDeclaration.cs:
       - Replace the `CreateTypeParameterConstraints(IReadOnlyList<ITypeParameterSymbol> typeParameters)` method body:
         - Convert the `IReadOnlyList` to `ImmutableArray`: `[..typeParameters]`
         - Call `TypeParameterConstraintBuilder.Create(immutableTypeParameters)`
         - Return `List(result)`
       - Keep the early return guard for empty type parameters
  </action>
  <verify>
    <automated>dotnet build src/Motiv.FluentFactory.Generator</automated>
  </verify>
  <done>TypeParameterConstraintBuilder.cs exists in Generation/Shared/. All 4 consumer files delegate to it. No inline constraint-building loops remain in any consumer. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test expectations for qualification bug and run full test suite</name>
  <files>src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs</files>
  <action>
    1. Search FluentFactoryGeneratorGenericTests.cs for all occurrences of method-level constraint patterns without `global::` prefix. Known locations:
       - Line ~833: `where T : struct, System.IComparable<T>` -> change to `where T : struct, global::System.IComparable<T>`
       - Line ~931: `where T : struct, System.IComparable<T>` -> change to `where T : struct, global::System.IComparable<T>`
    2. Search ALL test files for any other `where` clauses containing bare `System.` (without `global::`) in method-level constraints. Fix any found.
    3. If the constraint ordering unification from Task 1 changed the order for struct-level constraints (RootTypeDeclaration/FluentStepDeclaration), update those test expectations too. The struct-level declarations previously used reference-before-value ordering; if the unified builder uses value-before-reference, update accordingly.
    4. Run the full test suite: `dotnet test`
    5. If any tests fail due to ordering changes, examine the failure output, update test expectations to match the new canonical ordering, and re-run.
  </action>
  <verify>
    <automated>dotnet test</automated>
  </verify>
  <done>All tests pass. No occurrences of bare `System.` (without `global::`) remain in method-level type parameter constraints in test expectations. Generated output matches updated expectations.</done>
</task>

</tasks>

<verification>
- `dotnet test` passes all tests
- `grep -r "GlobalNamespaceStyle.Omitted" src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/` returns no results (bug is fixed)
- `grep -rn "TypeParameterConstraintBuilder" src/Motiv.FluentFactory.Generator/` shows usage in all 4 consumer files
- No inline constraint-building foreach loops remain in any of the 4 consumer files
</verification>

<success_criteria>
- TypeParameterConstraintBuilder.cs exists in Generation/Shared/ with a single Create method
- All 4 consumer files (FluentStepMethodDeclaration, FluentRootFactoryMethodDeclaration, RootTypeDeclaration, FluentStepDeclaration) call TypeParameterConstraintBuilder.Create instead of inline loops
- The qualification bug is fixed: all constraints use ToGlobalDisplayString()
- All tests pass with updated expectations
</success_criteria>

<output>
After completion, create `.planning/phases/08-syntax-generator-decomposition/08-01-SUMMARY.md`
</output>
