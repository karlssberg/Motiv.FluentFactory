---
phase: 08-syntax-generator-decomposition
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs
autonomous: true
requirements: [SYNTAX-02, XCUT-01, XCUT-02]

must_haves:
  truths:
    - "FluentRootFactoryMethodDeclaration is a thin orchestrator delegating to focused helpers"
    - "Type parameter syntax generation is in a focused method"
    - "Argument construction (field-sourced and method-sourced) is clearly separated"
    - "All existing tests pass with identical generated output"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs"
      provides: "Thin orchestrator for root factory method syntax"
      min_lines: 30
  key_links:
    - from: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs"
      to: "TargetTypeObjectCreationExpression.Create"
      via: "return value construction"
      pattern: "TargetTypeObjectCreationExpression\\.Create"
    - from: "src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs"
      to: "TypeParameterConstraintBuilder.Create"
      via: "constraint generation (from 08-01)"
      pattern: "TypeParameterConstraintBuilder\\.Create"
---

<objective>
Decompose FluentRootFactoryMethodDeclaration into a thin orchestrator with focused helper methods.

Purpose: After 08-01 extracts constraint building, FluentRootFactoryMethodDeclaration still has type parameter syntax generation, argument construction, and method declaration assembly tangled together. Extracting these into focused methods makes the class structurally similar to FluentStepMethodDeclaration -- both thin orchestrators assembling a MethodDeclarationSyntax from focused helpers.

Output: Thin FluentRootFactoryMethodDeclaration orchestrator.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-syntax-generator-decomposition/08-CONTEXT.md
@.planning/phases/08-syntax-generator-decomposition/08-RESEARCH.md
@.planning/phases/08-syntax-generator-decomposition/08-01-SUMMARY.md

@src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs

<interfaces>
<!-- Public API that MUST NOT change: -->
```csharp
// FluentRootFactoryMethodDeclaration entry point (caller: RootTypeDeclaration)
public static MethodDeclarationSyntax Create(
    IFluentMethod method,
    INamedTypeSymbol rootType)
```

<!-- After 08-01, GetConstraintClauses is already removed/replaced with TypeParameterConstraintBuilder.Create call -->
<!-- Remaining private methods to refactor: -->
<!-- GetTypeParameterSyntaxes(IFluentMethod, INamedTypeSymbol?) - type parameter list building -->
<!-- GetMethodDeclarationSyntax(IFluentMethod, ObjectCreationExpressionSyntax) - base declaration assembly -->
<!-- GetMethodSourcedArguments(IFluentMethod) - method parameter arguments -->
<!-- GetFieldSourcedArguments(IFluentMethod) - field-sourced arguments -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose FluentRootFactoryMethodDeclaration into thin orchestrator</name>
  <files>src/Motiv.FluentFactory.Generator/Generation/SyntaxElements/Methods/FluentRootFactoryMethodDeclaration.cs</files>
  <action>
    After 08-01, FluentRootFactoryMethodDeclaration will already have its constraint logic extracted. The remaining concerns:

    1. **Review the Create method** -- After constraint extraction, the public `Create` method should read as a clear sequence:
       - Get field-sourced and method-sourced arguments
       - Build return object expression via TargetTypeObjectCreationExpression.Create
       - Build base method declaration syntax
       - Check if type parameters are needed
       - Get type parameter syntaxes
       - Attach type parameters and constraints
       - Return

    2. **Ensure private methods are well-focused:**
       - `GetTypeParameterSyntaxes` -- already focused (type parameter list building with root type awareness). Review if it can be simplified after 08-01.
       - `GetMethodDeclarationSyntax` -- builds base declaration with attribute, modifier, body, parameter list, and doc trivia. This is the bulk of the logic. If it's still >30 lines, consider whether the parameter list attachment and the doc trivia attachment can be extracted into clearly named private methods.
       - `GetFieldSourcedArguments` and `GetMethodSourcedArguments` -- already small and focused.

    3. **Structural alignment with FluentStepMethodDeclaration** -- After both 08-02 and 08-03, these two classes should look structurally similar: thin orchestrator Create method, focused private helper methods, no inline constraint logic.

    4. The public `Create(IFluentMethod method, INamedTypeSymbol rootType)` signature MUST NOT change.

    5. Use discretion on exactly how to structure the private methods. The goal is each private method doing one thing with a clear name. If `GetMethodDeclarationSyntax` is already clear enough after constraint removal, further decomposition may not be warranted -- don't extract 5-line methods just to hit a line count.

    Target: FluentRootFactoryMethodDeclaration total file size ~80-120 lines (down from ~226 original, already reduced by constraint extraction in 08-01).
  </action>
  <verify>
    <automated>dotnet test</automated>
  </verify>
  <done>FluentRootFactoryMethodDeclaration's Create method reads as a clear sequence of delegating calls. Private methods each have a single clear responsibility. Public API unchanged. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet test` passes all tests
- FluentRootFactoryMethodDeclaration.cs is under 120 lines
- Create method reads as a clear orchestrator
- Public API signature unchanged
</verification>

<success_criteria>
- FluentRootFactoryMethodDeclaration is a thin orchestrator with focused private helper methods
- Structurally similar to FluentStepMethodDeclaration after its decomposition
- All tests pass with identical generated output
- SYNTAX-02 fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/08-syntax-generator-decomposition/08-03-SUMMARY.md`
</output>
