---
phase: 10-screaming-architecture-reorganization
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/Motiv.FluentFactory.Generator/Extensions/SymbolExtensions.cs
  - src/Motiv.FluentFactory.Generator/Extensions/SymbolDisplayExtensions.cs
  - src/Motiv.FluentFactory.Generator/Extensions/FluentModelExtensions.cs
  - src/Motiv.FluentFactory.Generator/Extensions/FluentAttributeExtensions.cs
  - src/Motiv.FluentFactory.Generator/Extensions/TypeParameterExtensions.cs
  - src/Motiv.FluentFactory.Generator/Extensions/TypeParameterFilterExtensions.cs
  - src/Motiv.FluentFactory.Generator/ConstructorAnalysis/FluentConstructorContextFactory.cs
  - src/Motiv.FluentFactory.Generator/ConstructorAnalysis/FluentFactoryMetadataReader.cs
  - src/Motiv.FluentFactory.Generator/MultiMethod.cs
  - src/Motiv.FluentFactory.Generator/MultiMethodDocumentationReader.cs
  - src/Motiv.FluentFactory.Generator/SyntaxGeneration/FluentStepMethodDeclaration.cs
  - src/Motiv.FluentFactory.Generator/SyntaxGeneration/StepMethodTypeParameterResolver.cs
  - src/Motiv.FluentFactory.Generator/SyntaxGeneration/FluentRootFactoryMethodDeclaration.cs
  - src/Motiv.FluentFactory.Generator/SyntaxGeneration/RootMethodTypeParameterResolver.cs
  - src/Motiv.FluentFactory.Generator/ModelBuilding/FluentMethodSelector.cs
  - src/Motiv.FluentFactory.Generator/ModelBuilding/FluentMethodFactory.cs
autonomous: true
requirements: [DECOMP-04, XCUT-01, XCUT-02]

must_haves:
  truths:
    - "No individual source file exceeds approximately 150 lines"
    - "All existing tests pass with identical generated output after splitting"
    - "Split files have clear single responsibilities"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator/Extensions/SymbolDisplayExtensions.cs"
      provides: "Symbol display formatting methods extracted from SymbolExtensions"
      contains: "ToGlobalDisplayString"
    - path: "src/Motiv.FluentFactory.Generator/Extensions/FluentAttributeExtensions.cs"
      provides: "Fluent attribute helpers extracted from FluentModelExtensions"
      contains: "GetFluentMethodName"
    - path: "src/Motiv.FluentFactory.Generator/ConstructorAnalysis/FluentFactoryMetadataReader.cs"
      provides: "Metadata extraction extracted from FluentConstructorContextFactory"
      contains: "GetFluentFactoryMetadata"
    - path: "src/Motiv.FluentFactory.Generator/MultiMethodDocumentationReader.cs"
      provides: "Documentation extraction extracted from MultiMethod"
      contains: "GetDocumentationSummary"
  key_links:
    - from: "src/Motiv.FluentFactory.Generator/Extensions/SymbolExtensions.cs"
      to: "src/Motiv.FluentFactory.Generator/Extensions/SymbolDisplayExtensions.cs"
      via: "shared static format fields or method calls"
      pattern: "SymbolDisplayExtensions|SymbolExtensions"
---

<objective>
Split all source files exceeding approximately 150 lines into focused, single-responsibility files to satisfy DECOMP-04.

Purpose: Every file should be "bite-sized" -- small enough to understand at a glance, with a clear single responsibility.

Output: All previously oversized files split into focused sub-files, each under ~150 lines, all tests passing identically.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-screaming-architecture-reorganization/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split extension classes exceeding 150 lines</name>
  <files>
    src/Motiv.FluentFactory.Generator/Extensions/SymbolExtensions.cs
    src/Motiv.FluentFactory.Generator/Extensions/SymbolDisplayExtensions.cs
    src/Motiv.FluentFactory.Generator/Extensions/FluentModelExtensions.cs
    src/Motiv.FluentFactory.Generator/Extensions/FluentAttributeExtensions.cs
    src/Motiv.FluentFactory.Generator/Extensions/TypeParameterExtensions.cs
    src/Motiv.FluentFactory.Generator/Extensions/TypeParameterFilterExtensions.cs
  </files>
  <action>
Split the three extension files exceeding ~150 lines. All new files keep namespace `Motiv.FluentFactory.Generator` (matching Phase 09 convention for cross-cutting accessibility).

**SymbolExtensions.cs (281 lines) -> 2 files:**

1. **SymbolDisplayExtensions.cs** (~90 lines) - Symbol display formatting concern:
   - `TypeNameOnlyFormat`, `GlobalQualifiedFormat`, `FullFormat` fields
   - `ToGlobalDisplayString()` method
   - `ToFullDisplayString()` method
   - `ToUnqualifiedDisplayString()` method
   - `AccessibilityToSyntaxKind()` method

2. **SymbolExtensions.cs** (~190 lines) - Type analysis, assignability, and attribute concern:
   - `IsOpenGenericType()` + `ContainsUnboundGenericTypes()` (private helper)
   - `IsPartial()`
   - `CanBeCustomStep()`
   - `IsAssignable()` (the large assignability check)
   - `ReplaceTypeParameters()`
   - `HasAttribute()` overloads
   - `GetAttributes()` overloads

Note: If SymbolExtensions is still over 150 lines after splitting out display methods, further split the attribute methods (HasAttribute, GetAttributes) into a separate `SymbolAttributeExtensions.cs`.

**FluentModelExtensions.cs (248 lines) -> 2 files:**

1. **FluentModelExtensions.cs** (~100 lines) - Method display and constructor analysis:
   - `FullFormat` field
   - `ToDisplayString()` + `SerializeFluentMethod()` (private helper)
   - `FindUnreachableConstructors()`
   - `GetGenericTypeArguments()`

2. **FluentAttributeExtensions.cs** (~150 lines) - Fluent attribute reading and configuration:
   - `GetFluentMethodName()`
   - `GetMultipleFluentMethodSymbols()`
   - `GetFluentMethodPriority()`
   - `GetLocationAtIndex()`
   - `GetAttribute()` (the IParameterSymbol overload)

**TypeParameterExtensions.cs (161 lines) -> 2 files:**

Read the file first to identify a good split point. The file likely has type parameter extraction/conversion methods and type parameter filtering/set operations. Split by these two concerns:

1. **TypeParameterExtensions.cs** (~80-90 lines) - Core type parameter extraction and conversion
2. **TypeParameterFilterExtensions.cs** (~80-90 lines) - Type parameter filtering, union, except, and set operations

After splitting, verify build: `dotnet build src/Motiv.FluentFactory.Generator/`
  </action>
  <verify>
    <automated>cd C:/Dev/Motiv.FluentFactory && dotnet build src/Motiv.FluentFactory.Generator/ --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <done>SymbolExtensions, FluentModelExtensions, and TypeParameterExtensions are each split into two focused files under ~150 lines. Project builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Split non-extension classes exceeding 150 lines and verify all tests</name>
  <files>
    src/Motiv.FluentFactory.Generator/ConstructorAnalysis/FluentConstructorContextFactory.cs
    src/Motiv.FluentFactory.Generator/ConstructorAnalysis/FluentFactoryMetadataReader.cs
    src/Motiv.FluentFactory.Generator/MultiMethod.cs
    src/Motiv.FluentFactory.Generator/MultiMethodDocumentationReader.cs
    src/Motiv.FluentFactory.Generator/SyntaxGeneration/FluentStepMethodDeclaration.cs
    src/Motiv.FluentFactory.Generator/SyntaxGeneration/StepMethodTypeParameterResolver.cs
    src/Motiv.FluentFactory.Generator/SyntaxGeneration/FluentRootFactoryMethodDeclaration.cs
    src/Motiv.FluentFactory.Generator/SyntaxGeneration/RootMethodTypeParameterResolver.cs
    src/Motiv.FluentFactory.Generator/ModelBuilding/FluentMethodSelector.cs
    src/Motiv.FluentFactory.Generator/ModelBuilding/FluentMethodFactory.cs
  </files>
  <action>
Split the remaining files exceeding ~150 lines. Use the new namespace locations from Plan 01.

**FluentConstructorContextFactory.cs (204 lines) -> 2 files:**

1. **FluentConstructorContextFactory.cs** (~100 lines) - Core constructor context creation:
   - `CreateConstructorContexts()` method
   - `DeDuplicateFluentConstructors()` method
   - `ChooseOverridingConstructors()` method

2. **FluentFactoryMetadataReader.cs** (~100 lines) - Metadata extraction and option conversion:
   - `GetFluentFactoryMetadata()` method
   - `ConvertToFluentFactoryGeneratorOptions()` method
   - Related private helpers

Both in namespace `Motiv.FluentFactory.Generator.ConstructorAnalysis`.

**MultiMethod.cs (190 lines) -> 2 files:**

1. **MultiMethod.cs** (~100-120 lines) - The class definition, constructor, properties, GetMethodParameters, GetTypeParameters, GetResolvedTypeParameters

2. **MultiMethodDocumentationReader.cs** (~70-80 lines) - Static documentation extraction helper class:
   - `GetDocumentationSummary()` (make internal static in new class)
   - `ExtractParameterDocumentation()` (private helper)
   - `ExtractSummaryFromDocumentation()` (private helper)
   - `GetParameterDocumentation()` (private helper)

   MultiMethod constructor calls `MultiMethodDocumentationReader.GetDocumentationSummary(...)` instead of the private method.

Both in namespace `Motiv.FluentFactory.Generator`.

**FluentStepMethodDeclaration.cs (212 lines) -> 2 files:**

1. **FluentStepMethodDeclaration.cs** (~120 lines) - The two Create() overloads, CreateMethodDeclaration, GetDocumentationTrivia, AttachParameterList, CreateStepConstructorArguments, GetDocumentationLinesWithParameters

2. **StepMethodTypeParameterResolver.cs** (~90 lines) - Type parameter resolution:
   - `GetMethodTypeParameterSyntaxes()`
   - `AttachTypeParameters()`
   - `GetCombinedTypeParameters()`

Both in namespace `Motiv.FluentFactory.Generator.SyntaxGeneration`.

**FluentRootFactoryMethodDeclaration.cs (172 lines) -> 2 files:**

1. **FluentRootFactoryMethodDeclaration.cs** (~100 lines) - Create(), CreateBaseMethodDeclaration(), GetMethodSourcedArguments(), GetFieldSourcedArguments()

2. **RootMethodTypeParameterResolver.cs** (~70 lines) - Type parameter resolution:
   - `AttachTypeParametersAndConstraints()`
   - `HasTypeParametersToAdd()`
   - `GetTypeParameterSyntaxes()`
   - `GetCombinedTypeParameters()`

Both in namespace `Motiv.FluentFactory.Generator.SyntaxGeneration`.

**FluentMethodSelector.cs (216 lines) -> 2 files:**

1. **FluentMethodSelector.cs** (~120 lines) - Method selection orchestration:
   - `ConvertNodeToFluentMethods()` - the main public method
   - `ChooseCandidateFluentMethod()` - static selection logic
   - `ValidateMultipleFluentMethodCompatibility()` - validation
   - `MergeConstructorMetadata()` - metadata merging
   - `NormalizedConverterMethod()` - normalization helper
   - `SelectedFluentMethod` record

2. **FluentMethodFactory.cs** (~100 lines) - Method creation:
   - `CreateFluentMethods()` method (the large private method that creates RegularMethod/MultiMethod instances)
   - Constructor takes same dependencies as FluentMethodSelector
   - FluentMethodSelector instantiates FluentMethodFactory and delegates to it

Both in namespace `Motiv.FluentFactory.Generator.ModelBuilding`.

**FluentModelFactory.cs (161 lines):** At 161 lines, this is borderline. The success criteria says "approximately 150 lines." If it reads cleanly as a single orchestrator, leave it. If a natural split presents itself (e.g., extracting a small helper), apply it.

After splitting all files, run full build and test suite to verify everything passes.
  </action>
  <verify>
    <automated>cd C:/Dev/Motiv.FluentFactory && dotnet test src/Motiv.FluentFactory.Generator.Tests/ 2>&1 | tail -10</automated>
  </verify>
  <done>All files exceeding ~150 lines have been split into focused files. No file exceeds approximately 150 lines. All 174 tests pass with identical generated output.</done>
</task>

</tasks>

<verification>
1. `find src/Motiv.FluentFactory.Generator/ -name "*.cs" -not -path "*/obj/*" -not -path "*/bin/*" | xargs wc -l | sort -rn | head -15` shows no file significantly exceeding 150 lines
2. `dotnet test src/Motiv.FluentFactory.Generator.Tests/` - all 174 tests pass
3. `dotnet build src/Motiv.FluentFactory.Example/` - example project builds
</verification>

<success_criteria>
- No individual source file exceeds approximately 150 lines
- Each split file has a clear, single responsibility
- All 174 tests pass with identical generated output
- Build succeeds with zero errors and zero warnings (WarningsAsErrors is enabled)
</success_criteria>

<output>
After completion, create `.planning/phases/10-screaming-architecture-reorganization/10-02-SUMMARY.md`
</output>
