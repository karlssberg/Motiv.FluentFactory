---
phase: 16-diagnostic-alignment
plan: 03
subsystem: diagnostics
tags: [refactor, vocabulary, naming-alignment, release-tracking, diagnostics]
one-liner: Rewrote AnalyzerReleases.Unshipped.md from 18 to 48 rows synchronized with FluentDiagnostics.cs and closed Phase 16 with the DIAG-04 repo-wide grep gate
dependency-graph:
  requires:
    - "16-01 (FluentDiagnostics.cs titles finalized with post-rename vocabulary)"
    - "16-02 (sibling Diagnostics/*.cs files drift-free)"
  provides:
    - "Complete analyzer release tracking file (48 rows, Converj category, titles verbatim from source)"
    - "Phase 16 completion gate: zero FluentFactory/MFFG/FluentConstructor drift in src/Converj.Generator/Diagnostics/"
    - "Phase 16 completion gate: zero FluentFactory category string in src/Converj.Generator/"
  affects:
    - "src/Converj.Generator/AnalyzerReleases.Unshipped.md (full rewrite, 18 → 48 rows)"
tech-stack:
  added: []
  patterns:
    - "Source-of-truth sync: release tracking file generated by reading descriptors and copying post-rename titles verbatim"
    - "Numeric-order row layout with gap preservation (CVJG0034 missing is reflected as absence, not a placeholder row)"
key-files:
  created: []
  modified:
    - "src/Converj.Generator/AnalyzerReleases.Unshipped.md (full rewrite from 18 rows to 48, category flipped to Converj, notes verbatim from descriptor titles)"
decisions:
  - "Row count is 48, not the plan's predicted 47 — FluentDiagnostics.cs actually contains 48 descriptors (CVJG0001..CVJG0049 minus CVJG0034). Plan 16-03 and 16-CONTEXT.md both said '47 descriptors with one gap at 0034' which is internally inconsistent (48 - 1 gap = 48 present, not 47). Reconciled by treating FluentDiagnostics.cs as the single source of truth per the plan's stated objective."
  - "Adapted Task 1/Task 2 verification scripts inline to expect 48 rather than 47 — this is the correct synchronization target"
metrics:
  duration_minutes: 2
  tasks_completed: 3
  files_modified: 1
  completed_date: "2026-04-12"
---

# Phase 16 Plan 03: AnalyzerReleases.Unshipped.md Completion Summary

## Objective

Complete `src/Converj.Generator/AnalyzerReleases.Unshipped.md` by rewriting it to reflect every descriptor in `FluentDiagnostics.cs` with Category=Converj and notes that match the post-rename titles verbatim. Run the phase-level DIAG-04 repo-wide grep check to close Phase 16.

## Outcome

`AnalyzerReleases.Unshipped.md` now contains 48 rows (one per descriptor in `FluentDiagnostics.cs`), ordered strictly by rule ID, with Category=Converj and Notes copied verbatim from `FluentDiagnostics.cs` titles. The DIAG-04 repo-wide grep returns zero hits. `dotnet build -p:TreatWarningsAsErrors=true` succeeds with zero warnings and all 415 tests pass.

## Tasks Executed

| # | Task                                                                                    | Commit    | Files Modified                                             |
| - | --------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------- |
| 1 | Extract post-rename descriptor metadata from FluentDiagnostics.cs (in-memory, no write) | —         | (extraction-only task; no modifications)                   |
| 2 | Rewrite AnalyzerReleases.Unshipped.md with all 48 rows, Category=Converj, sorted by ID  | `a6308c7` | `src/Converj.Generator/AnalyzerReleases.Unshipped.md`      |
| 3 | Phase-level DIAG-04 repo-wide grep gate and full build/test                             | —         | (verification gate; no modifications)                      |

## Changes Applied

### AnalyzerReleases.Unshipped.md — Full Rewrite

**Before:** 18 rows (CVJG0001–0017 + CVJG0044 out-of-order). Category column already flipped to Converj by plan 16-01 (Rule 3 scope expansion to unblock RS2001).

**After:** 48 rows (CVJG0001–CVJG0049 minus CVJG0034 gap). All rows:
- Category = `Converj`
- Severity extracted from `defaultSeverity: DiagnosticSeverity.X` in `FluentDiagnostics.cs`
- Notes column copied verbatim from post-rename descriptor `title:` fields
- Strict numeric ordering by rule ID

### Rule ID Sequence

`CVJG0001, CVJG0002, CVJG0003, CVJG0004, CVJG0005, CVJG0006, CVJG0007, CVJG0008, CVJG0009, CVJG0010, CVJG0011, CVJG0012, CVJG0013, CVJG0014, CVJG0015, CVJG0016, CVJG0017, CVJG0018, CVJG0019, CVJG0020, CVJG0021, CVJG0022, CVJG0023, CVJG0024, CVJG0025, CVJG0026, CVJG0027, CVJG0028, CVJG0029, CVJG0030, CVJG0031, CVJG0032, CVJG0033, CVJG0035, CVJG0036, CVJG0037, CVJG0038, CVJG0039, CVJG0040, CVJG0041, CVJG0042, CVJG0043, CVJG0044, CVJG0045, CVJG0046, CVJG0047, CVJG0048, CVJG0049`

**Gap:** CVJG0034 (not present in `FluentDiagnostics.cs`). Reflected as absence, not placeholder.

### Byte-for-byte Title Match Verification

A node script re-read `FluentDiagnostics.cs`, extracted each descriptor's `(id, severity, title)` tuple, sorted by id, and compared each row of the Unshipped file element-wise. All 48 rows match the source exactly. See Verification Results below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Plan expected 47 rows; actual descriptor count is 48**

- **Found during:** Task 1 descriptor extraction
- **Issue:** The plan's Task 1 verification script asserts `ids.length === 47` and would have halted execution. However, `FluentDiagnostics.cs` actually contains 48 descriptors (IDs CVJG0001..CVJG0049 with only CVJG0034 missing = 48 descriptors). The plan text, 16-CONTEXT.md, and the prior plan 16-01 SUMMARY.md all repeat "47 descriptors with one gap at 0034", but the arithmetic is internally inconsistent: 49 − 1 = 48, not 47. The 47 figure appears to have been an off-by-one carried forward into the planning documents.
- **Root cause:** Historical miscounting in the planning context. The source of truth has been stable: plan 16-01 applied its full-47 audit to what was actually a full-48 audit (all descriptors were read and the commit stood up to build/test).
- **Why in scope:** The plan's explicit stated goal is "synchronize `AnalyzerReleases.Unshipped.md` with `FluentDiagnostics.cs`" where `FluentDiagnostics.cs` is the declared source of truth. Synchronizing to a stale 47-count would violate the plan's own purpose. Rule 1 (bug fix) applies.
- **Fix:** Adapted Task 1 and Task 2 verification scripts inline to assert 48 rows and byte-for-byte equivalence against the source file. Rewrote the Unshipped file with 48 rows. Documented as a deviation for phase retrospective traceability.
- **Files modified:** `src/Converj.Generator/AnalyzerReleases.Unshipped.md` (already scoped to this plan)
- **Commit:** `a6308c7` (Task 2)
- **Downstream impact:** None — phase-level DIAG-04 grep, build, and test gates all pass. Phase 20 retrospective should correct the "47" figure to "48" wherever it appears in planning docs.

### Authentication Gates

None.

### Defects Discovered During Alignment

**1. [Pre-existing] `AnalyzerReleases.Unshipped.md` was 18/48 before Phase 16**

Per the plan's traceability requirement, this is the canonical defect being documented: before Phase 16, the release-tracking file contained only 18 of 48 unshipped descriptors. The file drifted from the descriptor source over time and was not kept in sync as new descriptors were added during v2.0 work (CVJG0018–CVJG0049 + the minor rule 0044-reordering). This plan fills the 30-row gap and re-establishes the descriptor file as the single source of truth, with the Unshipped file as a verbatim mirror.

**Before:** 18 rows (covering CVJG0001–CVJG0017 plus CVJG0044 out of order)
**After:** 48 rows (complete coverage, numeric ordering, gap at CVJG0034 preserved)

## Verification Results

### Task 2 Verification (byte-for-byte match)

```
row count: 48
OK: 48 rows, Converj category, numeric order, titles verbatim match
```

Node script compared each Unshipped row against the descriptor tuple extracted from `FluentDiagnostics.cs`. All 48 rows matched on `(id, severity, title)`.

### Task 3 — DIAG-04 Grep (Phase-level)

```
$ git grep -nE 'MFFG|"FluentFactory"|FluentConstructor' -- src/Converj.Generator/Diagnostics/
(zero hits — exit code 1)
```

### Task 3 — Category Literal Check (Phase-level)

```
$ git grep -n '"FluentFactory"' -- src/Converj.Generator/
(zero hits — exit code 1)
```

### Task 3 — Row Count Sanity

```
$ grep -cE "^CVJG[0-9]{4}" src/Converj.Generator/AnalyzerReleases.Unshipped.md
48
```

### Task 3 — Build (`dotnet build -p:TreatWarningsAsErrors=true`)

```
Build succeeded.
    0 Warning(s)
    0 Error(s)
```

All six projects build clean:
- `Converj.Attributes` (netstandard2.0)
- `Converj.Generator` (netstandard2.0)
- `Converj` (netstandard2.0 NuGet wrapper)
- `Converj.Example` (net10.0)
- `Converj.Tests` (net10.0)
- `Converj.Generator.Tests` (net10.0)

### Task 3 — Test Suite (`dotnet test --no-build`)

```
Passed!  - Failed:     0, Passed:    53, Skipped:     0, Total:    53 — Converj.Tests.dll (net10.0)
Passed!  - Failed:     0, Passed:   362, Skipped:     0, Total:   362 — Converj.Generator.Tests.dll (net10.0)
```

Grand total: **415 passed, 0 failed, 0 skipped.**

## Phase 16 Success Criteria Checklist

From ROADMAP.md Phase 16 success criteria (and the plan's `<verification>` block):

- [x] **Success criterion 1 (DIAG-01):** All descriptors in `FluentDiagnostics.cs` use `Category = "Converj"` — closed by plan 16-01.
- [x] **Success criterion 2 (DIAG-02):** Titles and messages no longer say "Factory" when referring to the fluent root — closed by plan 16-01 (CVJG0011, 0012, 0013, 0014, 0015, 0026, 0031 message text and XML docs).
- [x] **Success criterion 3 (DIAG-03):** `AnalyzerReleases.Unshipped.md` reflects the new category string, all 48 rows, consistent with descriptors — **closed by this plan (16-03)**.
- [x] **Success criterion 4 (DIAG-04):** `git grep -nE 'MFFG|"FluentFactory"|FluentConstructor' -- src/Converj.Generator/Diagnostics/` returns zero hits — **closed by this plan's Task 3 grep gate** (plans 16-01 and 16-02 eliminated the hits; 16-03 confirms).
- [x] **Success criterion 5 (BEHAV-01/02):** `dotnet build` succeeds with zero warnings, `dotnet test` passes every existing test — **confirmed by this plan's Task 3** (415/415 passing).

## Requirements Closed

- **DIAG-03** — `AnalyzerReleases.Unshipped.md` rewritten to 48 rows with Converj category, ordered by rule ID, notes verbatim from descriptor titles. Source-of-truth sync complete.
- **DIAG-04** — Repo-wide grep for `MFFG|"FluentFactory"|FluentConstructor` in `src/Converj.Generator/Diagnostics/` returns zero hits. Category literal check across entire generator project returns zero hits. Phase 16 drift-removal gate closed.

## Self-Check: PASSED

- [x] `src/Converj.Generator/AnalyzerReleases.Unshipped.md` exists and contains 48 CVJG rows (verified: `grep -c` returned `48`)
- [x] Every row uses `Converj` in the Category column (verified: regex `^CVJG\d{4} \| Converj \|` matches all 48)
- [x] Row ordering is strictly numeric by rule ID (verified: array equals sorted copy)
- [x] Notes column matches `FluentDiagnostics.cs` titles byte-for-byte (verified: element-wise comparison)
- [x] File contains zero `FluentFactory` substrings (verified: `md.includes('FluentFactory') === false`)
- [x] Commit `a6308c7` exists on `main` branch (verified via git commit output)
- [x] DIAG-04 grep returns zero hits
- [x] Full build succeeds with zero warnings, zero errors
- [x] All 415 tests pass (0 failed, 0 skipped)
- [x] Phase 16 success criteria 1–5 all satisfied
