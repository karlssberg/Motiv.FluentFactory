---
phase: 16-diagnostic-alignment
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - src/Converj.Generator/AnalyzerReleases.Unshipped.md
autonomous: true
requirements:
  - DIAG-03
  - DIAG-04
must_haves:
  truths:
    - "AnalyzerReleases.Unshipped.md contains exactly 47 rows in the New Rules table, one per descriptor in FluentDiagnostics.cs"
    - "Every row uses Category = Converj"
    - "Every row's Notes column matches the post-rename title verbatim from FluentDiagnostics.cs"
    - "Rows are ordered numerically by Rule ID (CVJG0001 → CVJG0049 with the known gap at CVJG0034)"
    - "Final repo-wide grep: git grep -nE 'MFFG|\\\"FluentFactory\\\"|FluentConstructor' -- src/Converj.Generator/Diagnostics/ returns zero hits"
    - "dotnet build succeeds with zero warnings"
    - "dotnet test passes every existing test"
  artifacts:
    - path: "src/Converj.Generator/AnalyzerReleases.Unshipped.md"
      provides: "Release tracking for all 47 unshipped diagnostic rules with Converj category"
      contains: "CVJG0001 | Converj"
      min_lines: 53
  key_links:
    - from: "AnalyzerReleases.Unshipped.md Notes column"
      to: "FluentDiagnostics.cs descriptor `title:` fields"
      via: "verbatim copy, one row per descriptor"
      pattern: "CVJG\\d{4} \\| Converj \\|"
---

<objective>
Complete the `AnalyzerReleases.Unshipped.md` release-tracking file. Today the file lists only 18 of 47 descriptors. This plan fills it to 47 rows, sets every Category column to `Converj`, copies each row's Notes column from the post-rename `title:` in `FluentDiagnostics.cs` (which Plan 16-01 finalized), and orders rows numerically by Rule ID. Final task is the phase-level DIAG-04 repo-wide grep check that closes Phase 16.

Purpose: Closes DIAG-03 (release file category string) and is the literal gate for DIAG-04 (zero drift hits across `src/Converj.Generator/Diagnostics/`). Also documents the completeness gap (29 missing rows) as a defect discovered during Phase 16 alignment.

Output: A complete 47-row `AnalyzerReleases.Unshipped.md` synchronized with `FluentDiagnostics.cs`, and a green phase-completion grep check.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-diagnostic-alignment/16-CONTEXT.md
@.planning/phases/16-diagnostic-alignment/16-01-SUMMARY.md
@.planning/phases/16-diagnostic-alignment/16-02-SUMMARY.md
@src/Converj.Generator/Diagnostics/FluentDiagnostics.cs
@src/Converj.Generator/AnalyzerReleases.Unshipped.md

<interfaces>
<!-- AnalyzerReleases.Unshipped.md format and current state -->

Current file (18 rows — confirmed from pre-read):
```
; Unshipped analyzer releases
; https://github.com/dotnet/roslyn-analyzers/blob/main/src/Microsoft.CodeAnalysis.Analyzers/ReleaseTrackingAnalyzers.Help.md

### New Rules

Rule ID | Category | Severity | Notes
--------|----------|----------|-------
CVJG0001 | FluentFactory | Error | Unreachable fluent constructor
CVJG0002 | FluentFactory | Warning | Multiple fluent method contains superseded method
...
CVJG0017 | FluentFactory | Warning | Empty TerminalVerb with TerminalMethod.None
CVJG0044 | FluentFactory | Warning | FluentTarget on instance method
```

Issues:
1. Category = `FluentFactory` on every row — must become `Converj` (DIAG-03).
2. Only 18 of 47 descriptors listed. Missing: CVJG0018 through CVJG0049 (minus 0034 if it is a known gap) and CVJG0044 is present but 0018–0043 (except 0044) and 0045–0049 are missing. Executor must reconcile the exact list against `FluentDiagnostics.cs` after Plan 16-01's rename pass.
3. Notes column is free-form — should match the post-rename descriptor `title:` verbatim so the file is internally consistent with the source of truth.
4. Row order is mostly numeric but CVJG0044 is out of place. Final file must be strictly numeric by Rule ID.

**Data source:** After Plan 16-01 completes, read `src/Converj.Generator/Diagnostics/FluentDiagnostics.cs` and extract a tuple per descriptor:
- Rule ID (from `id: "CVJG####"`)
- Severity (from `defaultSeverity: DiagnosticSeverity.X` → map to `Error` | `Warning` | `Info` | `Hidden`)
- Title (from `title: "..."` — the POST-RENAME value)

Sort the extracted list numerically by Rule ID. Emit one row per descriptor with Category = `Converj`.

**Dependency:** This plan depends on Plans 16-01 and 16-02 completing first. 16-01 finalizes the descriptor titles that feed the Notes column; 16-02 is not data-dependent but shares the phase-end DIAG-04 grep check, so running it first keeps the grep check authoritative.

**Documentation note:** The pre-existing 18-row partial state is a defect — the file drifted from the descriptor source over time and was not kept in sync as new descriptors were added in v2.0. Record this in the plan SUMMARY.md as "defect discovered during Phase 16: AnalyzerReleases.Unshipped.md was 18/47 before this phase" so the phase retrospective has traceability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract post-rename descriptor metadata from FluentDiagnostics.cs</name>
  <files>(no modifications — extraction/read task producing an in-memory list)</files>
  <action>
Read `src/Converj.Generator/Diagnostics/FluentDiagnostics.cs` (post Plan 16-01 state). For every `DiagnosticDescriptor` field, extract a tuple `(ruleId, severity, title)`:

- `ruleId`: the string after `id:` (e.g., `"CVJG0001"`)
- `severity`: the enum after `defaultSeverity: DiagnosticSeverity.` → display as `Error` / `Warning` / `Info` / `Hidden`
- `title`: the exact literal after `title:` (post-rename — must match the file on disk byte-for-byte after Plan 16-01's vocabulary pass)

Verification for this extraction step:
- Count the extracted tuples; must equal 47 (the known full count per 16-CONTEXT.md). If count differs, stop and flag — either Plan 16-01 dropped or added a descriptor unintentionally, or the pre-read count is stale.
- Sort numerically by rule ID ascending. Note the known gap (per 16-CONTEXT.md, there is one gap in the sequence — CVJG0034 per current count — the executor verifies during extraction).

Do not write the file yet — this task produces the data that Task 2 writes.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('src/Converj.Generator/Diagnostics/FluentDiagnostics.cs','utf8'); const ids = [...src.matchAll(/id:\\s*\"(CVJG\\d{4})\"/g)].map(m=>m[1]); const titles = [...src.matchAll(/title:\\s*\"([^\"]+)\"/g)].map(m=>m[1]); const sevs = [...src.matchAll(/defaultSeverity:\\s*DiagnosticSeverity\\.(\\w+)/g)].map(m=>m[1]); console.log('ids='+ids.length,'titles='+titles.length,'severities='+sevs.length); if (ids.length !== 47) { console.error('EXPECTED 47 descriptors, got '+ids.length); process.exit(1); } console.log('OK: 47 descriptors extracted');"</automated>
  </verify>
  <done>
- 47 tuples extracted, sorted numerically by rule ID
- Each tuple's title is verbatim from the post-rename `FluentDiagnostics.cs`
- Severity mapped correctly to Error / Warning / Info / Hidden
- If extraction count ≠ 47, escalate — do not proceed
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite AnalyzerReleases.Unshipped.md with all 47 rows, Category=Converj, ordered by rule ID</name>
  <files>src/Converj.Generator/AnalyzerReleases.Unshipped.md</files>
  <action>
Overwrite `src/Converj.Generator/AnalyzerReleases.Unshipped.md` with:

```
; Unshipped analyzer releases
; https://github.com/dotnet/roslyn-analyzers/blob/main/src/Microsoft.CodeAnalysis.Analyzers/ReleaseTrackingAnalyzers.Help.md

### New Rules

Rule ID | Category | Severity | Notes
--------|----------|----------|-------
CVJG0001 | Converj | <severity> | <title>
CVJG0002 | Converj | <severity> | <title>
...
CVJG0049 | Converj | <severity> | <title>
```

Rules for the rewrite:
- Every row: `Category = Converj`.
- Every row's `Notes` column: exact post-rename title from Task 1, no rewording, no truncation, no added punctuation.
- Rows in strictly numeric order by Rule ID.
- The known gap in the ID sequence (e.g., no CVJG0034 descriptor) is reflected as a missing row, NOT a placeholder row. The file lists exactly 47 rows, not 49.
- Preserve the two leading comment lines (`;`) and the `### New Rules` header. Keep the Markdown table header and separator lines.
- No trailing blank line beyond the standard single newline at EOF.

Do not modify any other content in the file (there is none — the current file is purely the release table).

After writing, re-count rows:
```bash
grep -cE "^CVJG[0-9]{4}" src/Converj.Generator/AnalyzerReleases.Unshipped.md
```
Must return `47`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const md=fs.readFileSync('src/Converj.Generator/AnalyzerReleases.Unshipped.md','utf8'); const rows=md.split('\\n').filter(l=>/^CVJG\\d{4} \\| Converj \\| (Error|Warning|Info|Hidden) \\| /.test(l)); if (rows.length !== 47) { console.error('Expected 47 rows, got '+rows.length); process.exit(1); } const ids=rows.map(r=>r.split(' | ')[0]); const sorted=[...ids].sort(); if (JSON.stringify(ids) !== JSON.stringify(sorted)) { console.error('Rows not in numeric order'); process.exit(1); } if (md.includes('FluentFactory')) { console.error('Found FluentFactory drift'); process.exit(1); } console.log('OK: 47 rows, Converj category, numeric order, no drift');"</automated>
  </verify>
  <done>
- File contains exactly 47 rows in the New Rules table
- Every row has `Category = Converj`
- Rows are in strict numeric order by rule ID
- Notes column matches post-rename descriptor titles verbatim
- File contains zero `FluentFactory` substrings
- Preserved leading comments and `### New Rules` header
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-level DIAG-04 repo-wide grep gate and full build/test</name>
  <files>(no modifications — phase-completion gate)</files>
  <action>
Run the literal DIAG-04 success-criterion grep and the behavior-preservation build/test gate:

**Step A — DIAG-04 grep (MUST return zero hits):**
```bash
git grep -nE "MFFG|\"FluentFactory\"|FluentConstructor" -- src/Converj.Generator/Diagnostics/
```

If this returns any hit, Phase 16 has residual drift. Investigate:
- Hit in `FluentDiagnostics.cs` → Plan 16-01 missed a spot, escalate and fix there
- Hit in `UnreachableConstructorAnalyzer.cs` or sibling files → Plan 16-02 missed a spot, escalate and fix there
- Hit in `IgnoredMultiMethodWarningFactory.cs` that is the class name itself on line 7 (`internal class IgnoredMultiMethodWarningFactory(`) — this is Phase 18 territory and IS expected to still be present. The grep pattern `"FluentFactory"` (with surrounding double-quotes, matching a category literal) will NOT match the class declaration. The `FluentConstructor` branch of the alternation also does NOT match `IgnoredMultiMethodWarningFactory` (no substring). The `MFFG` branch is legacy and should be absent repo-wide. **If the grep returns zero hits, Phase 18's deferred rename is not a Phase 16 concern.**

**Step B — Category constant confirmation:**
```bash
git grep -n '"FluentFactory"' -- src/Converj.Generator/
```
Must return zero hits anywhere in the generator project (not just Diagnostics/).

**Step C — Build and test:**
```bash
dotnet build /warnaserror
dotnet test --no-build
```
Both must pass clean.

**Step D — Row count sanity:**
```bash
grep -cE "^CVJG[0-9]{4}" src/Converj.Generator/AnalyzerReleases.Unshipped.md
```
Must return `47`.

If any step fails, this plan is incomplete — fix the root cause (which may live in Plan 16-01 or 16-02's file scope) and re-run all four steps.
  </action>
  <verify>
    <automated>bash -c 'set -e; HITS=$(git grep -nE "MFFG|\"FluentFactory\"|FluentConstructor" -- src/Converj.Generator/Diagnostics/ || true); if [ -n "$HITS" ]; then echo "DIAG-04 FAILED:"; echo "$HITS"; exit 1; fi; CAT=$(git grep -n "\"FluentFactory\"" -- src/Converj.Generator/ || true); if [ -n "$CAT" ]; then echo "CATEGORY DRIFT FOUND:"; echo "$CAT"; exit 1; fi; ROWS=$(grep -cE "^CVJG[0-9]{4}" src/Converj.Generator/AnalyzerReleases.Unshipped.md); if [ "$ROWS" != "47" ]; then echo "Expected 47 rows got $ROWS"; exit 1; fi; dotnet build /warnaserror && dotnet test --no-build'</automated>
  </verify>
  <done>
- `git grep -nE "MFFG|\"FluentFactory\"|FluentConstructor" -- src/Converj.Generator/Diagnostics/` returns zero hits
- `git grep -n '"FluentFactory"' -- src/Converj.Generator/` returns zero hits
- `AnalyzerReleases.Unshipped.md` contains exactly 47 rows
- `dotnet build /warnaserror` passes with zero warnings
- `dotnet test` passes every existing test
- Phase 16 completion gate closed
  </done>
</task>

</tasks>

<verification>
Phase-level cross-checks (these ARE the Phase 16 success criteria from ROADMAP.md):

1. **Success criterion 1 (DIAG-01):** All 47 descriptors in `FluentDiagnostics.cs` use `Category = "Converj"` — grep for `"FluentFactory"` in that file returns zero hits.
2. **Success criterion 2 (DIAG-02):** Titles and messages referring to the fluent root no longer say "Factory". Verified by re-reading descriptors.
3. **Success criterion 3 (DIAG-03):** `AnalyzerReleases.Unshipped.md` reflects the new category string, 47 rows, consistent with descriptors.
4. **Success criterion 4 (DIAG-04):** `git grep -nE "MFFG|\"FluentFactory\"|FluentConstructor" -- src/Converj.Generator/Diagnostics/` returns zero hits.
5. **Success criterion 5 (BEHAV-01/02):** `dotnet build` succeeds with zero warnings, `dotnet test` passes.
</verification>

<success_criteria>
- `AnalyzerReleases.Unshipped.md` complete at 47 rows with `Converj` category, numeric order, titles verbatim
- Defect "file was 18/47 before Phase 16" documented in 16-03-SUMMARY.md
- Phase-level DIAG-04 grep returns zero hits
- Phase-level repo-wide Category drift grep returns zero hits
- `dotnet build /warnaserror` and `dotnet test` both green
- Phase 16 success criteria 1-5 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-diagnostic-alignment/16-03-SUMMARY.md` capturing:
- Final row count (47) and the rule ID sequence (noting gaps)
- Defect note: "AnalyzerReleases.Unshipped.md was 18/47 before Phase 16 — completeness gap discovered and fixed during alignment"
- DIAG-04 grep output (should be empty)
- Final `dotnet build` and `dotnet test` confirmation
- Phase 16 success criteria checklist (1-5 from ROADMAP.md) all checked
</output>
