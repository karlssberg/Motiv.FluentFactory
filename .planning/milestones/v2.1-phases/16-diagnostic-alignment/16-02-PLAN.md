---
phase: 16-diagnostic-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs
  - src/Converj.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs
  - src/Converj.Generator/Diagnostics/DiagnosticList.cs
autonomous: true
requirements:
  - DIAG-04
must_haves:
  truths:
    - "UnreachableConstructorAnalyzer.cs contains no identifier matching the literal substring \"FluentConstructor\""
    - "IgnoredMultiMethodWarningFactory.cs contents (string literals, XML docs) contain no drift vocabulary referring to the fluent root (the class name itself is Phase 18 scope and is NOT renamed here)"
    - "DiagnosticList.cs contains no drift vocabulary (inspected — pre-read confirmed clean)"
    - "git grep -nE 'MFFG|\\\"FluentFactory\\\"|FluentConstructor' -- src/Converj.Generator/Diagnostics/ returns zero hits in the files this plan owns"
    - "dotnet build succeeds with zero warnings"
    - "dotnet test passes every existing test"
  artifacts:
    - path: "src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs"
      provides: "Analyzer tracking unreachable C# constructors that are fluent targets, with identifiers free of the \"FluentConstructor\" substring"
      min_lines: 70
    - path: "src/Converj.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs"
      provides: "GoF helper building ignored-multi-method diagnostics, with drift-free string literals and XML docs (type name itself deferred to Phase 18)"
      min_lines: 100
  key_links:
    - from: "UnreachableConstructorAnalyzer renamed fields"
      to: "AddReachableConstructor / AddAllFluentConstructors / IsReachable / GetUnreachableConstructorsDiagnostics"
      via: "Roslyn IDE rename propagation within the class"
      pattern: "_reachedTargetConstructors|_allTargetConstructors"
---

<objective>
Sibling-file cleanup of drift vocabulary in `src/Converj.Generator/Diagnostics/` that does NOT live in `FluentDiagnostics.cs`. Rename the `_allFluentConstructors` / `_reachedFluentConstructors` fields and related parameters in `UnreachableConstructorAnalyzer.cs` to remove the literal substring `FluentConstructor` (which is flagged by the DIAG-04 grep check). Inspect `IgnoredMultiMethodWarningFactory.cs` and `DiagnosticList.cs` for any drift vocabulary in string literals or XML docs and fix in place. Explicitly do NOT rename the `IgnoredMultiMethodWarningFactory` type or file — that is Phase 18 (NAME-07) territory.

Purpose: Closes the DIAG-04 grep check for sibling files so Phase 16's final repo-wide grep in Plan 16-03 passes. Without this plan, the phase-end DIAG-04 verification still finds hits even after `FluentDiagnostics.cs` is clean.

Output: Sibling files in `Diagnostics/` no longer contain any `FluentConstructor` identifier substring or fluent-root-sense drift vocabulary. Class and file names of the Phase 18 GoF helper are untouched.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-diagnostic-alignment/16-CONTEXT.md
@src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs
@src/Converj.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs
@src/Converj.Generator/Diagnostics/DiagnosticList.cs

<interfaces>
<!-- Key shapes of sibling files. Executor must use IDE rename for identifier changes. -->

**UnreachableConstructorAnalyzer.cs (75 lines)** — drift identifiers confirmed from pre-read:

```csharp
namespace Converj.Generator.Diagnostics;

internal class UnreachableConstructorAnalyzer
{
    private readonly List<IMethodSymbol> _allFluentConstructors = [];                        // ← DRIFT
    private readonly HashSet<IMethodSymbol> _reachedFluentConstructors =                     // ← DRIFT
        new(SymbolEqualityComparer.Default);

    public void AddReachableConstructor(IMethodSymbol constructor) { ... }                   // OK (C# ctor concept)
    public void RemoveReachableConstructor(IMethodSymbol constructor) { ... }                // OK
    public void AddReachableMethod(IFluentMethod method) { ... }                             // OK
    public void AddAllFluentConstructors(IEnumerable<IMethodSymbol> fluentConstructors) { }  // ← DRIFT (method name + param)
    public bool IsReachable(IMethodSymbol target) { ... }                                    // OK
    public void Clear() { ... }
    public IEnumerable<Diagnostic> GetUnreachableConstructorsDiagnostics() { ... }           // OK ("Unreachable constructors" = C# ctors)
    private IEnumerable<IMethodSymbol> GetUnreachableConstructors() { ... }                  // OK
}
```

**Call sites of the drift identifiers** (must be located by the executor and updated via IDE rename, which does it automatically):
- `_allFluentConstructors` — private field, used inside `UnreachableConstructorAnalyzer` only
- `_reachedFluentConstructors` — private field, used inside `UnreachableConstructorAnalyzer` only
- `AddAllFluentConstructors(...)` — public method; executor must grep for external call sites before rename
- Parameter name `fluentConstructors` inside `AddAllFluentConstructors` signature

**IgnoredMultiMethodWarningFactory.cs (107 lines)** — pre-read summary:
- Only match for "Factory" is the class declaration `internal class IgnoredMultiMethodWarningFactory(...)` on line 7. This is the GoF helper class, Phase 18 territory — DO NOT RENAME.
- Executor must re-scan the file for drift in string literals (yielded diagnostic messages), XML doc comments, and local-method names that may reference the fluent root.
- From pre-read: the yielded diagnostics reference `FluentDiagnostics.ContainsSupersededFluentMethodTemplate` and `FluentDiagnostics.FluentMethodTemplateSuperseded` — these are descriptor variable names (clean per vocabulary policy, "fluent method template" is a generator concept not a "Factory" drift).
- The `GetSelectedMethodDescription` local method composes a message mentioning "the fluent method" and "the parameter" and "the constructor" — these are all language concepts and OK.

**DiagnosticList.cs (40 lines)** — pre-read confirmed clean:
- No "Factory", "FluentConstructor", or "MFFG" matches. Generic `IReadOnlyList<Diagnostic>` wrapper. Included in this plan's `files_modified` only as a required inspection target; executor confirms and moves on.

**Vocabulary rule for this plan's file scope:**
The DIAG-04 success criterion is a literal grep: `git grep -n "MFFG\|\"FluentFactory\"\|FluentConstructor" -- src/Converj.Generator/Diagnostics/` must return zero hits. Note the alternation: any identifier whose substring contains `FluentConstructor` (e.g., `_allFluentConstructors`) triggers the check. That's why field rename is mandatory even though the identifiers refer to legitimate C# constructor concepts.

**Proposed rename pattern** (executor may adjust if a better name fits the call-site context):
- `_allFluentConstructors` → `_allTargetConstructors`
- `_reachedFluentConstructors` → `_reachedTargetConstructors`
- `AddAllFluentConstructors(IEnumerable<IMethodSymbol> fluentConstructors)` → `AddAllTargetConstructors(IEnumerable<IMethodSymbol> targetConstructors)`

Rationale for "Target": the constructors tracked are C# constructors decorated with `[FluentTarget]`. "Target" is current public vocabulary. Avoids "Fluent" prefix drift while staying accurate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename drift identifiers in UnreachableConstructorAnalyzer.cs via IDE rename</name>
  <files>src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs</files>
  <action>
Before renaming, grep the entire solution for external call sites of the public method being renamed:

```bash
git grep -nE "AddAllFluentConstructors" -- 'src/**/*.cs'
```

If any call sites exist outside `UnreachableConstructorAnalyzer.cs`, note them for verification after the IDE rename. IDE rename should update them automatically; the grep is a safety net.

Then apply three Roslyn / IDE renames (BEHAV-03: compiler-assisted, not blind text replace):

1. Field `_allFluentConstructors` → `_allTargetConstructors` (right-click → Rename, confirm all references update)
2. Field `_reachedFluentConstructors` → `_reachedTargetConstructors`
3. Method `AddAllFluentConstructors` → `AddAllTargetConstructors` (including the parameter `fluentConstructors` → `targetConstructors`)

Do NOT rename:
- `AddReachableConstructor` / `RemoveReachableConstructor` / `AddReachableMethod` — legitimate C# constructor vocabulary
- `IsReachable` / `Clear` / `GetUnreachableConstructorsDiagnostics` / `GetUnreachableConstructors` — same
- The class name `UnreachableConstructorAnalyzer` — legitimate C# constructor analyzer; not in Phase 16 scope and "constructor" is a language concept
- The namespace `Converj.Generator.Diagnostics`

If the executor's audit of `UnreachableConstructorAnalyzer.cs` reveals additional identifier or XML-doc drift beyond `FluentConstructors`, rename/edit in the same pass and record in SUMMARY.md.

Verify after rename:
```bash
git grep -n "FluentConstructor" -- src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs
```
Must return zero hits.
  </action>
  <verify>
    <automated>dotnet build src/Converj.Generator/Converj.Generator.csproj --nologo /warnaserror</automated>
  </verify>
  <done>
- `_allFluentConstructors`, `_reachedFluentConstructors`, `AddAllFluentConstructors`, and the `fluentConstructors` parameter no longer exist in `UnreachableConstructorAnalyzer.cs`
- All call sites (inside and outside the class) updated by IDE rename
- `git grep -n "FluentConstructor" -- src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs` returns zero hits
- `dotnet build src/Converj.Generator/Converj.Generator.csproj /warnaserror` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Inspect and fix drift in IgnoredMultiMethodWarningFactory.cs and DiagnosticList.cs</name>
  <files>src/Converj.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs, src/Converj.Generator/Diagnostics/DiagnosticList.cs</files>
  <action>
**IgnoredMultiMethodWarningFactory.cs:**

Re-read the full file (107 lines). The class name itself is Phase 18 territory (NAME-07) — DO NOT rename the type or the file. Phase 16 only fixes content drift.

Audit targets inside the file:
1. XML `/// <summary>` comments — none in the current file per pre-read; re-verify.
2. String literals yielded into diagnostics via `Diagnostic.Create(...)` — the current code passes `FluentDiagnostics.ContainsSupersededFluentMethodTemplate` (a descriptor variable, clean) and `FluentDiagnostics.FluentMethodTemplateSuperseded` (clean) plus dynamic arguments. No drift strings in the current file per pre-read.
3. Local-method names and local-variable names — pre-read confirms `HasActiveSiblingTemplates`, `IsSourceConstructorReachable`, `DoIgnoredMethodsCollectivelyCauseUnreachableConstructors`, `AreSelectedAndIgnoredMethodsFromSameContainingType`, `GetSelectedMethodDescription`. All clean (no "Factory" or "FluentConstructor" substrings).
4. The local composed string in `GetSelectedMethodDescription`: `"the parameter '...' in the constructor '...' was used as the basis for the fluent method. Perhaps the ignored method-template can be removed or modified."` — "the constructor" refers to a C# ctor, OK. "the fluent method" is generator vocabulary, OK. No edit required.

Expected outcome: **no changes** to `IgnoredMultiMethodWarningFactory.cs` from Phase 16. If the executor's close read finds any drift missed in pre-read, fix it in place and record in SUMMARY.md.

**DiagnosticList.cs:**

Re-read (40 lines). Pre-read confirms zero matches for "Factory", "FluentConstructor", "MFFG". Expected outcome: **no changes**. Executor confirms and moves on.

Both files are listed in `files_modified` of this plan's frontmatter to make the inspection scope explicit, but neither is expected to change. If either does change, update SUMMARY.md with the discovered drift for traceability.
  </action>
  <verify>
    <automated>node -e "const { execSync } = require('child_process'); const out = execSync('git grep -nE \"MFFG|\\\"FluentFactory\\\"|FluentConstructor\" -- src/Converj.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs src/Converj.Generator/Diagnostics/DiagnosticList.cs', { encoding: 'utf8', stdio: ['ignore','pipe','pipe'] }).trim(); if (out) { console.error('DRIFT FOUND:\\n' + out); process.exit(1); } console.log('clean');"</automated>
  </verify>
  <done>
- Both files inspected and confirmed free of drift vocabulary
- Any discovered drift fixed in place
- `IgnoredMultiMethodWarningFactory` type name and file name UNCHANGED (Phase 18 scope)
- `git grep` across both files for the DIAG-04 pattern returns zero hits
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite build and test gate</name>
  <files>(no file modifications — verification gate only)</files>
  <action>
Run full build and test to confirm the IDE renames in Task 1 propagated cleanly and nothing outside the plan's file scope broke.

```bash
dotnet build /warnaserror
dotnet test --no-build
```

Must be zero warnings, all tests passing. If a test fails, the IDE rename missed a call site the executor did not check — inspect the failure, fix, re-run.
  </action>
  <verify>
    <automated>dotnet build /warnaserror && dotnet test --no-build</automated>
  </verify>
  <done>
- `dotnet build /warnaserror` completes with zero warnings
- `dotnet test` reports all tests passing
- BEHAV-01 and BEHAV-02 satisfied for this plan's scope
  </done>
</task>

</tasks>

<verification>
1. **DIAG-04 partial (sibling files):** `git grep -nE "MFFG|\"FluentFactory\"|FluentConstructor" -- src/Converj.Generator/Diagnostics/UnreachableConstructorAnalyzer.cs src/Converj.Generator/Diagnostics/IgnoredMultiMethodWarningFactory.cs src/Converj.Generator/Diagnostics/DiagnosticList.cs` returns zero hits.
2. **Phase 18 boundary preserved:** `IgnoredMultiMethodWarningFactory` class and file name are unchanged — verified by `git diff --name-only HEAD~1` showing the file was edited but not renamed (or unchanged if the file required no edits).
3. **Behavior preservation:** `dotnet build /warnaserror && dotnet test` both green.
</verification>

<success_criteria>
- Drift identifiers in `UnreachableConstructorAnalyzer.cs` renamed via IDE rename (BEHAV-03)
- No `FluentConstructor` substring remains in any identifier in the three files this plan owns
- `IgnoredMultiMethodWarningFactory` type/file name untouched (Phase 18 deferred)
- `DiagnosticList.cs` confirmed clean
- `dotnet build /warnaserror` and `dotnet test` both green
- DIAG-04 partial gate (sibling files) closed; final DIAG-04 repo-wide verification happens in Plan 16-03
</success_criteria>

<output>
After completion, create `.planning/phases/16-diagnostic-alignment/16-02-SUMMARY.md` capturing:
- Final list of identifiers renamed in `UnreachableConstructorAnalyzer.cs`
- Whether `IgnoredMultiMethodWarningFactory.cs` or `DiagnosticList.cs` required any edits (and what)
- External call sites of `AddAllFluentConstructors` found during pre-rename grep (if any) and how they updated
- Confirmation that `IgnoredMultiMethodWarningFactory` type/file name are preserved for Phase 18
- Final `dotnet build` and `dotnet test` output confirmation
</output>
