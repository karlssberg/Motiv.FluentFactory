---
phase: 20-documentation-cleanup-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - src/Converj.Generator/FluentTargetValidator.cs
  - src/Converj.Generator/SyntaxGeneration/ExistingPartialTypeStepDeclaration.cs
  - src/Converj.Generator/Extensions/FluentAttributeExtensions.cs
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - FILE-02

must_haves:
  truths:
    - "CLAUDE.md references FluentRootGenerator and FluentRootCompilationUnit, not legacy FluentFactory* names"
    - "No source comment in src/Converj.Generator/ mentions FluentFactory as the attribute name"
    - "Repo-wide grep for legacy vocabulary returns zero hits in active source/tests/docs"
    - "No .cs file path under src/Converj.Generator/ contains FluentFactory, FluentConstructor, or BuilderMethod"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated documentation with current type names"
      contains: "FluentRootGenerator"
    - path: "src/Converj.Generator/FluentTargetValidator.cs"
      provides: "Comment fix: FluentFactory -> FluentRoot"
    - path: "src/Converj.Generator/SyntaxGeneration/ExistingPartialTypeStepDeclaration.cs"
      provides: "Comment fix: [FluentFactory] -> [FluentRoot]"
  key_links:
    - from: "CLAUDE.md"
      to: "src/Converj.Generator/FluentRootGenerator.cs"
      via: "Documentation references actual file name"
      pattern: "FluentRootGenerator"
---

<objective>
Update CLAUDE.md documentation and fix remaining source comment residuals so that no active source, test, or documentation file contains stale FluentFactory/FluentConstructor vocabulary.

Purpose: Complete the documentation alignment leg of the v2.1 naming refactor (DOC-01..03, FILE-02).
Output: Updated CLAUDE.md, three source files with comment fixes, clean grep gate.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and fix source comment residuals</name>
  <files>CLAUDE.md, src/Converj.Generator/FluentTargetValidator.cs, src/Converj.Generator/SyntaxGeneration/ExistingPartialTypeStepDeclaration.cs, src/Converj.Generator/Extensions/FluentAttributeExtensions.cs</files>
  <action>
Update CLAUDE.md with the following changes (all are stale FluentFactory* references):

1. Line 32: Change `FluentFactoryGenerator.cs` to `FluentRootGenerator.cs`
2. Line 40: Change `FluentFactoryCompilationUnit` to `FluentRootCompilationUnit`
3. Line 80: Change `CSharpSourceGeneratorVerifier<FluentFactoryGenerator>` to `CSharpSourceGeneratorVerifier<FluentRootGenerator>`
4. Line 87: Change `using VerifyCS = Converj.Generator.Tests.CSharpSourceGeneratorVerifier<FluentFactoryGenerator>;` to use `FluentRootGenerator`
5. Line 97: Change `typeof(FluentFactoryGenerator)` to `typeof(FluentRootGenerator)`

Fix three source code comment residuals:

6. `src/Converj.Generator/FluentTargetValidator.cs` line 111: Change comment from `// Check if the target type has the FluentFactory attribute` to `// Check if the target type has the FluentRoot attribute`
7. `src/Converj.Generator/SyntaxGeneration/ExistingPartialTypeStepDeclaration.cs` lines 80-81: Change XML doc from `[FluentFactory] attribute` to `[FluentRoot] attribute`, and on line 83 rename method `HasOwnFactoryDeclaration` to `HasOwnRootDeclaration` (update its single call site in the same file)
8. `src/Converj.Generator/Extensions/FluentAttributeExtensions.cs` line 91: Change diagnostic property key from `"FluentConstructorParameter"` to `"FluentTargetParameter"` — this is an internal diagnostic ImmutableDictionary key, not a public API string. Verify no other code reads this key by grepping for the string; if any consumer reads it, update that consumer too.

DOC-02 note: No sub-project CLAUDE.md files exist at `src/Converj.Generator/CLAUDE.md` or `src/Converj.Generator.Tests/CLAUDE.md`, so DOC-02 is satisfied by absence.

FILE-02 note: `git ls-files 'src/Converj.Generator/**/*.cs' | grep -i 'FluentFactory\|FluentConstructor\|BuilderMethod'` already returns zero hits, so FILE-02 is satisfied by prior phases.
  </action>
  <verify>
    <automated>cd C:/Dev/Converj && dotnet build --no-incremental 2>&1 | tail -5 && dotnet test --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All five CLAUDE.md lines updated to current type names. Three source comment residuals fixed. Build succeeds with zero warnings, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run DOC-03 repo-wide grep gate and FILE-02 file path gate</name>
  <files></files>
  <action>
Run the two verification gates specified in REQUIREMENTS.md and ROADMAP.md success criteria:

DOC-03 gate — repo-wide vocabulary grep:
```bash
git grep -n "FluentFactory\|FluentConstructor\|BuilderMethod\|InitialVerb\|MFFG\|Motiv\.FluentFactory" -- "*.cs" "*.md" "*.csproj" "*.props" "*.targets" ':!.planning/MILESTONES.md' ':!.planning/v1.*'
```
Expected: zero hits in active source/tests/docs. Hits in `.planning/` phase artifacts (CONTEXT.md, SUMMARY.md, STATE.md decisions, ROADMAP.md historical text, REQUIREMENTS.md) are acceptable — these are planning history, not active code/docs.

If any unexpected hits appear, fix them (same approach as Task 1 — comment/doc edits only, no behavior changes).

FILE-02 gate — no legacy file paths:
```bash
git ls-files 'src/Converj.Generator/**/*.cs' | grep -i "FluentFactory\|FluentConstructor\|BuilderMethod"
```
Expected: zero hits.

Record the exact command output as evidence for the SUMMARY.
  </action>
  <verify>
    <automated>cd C:/Dev/Converj && git grep -c "FluentFactory\|FluentConstructor\|BuilderMethod\|InitialVerb\|MFFG" -- "*.cs" "*.md" ':!.planning/*' ':!CLAUDE.md.bak' | head -20; echo "EXIT: $?"</automated>
  </verify>
  <done>DOC-03 grep gate passes with zero hits in active source/tests/docs. FILE-02 file path gate passes with zero hits. Evidence recorded.</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds with zero warnings
2. `dotnet test` passes all tests (415 expected)
3. `git grep -n "FluentFactory|FluentConstructor|BuilderMethod|InitialVerb|MFFG|Motiv\.FluentFactory" -- "*.cs" "*.md" ':!.planning/*'` returns zero hits
4. `git ls-files 'src/Converj.Generator/**/*.cs' | grep -i "FluentFactory|FluentConstructor|BuilderMethod"` returns zero hits
</verification>

<success_criteria>
CLAUDE.md contains no stale FluentFactory/FluentConstructor vocabulary. All source comments aligned. Repo-wide grep gate clean. File path gate clean. Build green, all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation-cleanup-final-verification/20-01-SUMMARY.md`
</output>
