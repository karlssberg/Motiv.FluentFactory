---
phase: 11-type-system-edge-cases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNullableTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericArrayTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorPartiallyOpenGenericTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorDeepNestedGenericTests.cs
autonomous: true
requirements: [TYPE-01, TYPE-03, TYPE-04, TYPE-05]

must_haves:
  truths:
    - "A test exercises nullable reference type annotations on constructor parameters and asserts correct generated output"
    - "A test exercises arrays of generic types (T[], List<T>[]) and asserts correct generated output"
    - "A test exercises partially open generic types (Dictionary<string, T>) and asserts correct generated output"
    - "A test exercises deeply nested non-delegate generics (3+ levels) and asserts correct generated output"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNullableTests.cs"
      provides: "Nullable annotation edge case tests"
      min_lines: 50
    - path: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericArrayTests.cs"
      provides: "Generic array type edge case tests"
      min_lines: 50
    - path: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorPartiallyOpenGenericTests.cs"
      provides: "Partially open generic edge case tests"
      min_lines: 50
    - path: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorDeepNestedGenericTests.cs"
      provides: "Deep nested generic edge case tests (non-delegate)"
      min_lines: 50
  key_links:
    - from: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNullableTests.cs"
      to: "FluentFactoryGenerator"
      via: "CSharpSourceGeneratorVerifier test pattern"
      pattern: "VerifyCS\\.Test"
    - from: "src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericArrayTests.cs"
      to: "FluentFactoryGenerator"
      via: "CSharpSourceGeneratorVerifier test pattern"
      pattern: "VerifyCS\\.Test"
---

<objective>
Write edge case tests for nullable annotations, generic arrays, partially open generics, and deep nested generics.

Purpose: Document the generator's behavior under unusual type system inputs. Tests assert DESIRED correct output -- failing tests are expected and indicate discovered shortcomings.
Output: Four new test files with 2-4 focused scenarios each.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-type-system-edge-cases/11-CONTEXT.md

@src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNestedGenericTests.cs
@src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs
@src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNonGenericTests.cs
@src/Motiv.FluentFactory.Generator.Tests/CSharpSourceGeneratorVerifier.cs

<interfaces>
<!-- Test verifier pattern used by all tests -->
From CSharpSourceGeneratorVerifier.cs:
```csharp
using VerifyCS = Motiv.FluentFactory.Generator.Tests.CSharpSourceGeneratorVerifier<FluentFactoryGenerator>;

// Usage:
var test = new VerifyCS.Test
{
    TestState =
    {
        Sources = { code },
        GeneratedSources =
        {
            (typeof(FluentFactoryGenerator), "Namespace.FactoryName.g.cs", expected)
        }
    }
};
await test.RunAsync();
```

<!-- Generator output pattern (from existing tests) -->
Generated code always:
- Wraps in namespace block
- Adds [GeneratedCode] attribute
- Uses `global::` fully qualified types
- Uses `in` modifier on all parameters
- Emits `// <auto-generated/>` and `#nullable enable` at end
- File named `{Namespace}.{FactoryTypeName}.g.cs`

<!-- From CONTEXT.md: SymbolDisplayExtensions.GlobalQualifiedFormat already includes IncludeNullableReferenceTypeModifier -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create nullable and generic array test files (TYPE-01, TYPE-03)</name>
  <files>
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNullableTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericArrayTests.cs
  </files>
  <behavior>
    TYPE-01 Nullable tests (FluentFactoryGeneratorNullableTests.cs):
    - Test 1: Single nullable reference parameter (string?) -- asserts generated output preserves nullable annotation in type references
    - Test 2: Nullable and non-nullable mixed parameters (string?, int, List&lt;string?&gt;) -- asserts both nullable and non-nullable types coexist correctly in generated steps
    - Test 3: Nullable generic parameter (T?) with class constraint -- asserts generated code handles nullable type parameter

    TYPE-03 Generic array tests (FluentFactoryGeneratorGenericArrayTests.cs):
    - Test 1: Simple generic array parameter (T[]) -- asserts generated output correctly uses array syntax
    - Test 2: Array of generic type (List&lt;T&gt;[]) -- asserts generated output handles array of constructed generic
    - Test 3: Multi-dimensional or jagged array of generic (T[][]) -- asserts generated output handles jagged arrays
  </behavior>
  <action>
    Create two new test files following the exact pattern from FluentFactoryGeneratorNestedGenericTests.cs.

    For each test file:
    1. Add `using VerifyCS = ...` alias at top
    2. Name class matching file name convention
    3. Each test method gets `[Fact]` and `internal async Task` signature
    4. Each test method gets an XML doc comment (`/// <summary>`) explaining the edge case
    5. Input code uses `[FluentFactory]` on a static partial class and `[FluentConstructor]` with `Options = FluentOptions.NoCreateMethod` on the target class constructor
    6. Expected output follows the established generated code pattern with `global::` qualification, `[GeneratedCode]` attribute, `[MethodImpl(AggressiveInlining)]`, and `in` parameter modifier

    For TYPE-01 nullable tests:
    - The generator already includes `IncludeNullableReferenceTypeModifier` in its display format, so expected output should include `?` on nullable reference types (e.g., `string?` becomes `string?` in generated code)
    - Use `#nullable enable` in the input code
    - For nullable generic T?, add `where T : class` constraint on the build target class

    For TYPE-03 generic array tests:
    - GenericAnalysis.cs already handles IArrayTypeSymbol recursively, so expected output should correctly represent array types
    - T[] should appear as `T[]` in generated code (with global:: qualification on containing types)
    - List&lt;T&gt;[] should appear as `global::System.Collections.Generic.List<T>[]`

    Tests assert DESIRED output. If the generator currently produces different output, the test will fail -- that is expected and acceptable.
  </action>
  <verify>
    <automated>dotnet build src/Motiv.FluentFactory.Generator.Tests/Motiv.FluentFactory.Generator.Tests.csproj</automated>
  </verify>
  <done>
    FluentFactoryGeneratorNullableTests.cs has 3 test methods covering nullable reference types, mixed nullable/non-nullable, and nullable generic parameters. FluentFactoryGeneratorGenericArrayTests.cs has 3 test methods covering T[], List&lt;T&gt;[], and T[][]. All tests compile. Tests may pass or fail -- both outcomes are valid for this edge case discovery milestone.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create partially open generic and deep nested generic test files (TYPE-04, TYPE-05)</name>
  <files>
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorPartiallyOpenGenericTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorDeepNestedGenericTests.cs
  </files>
  <behavior>
    TYPE-04 Partially open generic tests (FluentFactoryGeneratorPartiallyOpenGenericTests.cs):
    - Test 1: Dictionary&lt;string, T&gt; parameter -- asserts generated code correctly handles mix of concrete and open type arguments
    - Test 2: Func&lt;int, T&gt; parameter -- asserts partially closed delegate type generates correctly
    - Test 3: Multiple partially open parameters (Dictionary&lt;string, T&gt;, List&lt;int&gt;, Func&lt;T, bool&gt;) -- asserts multi-step chain with partially open types

    TYPE-05 Deep nested generic tests (FluentFactoryGeneratorDeepNestedGenericTests.cs):
    - Test 1: Dictionary&lt;string, List&lt;KeyValuePair&lt;T, U&gt;&gt;&gt; parameter -- non-delegate 3-level nesting
    - Test 2: IReadOnlyDictionary&lt;string, IEnumerable&lt;Tuple&lt;T, List&lt;U&gt;&gt;&gt;&gt; parameter -- 4-level non-delegate nesting

    Note: Existing FluentFactoryGeneratorNestedGenericTests.cs already covers delegate-based deep nesting (Func&lt;IEnumerable&lt;KeyValuePair&lt;T,T&gt;&gt;, bool&gt;). These new tests COMPLEMENT by testing non-delegate deep nesting per CONTEXT.md decision.
  </behavior>
  <action>
    Create two new test files following the exact pattern from FluentFactoryGeneratorNestedGenericTests.cs.

    For each test file:
    1. Add `using VerifyCS = ...` alias at top
    2. Name class matching file name convention
    3. Each test method gets `[Fact]` and `internal async Task` signature
    4. Each test method gets an XML doc comment (`/// <summary>`) explaining the edge case
    5. Input code uses `[FluentFactory]` on a static partial class and `[FluentConstructor]` with `Options = FluentOptions.NoCreateMethod` on the target class constructor
    6. Expected output follows the established generated code pattern

    For TYPE-04 partially open generics:
    - The key edge case is that `Dictionary<string, T>` has one closed argument (`string`) and one open (`T`). The generator must correctly resolve that only `T` needs a type parameter on the step method.
    - Expected generated method signature: `WithValue<T>(in global::System.Collections.Generic.Dictionary<string, T> value)`
    - Concrete type arguments should be fully qualified: `global::System.String` if the generator does that, or `string` -- match what the generator's GlobalQualifiedFormat produces for built-in types.

    For TYPE-05 deep nested generics:
    - These are non-delegate types to complement existing delegate-based nesting tests
    - Expected output uses full `global::` qualification at every level
    - E.g., `global::System.Collections.Generic.Dictionary<string, global::System.Collections.Generic.List<global::System.Collections.Generic.KeyValuePair<T, U>>>`

    Tests assert DESIRED output. Failing tests indicate discovered shortcomings.
  </action>
  <verify>
    <automated>dotnet build src/Motiv.FluentFactory.Generator.Tests/Motiv.FluentFactory.Generator.Tests.csproj</automated>
  </verify>
  <done>
    FluentFactoryGeneratorPartiallyOpenGenericTests.cs has 3 test methods covering partially closed Dictionary, Func, and multi-parameter scenarios. FluentFactoryGeneratorDeepNestedGenericTests.cs has 2 test methods covering non-delegate deep nesting (3+ levels). All tests compile. Tests may pass or fail -- both outcomes are valid.
  </done>
</task>

</tasks>

<verification>
All four test files compile without errors:
```bash
dotnet build src/Motiv.FluentFactory.Generator.Tests/Motiv.FluentFactory.Generator.Tests.csproj
```

Run the new tests to document current behavior (pass or fail both valid):
```bash
dotnet test --filter "FullyQualifiedName~FluentFactoryGeneratorNullableTests|FullyQualifiedName~FluentFactoryGeneratorGenericArrayTests|FullyQualifiedName~FluentFactoryGeneratorPartiallyOpenGenericTests|FullyQualifiedName~FluentFactoryGeneratorDeepNestedGenericTests" --no-build
```
</verification>

<success_criteria>
- 4 new test files exist in the test project
- Each file has 2-4 focused test scenarios with XML doc comments
- Tests follow the established CSharpSourceGeneratorVerifier pattern exactly
- Tests assert DESIRED correct output (not current behavior)
- All test files compile successfully
- Existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/11-type-system-edge-cases/11-01-SUMMARY.md`
</output>
