---
phase: 19-test-fixture-alignment
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified: []
autonomous: true
requirements: [TEST-05]

must_haves:
  truths:
    - "No test class or file name contains Factory or FluentConstructor unless retained as GoF pattern"
    - "git grep for Factory/FluentConstructor scoped to test directories returns only documented GoF exclusions"
    - "dotnet build succeeds with zero warnings and dotnet test passes all tests"
  artifacts: []
  key_links: []
---

<objective>
Run the Phase 19 final grep gate to verify no residual legacy vocabulary remains in test files, confirming TEST-05 is satisfied.

Purpose: Gate verification ensures plans 01 and 02 achieved complete coverage with no missed references.
Output: Verification pass confirming zero residual Factory/FluentConstructor vocabulary in tests.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-test-fixture-alignment/19-01-SUMMARY.md
@.planning/phases/19-test-fixture-alignment/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TEST-05 grep gate and fix any residual hits</name>
  <files>Any remaining files in src/Converj.Generator.Tests/ or src/Converj.Tests/ with residual vocabulary</files>
  <action>
1. Run the TEST-05 verification grep:
   ```
   git grep -n "Factory\|FluentConstructor" -- src/Converj.Generator.Tests/ src/Converj.Tests/
   ```

2. For each hit, classify:
   - **GoF pattern (KEEP):** Types like `ExplanationExpressionTreePropositionFactory`, `ExplanationWithNameExpressionTreePropositionFactory`, `MultiAssertionExplanationExpressionTreePropositionFactory` — these are real user types that legitimately use "Factory" as a GoF design pattern. Document these as intentional retentions.
   - **Fluent root reference (FIX):** Any remaining `Factory` that refers to the fluent root sample type. Fix by replacing with `Builder`.
   - **C# constructor reference (KEEP):** If "constructor" in a comment refers to the C# language concept (not the old FluentConstructor attribute), keep it.

3. Fix any residual hits that should have been caught by plans 01/02.

4. Run full build and test suite:
   ```
   dotnet build
   dotnet test
   ```

5. Verify file names contain no legacy vocabulary:
   ```
   git ls-files src/Converj.Generator.Tests/ src/Converj.Tests/ | grep -i "Factory\|FluentConstructor"
   ```
   This must return zero hits.

6. Document the final GoF exclusion list (types that legitimately retain "Factory") in the SUMMARY for Phase 20's final verification.
  </action>
  <verify>
    <automated>cd C:/Dev/Converj &amp;&amp; git grep -c "Factory\|FluentConstructor" -- src/Converj.Generator.Tests/ src/Converj.Tests/ 2>&amp;1 | grep -v "PropositionFactory" | head -20</automated>
  </verify>
  <done>Only GoF PropositionFactory* types retain "Factory" in test files; zero FluentConstructor hits; zero Factory/FluentConstructor in file names; build green; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Final build and full test suite verification</name>
  <files>None (verification only)</files>
  <action>
1. Run complete build from solution root:
   ```
   dotnet build
   ```
   Must succeed with zero warnings.

2. Run complete test suite:
   ```
   dotnet test
   ```
   Must pass all 415+ tests.

3. Verify git history preservation for renamed files:
   ```
   git log --follow --oneline -3 src/Converj.Generator.Tests/EmptyRootTests.cs
   git log --follow --oneline -3 src/Converj.Generator.Tests/NestedRootTests.cs
   git log --follow --oneline -3 src/Converj.Tests/NestedRootRuntimeTests.cs
   ```
   Each must show history from before the rename (proving git mv was used).

4. Report final test count and pass status.
  </action>
  <verify>
    <automated>cd C:/Dev/Converj &amp;&amp; dotnet test --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Build succeeds with zero warnings; all tests pass; git history preserved for renamed files</done>
</task>

</tasks>

<verification>
1. `git grep -n "Factory\|FluentConstructor" -- src/Converj.Generator.Tests/ src/Converj.Tests/` returns ONLY GoF PropositionFactory* hits
2. `git ls-files src/Converj.Generator.Tests/ src/Converj.Tests/ | grep -i "Factory\|FluentConstructor"` returns zero hits
3. `dotnet build` succeeds with zero warnings
4. `dotnet test` passes all tests
5. `git log --follow` confirms history preservation for all git mv'd files
</verification>

<success_criteria>
- TEST-05 grep gate passes: no Factory or FluentConstructor in test file names or content (except documented GoF exclusions)
- Full build green with zero warnings
- Full test suite passes
- Git history preserved for all renamed files
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-fixture-alignment/19-03-SUMMARY.md`
</output>
