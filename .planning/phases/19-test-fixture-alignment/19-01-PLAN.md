---
phase: 19-test-fixture-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Converj.Generator.Tests/EmptyFactoryTests.cs
  - src/Converj.Generator.Tests/NestedFactoryTests.cs
  - src/Converj.Generator.Tests/AsAttributeTests.cs
  - src/Converj.Generator.Tests/BugDiscoveryTests.cs
  - src/Converj.Generator.Tests/CompilationErrorResilienceTests.cs
  - src/Converj.Generator.Tests/ConstructorChainingTests.cs
  - src/Converj.Generator.Tests/CreateMethodNoneOptionalTests.cs
  - src/Converj.Generator.Tests/DeepNestedGenericTests.cs
  - src/Converj.Generator.Tests/DefaultsTests.cs
  - src/Converj.Generator.Tests/DiagnosticsTests.cs
  - src/Converj.Generator.Tests/FluentParameterDiagnosticsTests.cs
  - src/Converj.Generator.Tests/FluentParameterTests.cs
  - src/Converj.Generator.Tests/GatewayMethodStorageTests.cs
  - src/Converj.Generator.Tests/GeneratedCodeAttributeTests.cs
  - src/Converj.Generator.Tests/GenericArrayTests.cs
  - src/Converj.Generator.Tests/GenericAttributeTests.cs
  - src/Converj.Generator.Tests/GenericTests.cs
  - src/Converj.Generator.Tests/HashCodeContractTests.cs
  - src/Converj.Generator.Tests/InvalidGenericConstraintTests.cs
  - src/Converj.Generator.Tests/LargeParameterCountTests.cs
  - src/Converj.Generator.Tests/MalformedAttributeTests.cs
  - src/Converj.Generator.Tests/MergeDissimilarStepsTests.cs
  - src/Converj.Generator.Tests/MergeTests.cs
  - src/Converj.Generator.Tests/MethodCustomizationTests.cs
  - src/Converj.Generator.Tests/MethodPrefixTests.cs
  - src/Converj.Generator.Tests/MultipleMethodsGenerationTests.cs
  - src/Converj.Generator.Tests/NamespaceConflictTests.cs
  - src/Converj.Generator.Tests/NamespaceDisambiguationTests.cs
  - src/Converj.Generator.Tests/NamespaceTests.cs
  - src/Converj.Generator.Tests/NestedGenericTests.cs
  - src/Converj.Generator.Tests/NonGenericTests.cs
  - src/Converj.Generator.Tests/NullableTests.cs
  - src/Converj.Generator.Tests/OptionalParameterTests.cs
  - src/Converj.Generator.Tests/OverlappingMethodNameTests.cs
  - src/Converj.Generator.Tests/ParameterModifierTests.cs
  - src/Converj.Generator.Tests/PartiallyOpenGenericTests.cs
  - src/Converj.Generator.Tests/PrimaryConstructorTests.cs
  - src/Converj.Generator.Tests/RecordVariationTests.cs
  - src/Converj.Generator.Tests/RequiredPropertyDiagnosticsTests.cs
  - src/Converj.Generator.Tests/RequiredPropertyTests.cs
  - src/Converj.Generator.Tests/ScopeAndAccessibilityTests.cs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-04]

must_haves:
  truths:
    - "EmptyFactoryTests is renamed to EmptyRootTests (class + file via git mv)"
    - "NestedFactoryTests is renamed to NestedRootTests (class + file via git mv)"
    - "Every test-local 'class Factory' sample type in generator tests uses 'Builder' instead"
    - "Expected generated output in test strings reflects the Builder rename (file names, step struct names)"
    - "FluentConstructor references in comments and method names are updated to FluentTarget"
    - "dotnet build succeeds with zero warnings and dotnet test passes all tests"
  artifacts:
    - path: "src/Converj.Generator.Tests/EmptyRootTests.cs"
      provides: "Renamed EmptyFactoryTests"
    - path: "src/Converj.Generator.Tests/NestedRootTests.cs"
      provides: "Renamed NestedFactoryTests"
  key_links:
    - from: "src/Converj.Generator.Tests/*.cs"
      to: "generator output"
      via: "test source string class names match expected output struct/file names"
      pattern: "class Builder.*Factory\\.g\\.cs.*Builder\\.g\\.cs"
---

<objective>
Rename test classes and test-local sample types in `src/Converj.Generator.Tests/` to eliminate legacy `Factory` vocabulary for fluent root types and `FluentConstructor` references.

Purpose: Phase 19 aligns test fixtures with v2.0 public API vocabulary. This plan handles all generator test files (~53 files).
Output: All generator test files use `Builder` instead of `Factory` for fluent root sample types; `EmptyFactoryTests` and `NestedFactoryTests` classes renamed.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv EmptyFactoryTests and NestedFactoryTests, rename class names</name>
  <files>src/Converj.Generator.Tests/EmptyFactoryTests.cs, src/Converj.Generator.Tests/NestedFactoryTests.cs</files>
  <action>
1. `git mv src/Converj.Generator.Tests/EmptyFactoryTests.cs src/Converj.Generator.Tests/EmptyRootTests.cs`
2. In `EmptyRootTests.cs`: rename class `EmptyFactoryTests` to `EmptyRootTests`. Update the method name `Should_not_crash_when_factory_has_no_constructors` to `Should_not_crash_when_root_has_no_targets` (aligns with FluentRoot/FluentTarget vocabulary). Update XML doc if it says "factory" referring to the fluent root.
3. `git mv src/Converj.Generator.Tests/NestedFactoryTests.cs src/Converj.Generator.Tests/NestedRootTests.cs`
4. In `NestedRootTests.cs`: rename class `NestedFactoryTests` to `NestedRootTests`. Update method names: `Should_wrap_generated_factory_in_containing_type_when_factory_is_nested` to `Should_wrap_generated_root_in_containing_type_when_root_is_nested`, and `Should_wrap_generated_factory_in_deeply_nested_containing_types` to `Should_wrap_generated_root_in_deeply_nested_containing_types`. Update XML doc comments that say "factory" when referring to the fluent root — but do NOT rename the `class Factory` sample type inside source strings yet (Task 2 handles that).
5. Verify: `dotnet build src/Converj.Generator.Tests/` compiles.
  </action>
  <verify>
    <automated>dotnet build C:/Dev/Converj/src/Converj.Generator.Tests/ --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>EmptyRootTests.cs and NestedRootTests.cs exist with correct class names; old files gone from git; build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Bulk rename Factory to Builder in generator test source strings and expected outputs</name>
  <files>All *.cs files in src/Converj.Generator.Tests/</files>
  <action>
Perform a mechanical bulk replacement across ALL .cs files in `src/Converj.Generator.Tests/`. This is a multi-pattern replacement within raw string literals (test source code and expected generated output).

**Replacement patterns (in this order):**

Within test source strings (input code):
- `partial class Factory` -> `partial class Builder` (the [FluentRoot]-annotated sample type)
- `typeof(Factory)` -> `typeof(Builder)` (FluentTarget references)
- `typeof(Outer.Factory)` -> `typeof(Outer.Builder)` (nested references)
- `typeof(Level1.Level2.Factory)` -> `typeof(Level1.Level2.Builder)` (deeply nested)

Within expected generated output strings:
- `.Factory.g.cs` -> `.Builder.g.cs` (generated file names)
- `__Factory` -> `__Builder` (step struct name suffixes like `Step_0__Test_Factory` -> `Step_0__Test_Builder`)
- `partial class Factory` -> `partial class Builder` (in generated output too)

Within C# test code (not strings):
- `"Test.Factory.g.cs"` -> `"Test.Builder.g.cs"` (GeneratedSources tuple)
- `"MyNamespace.Factory.g.cs"` -> `"MyNamespace.Builder.g.cs"`
- `"Test.Outer.Factory.g.cs"` -> `"Test.Outer.Builder.g.cs"`
- `"Test.Level1.Level2.Factory.g.cs"` -> `"Test.Level1.Level2.Builder.g.cs"`
- Variable names like `factoryExpected` -> `builderExpected`

Within comments and method names referencing fluent roots:
- `factory` (lowercase, when referring to fluent root) -> `root` in method names and comments
- `FluentConstructor` -> `FluentTarget` in comments and XML doc
- `FluentConstructors` -> `FluentTargets` in method names

**CRITICAL EXCLUSIONS — DO NOT RENAME:**
- `ExplanationExpressionTreePropositionFactory` and similar GoF-pattern types in DiagnosticsTests.cs — these are real user types that legitimately use "Factory" as a GoF pattern
- `ExplanationWithNameExpressionTreePropositionFactory` — same GoF pattern
- `MultiAssertionExplanationExpressionTreePropositionFactory` — same GoF pattern
- Any comment that says "factory" referring to the C# GoF Factory pattern (not the fluent root)
- The string `"FluentFactory"` if it appears inside a diagnostic message being tested (check context — if the test is verifying that a diagnostic mentions "FluentRoot", that was already renamed in Phase 16)
- `fluentFactoryRootType` variable in BugDiscoveryTests.cs — rename to `fluentRootType` (it's a variable name, not a GoF type)

**Strategy:** Use `sed` for the mechanical bulk replacements. Do a first pass with broad patterns, then manually verify/fix edge cases. The key insight is that within raw string literal test fixtures, `Factory` always means the [FluentRoot] sample type (except for the GoF exclusions noted above).

After all replacements, run `dotnet build` to catch any missed references, then `dotnet test` to verify all 415+ tests pass.
  </action>
  <verify>
    <automated>cd C:/Dev/Converj &amp;&amp; dotnet test --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Zero occurrences of "class Factory" as a [FluentRoot] sample type in generator tests; all step struct names use __Builder suffix; all .g.cs file names use Builder; all FluentConstructor comments updated to FluentTarget; build succeeds with zero warnings; all tests pass</done>
</task>

</tasks>

<verification>
1. `git grep -n "partial class Factory" -- src/Converj.Generator.Tests/` returns zero hits
2. `git grep -n "typeof(Factory)" -- src/Converj.Generator.Tests/` returns zero hits
3. `git grep -n "\.Factory\.g\.cs" -- src/Converj.Generator.Tests/` returns zero hits
4. `git grep -n "__Factory" -- src/Converj.Generator.Tests/` returns zero hits
5. `git grep -n "FluentConstructor" -- src/Converj.Generator.Tests/` returns zero hits
6. `git grep -n "EmptyFactoryTests\|NestedFactoryTests" -- src/Converj.Generator.Tests/` returns zero hits
7. GoF types preserved: `git grep -n "PropositionFactory" -- src/Converj.Generator.Tests/` still returns hits
8. `dotnet build` succeeds with zero warnings
9. `dotnet test` passes all tests
</verification>

<success_criteria>
- EmptyFactoryTests renamed to EmptyRootTests (class + file via git mv)
- NestedFactoryTests renamed to NestedRootTests (class + file via git mv)
- All test-local `class Factory` sample types renamed to `class Builder`
- All generated output references updated (step struct names, .g.cs file names)
- All FluentConstructor references in comments/method names updated to FluentTarget
- GoF Factory types (PropositionFactory*) preserved unchanged
- Build green, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-fixture-alignment/19-01-SUMMARY.md`
</output>
