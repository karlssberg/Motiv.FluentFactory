---
phase: 20-documentation-cleanup-final-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified: []
autonomous: true
requirements:
  - BEHAV-01
  - BEHAV-02
  - BEHAV-03
  - BEHAV-04

must_haves:
  truths:
    - "dotnet build succeeds with zero warnings"
    - "dotnet test passes every existing test (415 tests, 0 failures, 0 skipped)"
    - "Every file rename across phases 16-19 was performed via git mv (git log --follow shows history)"
    - "No test assertions were modified during the entire v2.1 milestone"
  artifacts: []
  key_links: []
---

<objective>
Formally verify milestone-wide behavior preservation across all v2.1 phases (16-19 + 20-01). This is the final gate before closing the milestone.

Purpose: Satisfy BEHAV-01..04 requirements with auditable evidence that the v2.1 rename refactor preserved all behavior.
Output: Verification evidence in SUMMARY proving build green, tests green, git mv used, no assertion changes.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BEHAV-01 and BEHAV-02 — Build and test verification</name>
  <files></files>
  <action>
Run the final milestone build and test gate:

1. `dotnet build --no-incremental` — must succeed with zero warnings
2. `dotnet test` — must pass all tests with zero failures and zero skipped

Record the exact output (test count, pass count, fail count) as evidence for BEHAV-01 and BEHAV-02.

If any test fails, investigate and fix (but per milestone rules, no assertion changes are allowed — only rename-related fixes).
  </action>
  <verify>
    <automated>cd C:/Dev/Converj && dotnet test 2>&1 | tail -10</automated>
  </verify>
  <done>dotnet build succeeds with zero warnings. dotnet test passes all 415 tests with 0 failures and 0 skipped.</done>
</task>

<task type="auto">
  <name>Task 2: BEHAV-03 and BEHAV-04 — Git history and rename method verification</name>
  <files></files>
  <action>
Verify that renamed files preserve git history via `git mv` (not delete+create):

Run `git log --follow --oneline -3` on a representative sample of renamed files from each phase:

Phase 16 (no file renames — skip)

Phase 17 (core type renames):
- `src/Converj.Generator/FluentRootGenerator.cs` (was FluentFactoryGenerator.cs)
- `src/Converj.Generator/FluentRootCompilationUnit.cs` (was FluentFactoryCompilationUnit.cs)

Phase 18 (builder renames):
- `src/Converj.Generator/ModelBuilding/FluentModelBuilder.cs` (was FluentModelFactory.cs)
- `src/Converj.Generator/ModelBuilding/FluentMethodBuilder.cs` (was FluentMethodFactory.cs)

Phase 19 (test fixture renames):
- `src/Converj.Generator.Tests/EmptyRootTests.cs` (was EmptyFactoryTests.cs)
- `src/Converj.Generator.Tests/NestedRootTests.cs` (was NestedFactoryTests.cs)

For each file, confirm `git log --follow` shows commits both before and after the rename, proving `git mv` was used (BEHAV-04).

For BEHAV-03 (compiler-assisted rename): Review the phase SUMMARYs from phases 16-19. Each phase used dotnet build as verification after renames, which serves as compiler-assisted verification — if any reference was missed, the build would fail. Record this evidence.

Also verify no test assertions were modified during v2.1:
```bash
git log --oneline --all -- src/Converj.Generator.Tests/ | head -20
```
Cross-reference with phase SUMMARYs confirming "no assertion changes" in each phase.

Record all evidence in the SUMMARY for milestone sign-off.
  </action>
  <verify>
    <automated>cd C:/Dev/Converj && git log --follow --oneline -5 src/Converj.Generator/FluentRootGenerator.cs && echo "---" && git log --follow --oneline -5 src/Converj.Generator/ModelBuilding/FluentModelBuilder.cs && echo "---" && git log --follow --oneline -5 src/Converj.Generator.Tests/EmptyRootTests.cs</automated>
  </verify>
  <done>git log --follow shows preserved history across renames for all sampled files. Phase SUMMARYs confirm compiler-assisted rename methodology. No test assertions were modified during v2.1.</done>
</task>

</tasks>

<verification>
1. `dotnet build` zero warnings — BEHAV-01 satisfied
2. `dotnet test` all pass — BEHAV-02 satisfied
3. Phase SUMMARYs confirm compiler-assisted rename + build verification — BEHAV-03 satisfied
4. `git log --follow` shows history preservation on sample of renamed files — BEHAV-04 satisfied
</verification>

<success_criteria>
All four BEHAV requirements satisfied with recorded evidence. Milestone v2.1 ready to close.
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation-cleanup-final-verification/20-02-SUMMARY.md`
</output>
