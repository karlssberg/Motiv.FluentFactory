---
phase: 06-generated-code-hardening
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNonGenericTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNestedGenericTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorMergeTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorMergeDissimilarStepsTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorTargetTypeTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorPrimaryConstructorTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorBugDiscoveryTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryMethodCustomizationTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryMultipleMethodsGenerationTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryRootTypeTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryXmlDocumentationTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryXmlHeaderTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/FluentFactoryDiagnosticsTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/NamespaceTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/NamespaceConflictTests.cs
  - src/Motiv.FluentFactory.Generator.Tests/GeneratedCodeAttributeTests.cs
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "All existing tests pass with updated expected output reflecting global:: types, no usings, auto-generated header, nullable enable, and GeneratedCode attribute"
    - "A namespace conflict test proves global:: prevents compilation errors when consumer has conflicting type names"
    - "A test verifies GeneratedCode attribute presence on all generated type declarations"
    - "A test verifies the version string is non-empty and well-formed"
  artifacts:
    - path: "src/Motiv.FluentFactory.Generator.Tests/NamespaceConflictTests.cs"
      provides: "Namespace conflict regression test"
      contains: "NamespaceConflict"
    - path: "src/Motiv.FluentFactory.Generator.Tests/GeneratedCodeAttributeTests.cs"
      provides: "GeneratedCode attribute verification test"
      contains: "GeneratedCode"
  key_links:
    - from: "src/Motiv.FluentFactory.Generator.Tests/NamespaceConflictTests.cs"
      to: "CSharpSourceGeneratorVerifier"
      via: "Uses existing verifier infrastructure"
      pattern: "VerifyCS"
    - from: "src/Motiv.FluentFactory.Generator.Tests/GeneratedCodeAttributeTests.cs"
      to: "CSharpSourceGeneratorVerifier"
      via: "Uses existing verifier infrastructure"
      pattern: "VerifyCS"
---

<objective>
Bulk-update all existing test expected outputs to match the new generated code format, and add new validation tests for namespace conflict prevention and GeneratedCode attribute presence.

Purpose: Ensure zero test regressions after generator changes (QUAL-01) and validate the two new behaviors with dedicated tests (QUAL-01 namespace conflicts, QUAL-02 attribution).
Output: Full green test suite with 15+ updated test files and 2 new test files.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-generated-code-hardening/06-CONTEXT.md
@.planning/phases/06-generated-code-hardening/06-RESEARCH.md
@.planning/phases/06-generated-code-hardening/06-01-SUMMARY.md

<interfaces>
<!-- Test pattern used throughout the test project -->

From src/Motiv.FluentFactory.Generator.Tests/CSharpSourceGeneratorVerifier.cs:
```csharp
// Tests follow this pattern:
using VerifyCS = Motiv.FluentFactory.Generator.Tests.CSharpSourceGeneratorVerifier<Motiv.FluentFactory.Generator.FluentFactoryGenerator>;

// Create test:
await new VerifyCS.Test
{
    TestState =
    {
        Sources = { code },
        GeneratedSources =
        {
            (typeof(FluentFactoryGenerator), "FileName.g.cs", expected)
        }
    }
}.RunAsync();
```

<!-- Expected output format AFTER Plan 01 changes -->
Every expected output string must now:
1. Start with `// <auto-generated/>\n#nullable enable\n` (before the namespace)
2. Have NO using directives at the top
3. Use `global::` prefix on all concrete type references (parameters, return types, fields, attributes, constraint types)
4. Keep C# keywords as-is: int, string, bool, decimal, object, etc.
5. Keep type parameters (T, TResult) unqualified
6. Keep step struct names (Step_0__*, Step_1__*) unqualified within same namespace
7. Have `[global::System.CodeDom.Compiler.GeneratedCode("Motiv.FluentFactory", "VERSION")]` on every type declaration
8. Use `[global::System.Runtime.CompilerServices.MethodImpl(global::System.Runtime.CompilerServices.MethodImplOptions.AggressiveInlining)]` instead of `[System.Runtime.CompilerServices.MethodImpl(...)]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-update all existing test expected outputs</name>
  <files>
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNonGenericTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorGenericTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorNestedGenericTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorMergeTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorMergeDissimilarStepsTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorTargetTypeTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorPrimaryConstructorTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryGeneratorBugDiscoveryTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryMethodCustomizationTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryMultipleMethodsGenerationTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryRootTypeTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryXmlDocumentationTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryXmlHeaderTests.cs
    src/Motiv.FluentFactory.Generator.Tests/FluentFactoryDiagnosticsTests.cs
    src/Motiv.FluentFactory.Generator.Tests/NamespaceTests.cs
  </files>
  <action>
    Systematically update every `expected` string in all 15 test files. The approach:

    **Strategy: Run tests first, use failure output to guide updates.**

    1. Run `dotnet test src/Motiv.FluentFactory.Generator.Tests --no-build -v detailed 2>&1 | head -200` to see the first batch of failures. The test framework will show expected vs. actual output diffs.

    2. If tests cannot run with `--no-build` (because the generator project changed), run `dotnet build src/Motiv.FluentFactory.Generator.Tests` first.

    3. For each test file, apply these transformations to every `expected` string constant:

    **a. Remove using directives and add header:**
    Replace any `using ...;` lines at the top of expected strings with:
    ```
    // <auto-generated/>
    #nullable enable
    ```

    **b. Add [GeneratedCode] attribute to every type declaration:**
    Before each `public static partial class`, `public struct`, `public partial class`, etc., add:
    ```
    [global::System.CodeDom.Compiler.GeneratedCode("Motiv.FluentFactory", "VERSION")]
    ```
    For the version string, use a pattern match approach. Since the version will vary, the test verifier may need exact match. Check what version the assembly reports and use that exact string. If the version is `1.0.0.0` (default assembly version), use that. Run a quick check: the generator csproj may set a version. Use whatever the actual assembly version is.

    IMPORTANT: If exact version matching is problematic, consider creating a test helper that normalizes the version string before comparison, OR check what version the assembly reports at test time and use that value in expected strings.

    **c. Qualify all type references with global:::**
    - `MyBuildTarget` (return type of Create methods) -> `global::Test.MyBuildTarget` (or `global::FullNamespace.TypeName`)
    - `new MyBuildTarget(...)` (object creation) -> `new global::Test.MyBuildTarget(...)`
    - Types from other namespaces -> `global::Namespace.TypeName`
    - C# keywords (`int`, `string`, `bool`, `decimal`, `object`) stay as-is
    - Type parameters (`T`, `TResult`) stay as-is
    - Step struct names (`Step_0__Test_Factory`) stay unqualified within same namespace
    - `System.Runtime.CompilerServices.MethodImpl(...)` -> `global::System.Runtime.CompilerServices.MethodImpl(global::System.Runtime.CompilerServices.MethodImplOptions.AggressiveInlining)`

    **d. Handle namespace-spanning cases (NamespaceTests.cs):**
    When step structs are referenced from a different namespace, they need `global::` qualification. When in the same namespace, they stay unqualified.

    **e. Iterative approach:**
    After updating a batch of tests, run `dotnet test src/Motiv.FluentFactory.Generator.Tests -x` (which stops at first failure) to verify progress. Fix failures iteratively until all tests pass.

    **f. Pay attention to whitespace:**
    The generator uses `NormalizeWhitespace()` which standardizes indentation. Ensure expected strings match exactly, including indentation levels and blank lines.
  </action>
  <verify>
    <automated>cd C:/Dev/Motiv.FluentFactory && dotnet test src/Motiv.FluentFactory.Generator.Tests 2>&1 | tail -20</automated>
  </verify>
  <done>All existing tests pass with updated expected output strings reflecting global:: qualification, no using directives, auto-generated header, nullable enable, GeneratedCode attribute, and global:: AggressiveInlining attribute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add namespace conflict test and GeneratedCode attribute verification test</name>
  <files>
    src/Motiv.FluentFactory.Generator.Tests/NamespaceConflictTests.cs
    src/Motiv.FluentFactory.Generator.Tests/GeneratedCodeAttributeTests.cs
  </files>
  <behavior>
    - Test 1 (NamespaceConflictTests): A consumer project defines a type named `String` in its own namespace. The generated fluent factory code compiles without errors because all type references use `global::` prefix, preventing the consumer's `String` from shadowing `System.String`.
    - Test 2 (NamespaceConflictTests): A consumer project defines a type named `List` that could shadow `System.Collections.Generic.List<T>`. Generated code compiles because it uses `global::System.Collections.Generic.List<T>`.
    - Test 3 (GeneratedCodeAttributeTests): Generated output for a simple factory contains `[global::System.CodeDom.Compiler.GeneratedCode("Motiv.FluentFactory", "VERSION")]` on the root partial class declaration.
    - Test 4 (GeneratedCodeAttributeTests): Generated output for a simple factory contains `[global::System.CodeDom.Compiler.GeneratedCode("Motiv.FluentFactory", "VERSION")]` on the step struct declaration.
    - Test 5 (GeneratedCodeAttributeTests): The version string in the GeneratedCode attribute is non-empty and matches a version-like pattern (digits and dots).
  </behavior>
  <action>
    **Step A: Create NamespaceConflictTests.cs**

    ```csharp
    using VerifyCS = Motiv.FluentFactory.Generator.Tests.CSharpSourceGeneratorVerifier<Motiv.FluentFactory.Generator.FluentFactoryGenerator>;

    namespace Motiv.FluentFactory.Generator.Tests;

    public class NamespaceConflictTests
    {
        [Fact]
        internal async Task Should_compile_when_consumer_defines_type_named_String()
        {
            // Source code where the consumer has a `String` type in the same namespace
            const string code = """
                using System;
                using Motiv.FluentFactory.Generator;

                namespace ConflictTest;

                public class String { }  // Shadows System.String

                [FluentFactory]
                public static partial class Factory;

                public class Target
                {
                    [FluentConstructor(typeof(Factory))]
                    public Target(string name, int value)
                    {
                        Name = name;
                        Value = value;
                    }
                    public string Name { get; }
                    public int Value { get; }
                }
                """;

            // Expected output uses global:: so `string` keyword and `global::ConflictTest.Target` avoid conflicts
            const string expected = /* expected output with global:: types, GeneratedCode attr, no usings, header */;

            await new VerifyCS.Test
            {
                TestState =
                {
                    Sources = { code },
                    GeneratedSources =
                    {
                        (typeof(FluentFactoryGenerator), "Factory.g.cs", expected)
                    }
                }
            }.RunAsync();
        }
    }
    ```

    Build the full expected output string following the format established by the updated tests in Task 1. The key assertion: the test compiles and the verifier confirms the output matches, proving that a namespace with a conflicting `String` type doesn't break generated code.

    **Step B: Create GeneratedCodeAttributeTests.cs**

    ```csharp
    using VerifyCS = Motiv.FluentFactory.Generator.Tests.CSharpSourceGeneratorVerifier<Motiv.FluentFactory.Generator.FluentFactoryGenerator>;

    namespace Motiv.FluentFactory.Generator.Tests;

    public class GeneratedCodeAttributeTests
    {
        [Fact]
        internal async Task Should_add_GeneratedCode_attribute_to_root_type_and_step_struct()
        {
            // Simple factory with one parameter
            const string code = """
                using System;
                using Motiv.FluentFactory.Generator;

                namespace Test;

                [FluentFactory]
                public static partial class Factory;

                public class MyTarget
                {
                    [FluentConstructor(typeof(Factory))]
                    public MyTarget(int value) { Value = value; }
                    public int Value { get; }
                }
                """;

            const string expected = /* expected output with GeneratedCode on both type declarations */;

            await new VerifyCS.Test
            {
                TestState =
                {
                    Sources = { code },
                    GeneratedSources =
                    {
                        (typeof(FluentFactoryGenerator), "Factory.g.cs", expected)
                    }
                }
            }.RunAsync();
        }

        [Fact]
        internal void Should_have_non_empty_well_formed_version_string()
        {
            var version = typeof(FluentFactoryGenerator).Assembly.GetName().Version?.ToString();
            Assert.NotNull(version);
            Assert.NotEmpty(version);
            Assert.Matches(@"^\d+\.\d+\.\d+(\.\d+)?$", version);
        }
    }
    ```

    Fill in the full expected output strings. The GeneratedCode attribute version must match whatever the assembly actually reports. Determine this by checking `typeof(FluentFactoryGenerator).Assembly.GetName().Version` in the test helper or by reading the generator csproj version property.

    **Step C: Follow TDD cycle**

    RED: Write the test files with expected output. Run tests -- they should fail if any expected output is wrong.
    GREEN: Adjust expected output to match actual generator output.
    REFACTOR: Clean up test code if needed.
  </action>
  <verify>
    <automated>cd C:/Dev/Motiv.FluentFactory && dotnet test src/Motiv.FluentFactory.Generator.Tests --filter "FullyQualifiedName~NamespaceConflict|FullyQualifiedName~GeneratedCode" 2>&1 | tail -20</automated>
  </verify>
  <done>NamespaceConflictTests pass proving global:: prevents compilation errors with conflicting type names. GeneratedCodeAttributeTests pass proving all generated types carry the attribute. Version string test confirms non-empty, well-formed version. Full test suite passes: `dotnet test src/Motiv.FluentFactory.Generator.Tests` reports zero failures.</done>
</task>

</tasks>

<verification>
Full test suite green: `dotnet test src/Motiv.FluentFactory.Generator.Tests`
Namespace conflict test passes: `dotnet test src/Motiv.FluentFactory.Generator.Tests --filter "FullyQualifiedName~NamespaceConflict"`
GeneratedCode attribute test passes: `dotnet test src/Motiv.FluentFactory.Generator.Tests --filter "FullyQualifiedName~GeneratedCode"`
</verification>

<success_criteria>
- All 15+ existing test files updated with correct expected output
- Zero test failures in full suite
- NamespaceConflictTests validates that conflicting type names (String, List) in consumer namespace do not break generated code
- GeneratedCodeAttributeTests validates attribute presence on root type and step struct
- Version string test validates non-empty, well-formed version
</success_criteria>

<output>
After completion, create `.planning/phases/06-generated-code-hardening/06-02-SUMMARY.md`
</output>
