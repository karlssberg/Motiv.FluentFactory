---
phase: 21-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Converj.Generator.Tests/SingularizationTests.cs
  - src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs
  - src/Converj.Generator.Tests/CollectionTypeDetectionTests.cs
  - src/Converj.Generator.Tests/AccumulatorNameOverrideTests.cs
  - src/Converj.Generator.Tests/AccumulatorNameCollisionTests.cs
  - src/Converj.Generator.Tests/BackwardCompatibilitySnapshotTests.cs
autonomous: true
requirements: [ATTR-01, ATTR-02, ATTR-03, NAME-01, NAME-02, NAME-03, NAME-04, BACK-02]
must_haves:
  truths:
    - "All six test classes exist and compile as empty/skipped shells"
    - "Full `dotnet test` still yields exactly the same pass/fail counts as before this plan (no new regressions)"
    - "VALIDATION.md's Wave 0 checklist items can be marked complete after this plan"
  artifacts:
    - path: "src/Converj.Generator.Tests/SingularizationTests.cs"
      provides: "Test class stub for NAME-01/NAME-03"
      contains: "public class SingularizationTests"
    - path: "src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs"
      provides: "Test class stub for ATTR-01"
      contains: "public class FluentCollectionMethodAttributeTests"
    - path: "src/Converj.Generator.Tests/CollectionTypeDetectionTests.cs"
      provides: "Test class stub for ATTR-02/ATTR-03"
      contains: "public class CollectionTypeDetectionTests"
    - path: "src/Converj.Generator.Tests/AccumulatorNameOverrideTests.cs"
      provides: "Test class stub for NAME-02"
      contains: "public class AccumulatorNameOverrideTests"
    - path: "src/Converj.Generator.Tests/AccumulatorNameCollisionTests.cs"
      provides: "Test class stub for NAME-04"
      contains: "public class AccumulatorNameCollisionTests"
    - path: "src/Converj.Generator.Tests/BackwardCompatibilitySnapshotTests.cs"
      provides: "Test class stub for BACK-02"
      contains: "public class BackwardCompatibilitySnapshotTests"
  key_links:
    - from: "Downstream plans 02-05"
      to: "Stub test classes"
      via: "Fact methods appended (not replacing stub classes)"
      pattern: "public class .+Tests"
---

<objective>
Create Wave 0 test scaffolding — the six empty xUnit test classes that Plans 02–05 will fill with concrete test methods. This plan establishes the nyquist-compliant test file layout described in `21-VALIDATION.md` before any production code ships, so every downstream task has a valid `dotnet test --filter ...` target from the moment its implementation commit lands.

Purpose: VALIDATION.md's Wave 0 requirement list mandates these six test files exist before any Phase 21 implementation. Creating stubs first keeps `--no-build` test filtering deterministic, decouples test-class creation from the heavier wiring tasks, and lets Plans 02–05 execute in smaller, strictly-focused commits.

Output: Six new `_Tests.cs` files in `src/Converj.Generator.Tests/`, each a compilable empty xUnit class with the established `using` and `VerifyCS` aliases, committed under `test(21-01): ...`.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-foundation/21-CONTEXT.md
@.planning/phases/21-foundation/21-RESEARCH.md
@.planning/phases/21-foundation/21-VALIDATION.md

@src/Converj.Generator.Tests/ExtensionMethodTests.cs
@src/Converj.Generator.Tests/CSharpSourceGeneratorVerifier.cs

<interfaces>
<!-- Established test file skeleton pattern in this repo (from ExtensionMethodTests.cs). -->
<!-- All new test files must follow this exact top-of-file shape: -->

```csharp
using Microsoft.CodeAnalysis.Testing;
using static Converj.Generator.Diagnostics.FluentDiagnostics;
using VerifyCS =
    Converj.Generator.Tests.CSharpSourceGeneratorVerifier<Converj.Generator.FluentRootGenerator>;

namespace Converj.Generator.Tests;

public class {Name}Tests
{
    // [Fact] methods added in later plans
}
```

`CSharpSourceGeneratorVerifier<FluentRootGenerator>` already injects `global using Converj.Attributes;` into every test compilation (Pitfall 8 in research), so new test code does not need explicit `using Converj.Attributes;`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create attribute + detection + naming test class stubs</name>
  <files>
    src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs,
    src/Converj.Generator.Tests/CollectionTypeDetectionTests.cs,
    src/Converj.Generator.Tests/SingularizationTests.cs,
    src/Converj.Generator.Tests/AccumulatorNameOverrideTests.cs
  </files>
  <action>
    Create four new xUnit test class files under `src/Converj.Generator.Tests/`. Each file must use the exact top-of-file header from `ExtensionMethodTests.cs` (see `<interfaces>` block):

    - `using Microsoft.CodeAnalysis.Testing;`
    - `using static Converj.Generator.Diagnostics.FluentDiagnostics;`
    - `using VerifyCS = Converj.Generator.Tests.CSharpSourceGeneratorVerifier<Converj.Generator.FluentRootGenerator>;`
    - `namespace Converj.Generator.Tests;`
    - One `public class` declaration per file matching the file name (no `[Fact]` methods yet).

    Files and class names:
    - `FluentCollectionMethodAttributeTests.cs` → `public class FluentCollectionMethodAttributeTests`
    - `CollectionTypeDetectionTests.cs` → `public class CollectionTypeDetectionTests`
    - `SingularizationTests.cs` → `public class SingularizationTests`
    - `AccumulatorNameOverrideTests.cs` → `public class AccumulatorNameOverrideTests`

    Add a single placeholder `[Fact] internal void Placeholder() { /* replaced in later plans */ }` to each class so xUnit's discovery does not flag an empty class and so filtered `dotnet test --filter "FullyQualifiedName~{ClassName}"` returns at least one executed test (confirms the class is discoverable).

    Do NOT reference `FluentCollectionMethodAttribute`, `Singularize`, or any `CVJG005x` descriptor in these files yet — those types do not exist until Plans 02/03 and referencing them here would break the build. The stubs must compile against the current codebase unchanged.

    Do NOT use `using static Converj.Generator.Diagnostics.FluentDiagnostics;` unless a placeholder uses it; if it becomes an "unused using" warning under `WarningsAsErrors`, drop it in the stubs and let Plans 02–05 add it back when real tests are written. Prefer to omit it in the stubs to stay green.
  </action>
  <verify>
    <automated>dotnet build src/Converj.Generator.Tests/Converj.Generator.Tests.csproj</automated>
  </verify>
  <done>All four new files exist, the test project builds clean with zero warnings, and `dotnet test --filter "FullyQualifiedName~FluentCollectionMethodAttributeTests|FullyQualifiedName~CollectionTypeDetectionTests|FullyQualifiedName~SingularizationTests|FullyQualifiedName~AccumulatorNameOverrideTests" --no-build` executes exactly four placeholder tests, all passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create collision + backward-compat test class stubs</name>
  <files>
    src/Converj.Generator.Tests/AccumulatorNameCollisionTests.cs,
    src/Converj.Generator.Tests/BackwardCompatibilitySnapshotTests.cs
  </files>
  <action>
    Create the remaining two xUnit test class stubs in `src/Converj.Generator.Tests/` following the same header pattern as Task 1 (see `<interfaces>`):

    - `AccumulatorNameCollisionTests.cs` → `public class AccumulatorNameCollisionTests`
    - `BackwardCompatibilitySnapshotTests.cs` → `public class BackwardCompatibilitySnapshotTests`

    Both files contain one `[Fact] internal void Placeholder() { }` method and no other test content. `BackwardCompatibilitySnapshotTests` is where Plan 05 will add the pinned-string snapshot test for BACK-02 — keep the stub deliberately empty so that work lands in a clean diff.

    Again, do NOT reference `CVJG0052 AccumulatorMethodNameCollision` (doesn't exist yet) or `FluentCollectionMethodAttribute` here. Stubs must compile against the pre-Phase-21 codebase.
  </action>
  <verify>
    <automated>dotnet test src/Converj.Generator.Tests/Converj.Generator.Tests.csproj --filter "FullyQualifiedName~AccumulatorNameCollisionTests|FullyQualifiedName~BackwardCompatibilitySnapshotTests" --no-build</automated>
  </verify>
  <done>Both files exist, the full `dotnet test` suite still returns the same pass/fail count as before this plan plus exactly 6 new passing placeholder tests (one per new file from Tasks 1+2), with zero new failures. `21-VALIDATION.md` Wave 0 checkboxes ready to be ticked.</done>
</task>

</tasks>

<verification>
Run the full test suite and confirm no regressions:
```
dotnet test
```
Expected: Pre-plan baseline `(415 passed - 9 known-failing = 406 passing, 9 failing)` + 6 new placeholders → `412 passing, 9 failing` (baseline count delta verified).
</verification>

<success_criteria>
- All six new test files exist and compile cleanly under `<WarningsAsErrors>true</WarningsAsErrors>`
- `dotnet test` pre/post delta is exactly +6 passing tests, zero new failures
- `21-VALIDATION.md` Wave 0 file-exists checklist can be marked complete
- No production code touched in this plan (files_modified is scoped to Tests project only)
</success_criteria>

<output>
After completion, create `.planning/phases/21-foundation/21-01-SUMMARY.md` summarising: the six test files created, their class names, the placeholder-test count, and the pre/post `dotnet test` delta. Flag that Plans 02–05 will extend these classes with real `[Fact]` methods.
</output>
