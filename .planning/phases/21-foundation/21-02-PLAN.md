---
phase: 21-foundation
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/Converj.Attributes/FluentCollectionMethodAttribute.cs
  - src/Converj.Generator/Domain/TypeName.cs
  - src/Converj.Generator/Diagnostics/FluentDiagnostics.cs
  - src/Converj.Generator/AnalyzerReleases.Unshipped.md
  - src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs
autonomous: true
requirements: [ATTR-01, ATTR-03, NAME-03, NAME-04]
must_haves:
  truths:
    - "Developer can write `[FluentCollectionMethod]` and `[FluentCollectionMethod(\"AddEntry\")]` in user code and it resolves — the attribute class exists in Converj.Attributes"
    - "`[FluentCollectionMethod]` can only be applied to parameters (AttributeTargets.Parameter), not to types, properties, methods, etc."
    - "`FluentCollectionMethodAttribute.MinItems` is a public settable int property defaulting to 0, stable for v2.2 even though Phase 21 does not enforce it"
    - "Three new diagnostic descriptors (CVJG0050, CVJG0051, CVJG0052) are available from FluentDiagnostics and registered in AnalyzerReleases.Unshipped.md"
    - "Generator code can reference the new attribute via `TypeName.FluentCollectionMethodAttribute`"
  artifacts:
    - path: "src/Converj.Attributes/FluentCollectionMethodAttribute.cs"
      provides: "Public user-facing attribute class"
      contains: "public class FluentCollectionMethodAttribute"
    - path: "src/Converj.Generator/Domain/TypeName.cs"
      provides: "FluentCollectionMethodAttribute TypeName constant"
      contains: "public static readonly TypeName FluentCollectionMethodAttribute"
    - path: "src/Converj.Generator/Diagnostics/FluentDiagnostics.cs"
      provides: "CVJG0050/0051/0052 DiagnosticDescriptor fields"
      contains: "NonCollectionFluentCollectionMethod"
    - path: "src/Converj.Generator/AnalyzerReleases.Unshipped.md"
      provides: "CVJG0050..0052 rule entries"
      contains: "CVJG0050"
    - path: "src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs"
      provides: "ATTR-01 real tests replacing placeholder"
      contains: "[Fact]"
  key_links:
    - from: "FluentCollectionMethodAttribute"
      to: "Converj.Attributes namespace"
      via: "namespace declaration"
      pattern: "namespace Converj\\.Attributes"
    - from: "TypeName.FluentCollectionMethodAttribute"
      to: "FluentCollectionMethodAttribute full name string"
      via: "static readonly field using GeneratorAttributesNamespace prefix"
      pattern: "GeneratorAttributesNamespace \\+ \"FluentCollectionMethodAttribute\""
    - from: "AnalyzerReleases.Unshipped.md"
      to: "CVJG0050..0052 descriptors"
      via: "three appended rows with Category = Converj, Severity = Error"
      pattern: "CVJG005[012] \\| Converj \\| Error"
---

<objective>
Ship the public `[FluentCollectionMethod]` attribute surface and the three new diagnostic descriptors that Phase 21's analyzer (Plan 04) and collision filter (Plan 05) will emit. After this plan, user code using `[FluentCollectionMethod]` compiles against `Converj.Attributes` but the generator ignores it — the attribute is visible to consumers before the analyzer wiring lands, which is exactly the order CONTEXT.md mandates (stable API first, behaviour later).

Purpose: Attribute + diagnostics are the shared foundation that Plans 03 and 04 depend on. They are independent of singularization, and splitting them from the analyzer keeps each plan under ~50% context and aligns file ownership (no overlap with Plan 03).

Output: One new attribute class, one new TypeName constant, three new `DiagnosticDescriptor` fields in `FluentDiagnostics`, three new rows in `AnalyzerReleases.Unshipped.md`, and three new `[Fact]` tests in `FluentCollectionMethodAttributeTests` exercising ATTR-01 at the "user code compiles" level (no analyzer behaviour yet).
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-foundation/21-CONTEXT.md
@.planning/phases/21-foundation/21-RESEARCH.md

@src/Converj.Attributes/FluentMethodAttribute.cs
@src/Converj.Generator/Diagnostics/FluentDiagnostics.cs
@src/Converj.Generator/Domain/TypeName.cs
@src/Converj.Generator/AnalyzerReleases.Unshipped.md

<interfaces>
<!-- Existing attribute shape to mirror (FluentMethodAttribute.cs): -->
```csharp
[ExcludeFromCodeCoverage]
[AttributeUsage(AttributeTargets.Parameter | AttributeTargets.Property)]
public class FluentMethodAttribute : Attribute
{
    public FluentMethodAttribute() { }
    public FluentMethodAttribute(string methodName) { MethodName = methodName; }
    public string? MethodName { get; }
}
```

<!-- Existing TypeName constant shape: -->
```csharp
public static readonly TypeName FluentMethodAttribute =
    new(GeneratorAttributesNamespace + "FluentMethodAttribute");
```
(`GeneratorAttributesNamespace` is the private constant already defined in `TypeName.cs`.)

<!-- Existing diagnostic descriptor shape (CVJG0011 UnsupportedParameterModifier): -->
```csharp
public static readonly DiagnosticDescriptor UnsupportedParameterModifier = new(
    id: "CVJG0011",
    title: "Unsupported parameter modifier",
    messageFormat: "...",
    category: Category,                        // private const string Category = "Converj";
    defaultSeverity: DiagnosticSeverity.Error, // or Warning
    isEnabledByDefault: true);
```

<!-- AnalyzerReleases.Unshipped.md row format: -->
```
Rule ID | Category | Severity | Notes
CVJG0050 | Converj | Error | FluentCollectionMethod on non-collection parameter
```

<!-- Test verifier (used in FluentCollectionMethodAttributeTests): -->
```csharp
using VerifyCS = Converj.Generator.Tests.CSharpSourceGeneratorVerifier<Converj.Generator.FluentRootGenerator>;
// VerifyCS.Test already injects `global using Converj.Attributes;` into test compilations
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FluentCollectionMethodAttribute class</name>
  <files>
    src/Converj.Attributes/FluentCollectionMethodAttribute.cs,
    src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs
  </files>
  <behavior>
    - Test 1 `Applying_parameterless_attribute_compiles`: user code `public Cat([FluentCollectionMethod] IEnumerable&lt;string&gt; tags) { }` inside a `[FluentRoot]`-annotated file compiles without error via `VerifyCS.Test { TestState = { Sources = { code } } }.RunAsync()`. No generated source assertion yet (analyzer doesn't exist).
    - Test 2 `Applying_single_string_attribute_compiles`: `[FluentCollectionMethod("AddEntry")]` on a collection parameter compiles. Same test shape.
    - Test 3 `MinItems_named_argument_compiles`: `[FluentCollectionMethod(MinItems = 3)]` compiles (exercises the settable property).
    - Test 4 `Applying_to_type_is_compile_error`: `[FluentCollectionMethod] public class Foo {}` produces a CS0592-style diagnostic from the C# compiler (not the generator) because AttributeUsage is Parameter-only. Assert via `ExpectedDiagnostics.Add(DiagnosticResult.CompilerError("CS0592").WithSpan(...))`.

    All four tests go in `FluentCollectionMethodAttributeTests.cs`, REPLACING the placeholder test from Plan 01.
  </behavior>
  <action>
    1. Create `src/Converj.Attributes/FluentCollectionMethodAttribute.cs` exactly per the Pattern 1 / CONTEXT spec — mirror `FluentMethodAttribute.cs` structure:
       - `namespace Converj.Attributes;`
       - `[ExcludeFromCodeCoverage]` (requires `using System.Diagnostics.CodeAnalysis;`)
       - `[AttributeUsage(AttributeTargets.Parameter)]` — Parameter ONLY, not `Parameter | Property`
       - `public class FluentCollectionMethodAttribute : Attribute`
       - Two constructors: parameterless and `(string methodName)` assigning `MethodName`
       - `public string? MethodName { get; }` (init-by-ctor, no public setter)
       - `public int MinItems { get; set; } = 0;` (public settable, initial value 0) — CONTEXT decision: defined in Phase 21 for stable v2.2 surface, enforcement deferred to Phase 24
       - XML doc comments on the class and every public member referencing Phase 24 enforcement for `MinItems`

    2. Replace the placeholder in `src/Converj.Generator.Tests/FluentCollectionMethodAttributeTests.cs` with the four tests described in `<behavior>`. Use raw string literals (`"""..."""`) for source code. Tests do NOT assert on generated sources (analyzer still absent); they only assert the user code compiles (no `GeneratedSources`, no `ExpectedDiagnostics` except for Test 4's CS0592).

    3. Run `dotnet build src/Converj.Attributes/Converj.Attributes.csproj` to confirm the attributes project still packages cleanly (netstandard2.0 target). Then run the filtered test.

    Do NOT add any generator-side code in this task. Do NOT touch `TypeName.cs` yet (next task). The attribute must be purely additive to the attributes project.
  </action>
  <verify>
    <automated>dotnet test src/Converj.Generator.Tests/Converj.Generator.Tests.csproj --filter "FullyQualifiedName~FluentCollectionMethodAttributeTests"</automated>
  </verify>
  <done>All four `FluentCollectionMethodAttributeTests` pass. The attributes project builds for netstandard2.0 with zero warnings. The attribute class has `[AttributeUsage(AttributeTargets.Parameter)]`, two ctors, nullable `MethodName`, and settable `int MinItems = 0`. ATTR-01 closed at the "attribute exists and binds" level (analyzer behaviour still pending Plan 04).</done>
</task>

<task type="auto">
  <name>Task 2: Register TypeName constant + three diagnostic descriptors + Unshipped.md entries</name>
  <files>
    src/Converj.Generator/Domain/TypeName.cs,
    src/Converj.Generator/Diagnostics/FluentDiagnostics.cs,
    src/Converj.Generator/AnalyzerReleases.Unshipped.md
  </files>
  <action>
    1. **`src/Converj.Generator/Domain/TypeName.cs`** — append one line after the existing `FluentMethodAttribute` constant (line 16):
       ```csharp
       public static readonly TypeName FluentCollectionMethodAttribute =
           new(GeneratorAttributesNamespace + "FluentCollectionMethodAttribute");
       ```
       Preserve alphabetical / logical ordering of surrounding constants as-is. Do not touch other entries.

    2. **`src/Converj.Generator/Diagnostics/FluentDiagnostics.cs`** — append three new `DiagnosticDescriptor` fields at the END of the class (after `RootMustBeStaticForExtensionTargets`, the current CVJG0049 tail). Use exactly these IDs, titles, message formats, and severities from RESEARCH.md Pattern 5:

       - **CVJG0050 NonCollectionFluentCollectionMethod** (severity Error)
         `title`: `"FluentCollectionMethod on non-collection parameter"`
         `messageFormat`: `"Parameter '{0}' of type '{1}' is not a supported collection type. [FluentCollectionMethod] requires T[], IEnumerable<T>, ICollection<T>, IList<T>, IReadOnlyCollection<T>, or IReadOnlyList<T>."`

       - **CVJG0051 UnsingularizableParameterName** (severity Error)
         `title`: `"Cannot derive accumulator method name"`
         `messageFormat`: `"Cannot derive an accumulator method name from parameter '{0}'. Provide an explicit name, e.g., [FluentCollectionMethod(\"AddEntry\")]."`

       - **CVJG0052 AccumulatorMethodNameCollision** (severity Error)
         `title`: `"Accumulator method name collision"`
         `messageFormat`: `"Parameters '{0}' and '{1}' on '{2}' both produce accumulator method '{3}'. Disambiguate via [FluentCollectionMethod(\"AlternateName\")]."`

       All three: `category: Category`, `defaultSeverity: DiagnosticSeverity.Error`, `isEnabledByDefault: true`. Copy the XML `<summary>` doc-comment style from `UnsupportedParameterModifier` (CVJG0011) exactly — one-sentence summary referencing the requirement ID (ATTR-03, NAME-03, NAME-04) is helpful for future readers.

       Note `CVJG0034` is intentionally skipped (gap); do not reuse it. Use 0050/0051/0052 strictly.

    3. **`src/Converj.Generator/AnalyzerReleases.Unshipped.md`** — append three rows at the end of the existing table (after the CVJG0049 row):
       ```
       CVJG0050 | Converj | Error | FluentCollectionMethod on non-collection parameter
       CVJG0051 | Converj | Error | Cannot derive accumulator method name
       CVJG0052 | Converj | Error | Accumulator method name collision
       ```

    4. Build the generator and run the full test suite. No existing test should break — these additions are purely additive. The descriptors are defined but not yet emitted (no analyzer wiring until Plan 04). `FluentCollectionMethodAttributeTests` still passes.

    5. Do NOT add any `using static Converj.Generator.Diagnostics.FluentDiagnostics;` import anywhere to reference the new descriptors yet — that wiring belongs in Plans 04 and 05.

    Do NOT modify `AnalyzerReleases.Shipped.md`.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~FluentCollectionMethodAttributeTests"</automated>
  </verify>
  <done>
    - `TypeName.FluentCollectionMethodAttribute` resolves in generator code (compiles).
    - `FluentDiagnostics.NonCollectionFluentCollectionMethod`, `.UnsingularizableParameterName`, and `.AccumulatorMethodNameCollision` are accessible public static readonly fields on `FluentDiagnostics`.
    - `AnalyzerReleases.Unshipped.md` has exactly three new rows for CVJG0050/0051/0052 — passes the `Microsoft.CodeAnalysis.Analyzers` release-tracking check (build stays green).
    - Full `dotnet test` pass/fail count unchanged vs. pre-plan baseline + the four new ATTR-01 tests (Task 1) passing.
  </done>
</task>

</tasks>

<verification>
Run full test suite and confirm the release-tracking analyzer accepts the Unshipped.md additions:
```
dotnet build
dotnet test
```
Expected: No new failures. The RS2008 / release-tracking diagnostics do not trigger (all three new descriptors are registered in Unshipped.md).
</verification>

<success_criteria>
- `[FluentCollectionMethod]` attribute is resolvable from user code (ATTR-01 scaffolded, full analyzer behaviour pending Plan 04).
- `[FluentCollectionMethod]` on a non-parameter target produces the C# compiler's own CS0592 (baseline enforcement before our analyzer layer).
- Three new `CVJG005x` descriptors exist and are release-tracked; generator compiles without release-tracking warnings.
- `TypeName.FluentCollectionMethodAttribute` constant is consumable by analyzer code that Plan 04 will add.
- BACK-01 still holds: existing 406 passing tests still pass; 9 failing tests still fail; no delta beyond Plan 01/02 additions.
</success_criteria>

<output>
After completion, create `.planning/phases/21-foundation/21-02-SUMMARY.md` listing: the attribute file, its public shape (two ctors + `MethodName` + `MinItems`), the TypeName constant, the three new `DiagnosticDescriptor` field names + IDs + severities, and the Unshipped.md rows. Explicitly note MinItems is parsed-but-not-enforced in Phase 21.
</output>
