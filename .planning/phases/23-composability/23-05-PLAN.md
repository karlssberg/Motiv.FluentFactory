---
phase: 23-composability
plan: 05
type: execute
wave: 5
depends_on: ["23-04"]
files_modified:
  - src/Converj.Generator.Tests/BackwardCompatibilitySnapshotTests.cs
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "Full solution builds under warnings-as-errors with the Phase 23 changes applied"
    - "Every existing test from Phase 22 baseline (440 tests) continues to pass — BACK-01 compliance"
    - "Backward compatibility snapshot (BackwardCompatibilitySnapshotTests) produces byte-identical generated output for code not using the Phase 23 feature widenings — BACK-02 compliance"
    - "Retrofit audit deviations (if any) documented in Plan 23-04 SUMMARY are human-approved via the checkpoint in this plan"
    - "Phase 23 success criteria from the amended ROADMAP.md are all observable via dotnet test"
  artifacts:
    - path: ".planning/phases/23-composability/23-05-SUMMARY.md"
      provides: "Final Phase 23 completion summary with test counts and audit dispositions"
      min_lines: 40
  key_links:
    - from: "BackwardCompatibilitySnapshotTests"
      to: "Phase 21 Plan 05 snapshot fixtures"
      via: "Byte-identical regeneration check"
      pattern: "BackwardCompatibilitySnapshotTests"
---

<objective>
Phase 23 regression gate. Run the full test suite; confirm BACK-01 (existing tests unchanged) and BACK-02 (byte-identical output for non-feature code). Review the retrofit audit table from Plan 23-04 with the user. Present the final Phase 23 success criteria as observable test outcomes. Produce the phase-level SUMMARY consolidating Plans 23-01 through 23-05.

Purpose: Phase completion gate. All preceding plans delivered incremental capability; this plan verifies nothing regressed, asks the user to sign off on any documented deviations from BACK-01/BACK-02, and closes the phase.

Output: Final SUMMARY, user-approved audit dispositions, green full suite.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-composability/23-CONTEXT.md
@.planning/phases/23-composability/23-RESEARCH.md
@.planning/phases/23-composability/23-VALIDATION.md
@.planning/phases/23-composability/23-01-SUMMARY.md
@.planning/phases/23-composability/23-02-SUMMARY.md
@.planning/phases/23-composability/23-03-SUMMARY.md
@.planning/phases/23-composability/23-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@src/Converj.Generator.Tests/BackwardCompatibilitySnapshotTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite regression + BACK-01 / BACK-02 verification + Phase 23 success-criteria observability</name>
  <files>src/Converj.Generator.Tests/BackwardCompatibilitySnapshotTests.cs</files>
  <action>
(A) Run full build and test suite from a clean state:
  1. `dotnet build --nologo` — MUST be green under warnings-as-errors.
  2. `dotnet test --no-build --nologo` — capture total, passed, failed counts.
  3. Record pre-phase baseline (440 tests at end of Phase 22 per STATE.md) and post-phase actual count.
  4. Difference = new tests added across Plans 23-01 through 23-04. Compute the expected delta from each plan's SUMMARY and confirm the total matches.

(B) Backward compatibility snapshot verification (BACK-02):
  - `BackwardCompatibilitySnapshotTests.cs` was established in Phase 21 Plan 05 as the byte-identical snapshot gate. Re-run it; if any snapshot fails, investigate. Determinations:
    - If the change is a `$$VERSION$$` placeholder or equivalent expected drift → no action.
    - If the change touches non-Phase-23 code paths → regression. Investigate and fix OR escalate to checkpoint.
    - If the change is in Phase-23-modified files and the feature is NOT in use in the snapshot fixture → regression. Investigate: file touched but fixture doesn't exercise feature means emission is not byte-identical. Find the cause (typically an emission-order change or a forgotten branch).
  - Do not blindly re-pin snapshots. BACK-02 explicitly requires byte-identical output for non-feature code.

(C) Phase 23 amended success criteria — map each to an observable test or test-class:
  | Amended Success Criterion (from ROADMAP.md Phase 23) | Observable via |
  |------------------------------------------------------|----------------|
  | Free composition of AddX/WithXs on accumulator step  | `CollectionMethodComposabilityTests.COMP_03_free_composition_runtime_behavior` (or source-gen equivalent) |
  | Both methods emitted, aggressive-inlining, self-returning | `CollectionMethodBulkSetTests` |
  | Property parity — same shape as parameter           | `PropertyBackedCollectionTests.Property_accumulator_emits_addX_method` |
  | CVJG0053 on unsupported accessor                     | `CollectionMethodPropertyAccessorDiagnosticTests` |
  | Broad overload / CVJG0052 narrowing                  | `CollectionMethodOverloadingTests` + `AccumulatorNameCollisionTests` |

Confirm each maps to a green [Fact]. If a criterion lacks a test, escalate in Task 2 checkpoint.

(D) NO new source-code changes expected in this task. Only test reruns + optional BACK-02 snapshot investigation. If a code change IS required to fix a regression, scope it here and note it in the SUMMARY; if the fix is non-trivial, convert to a gap-closure plan via `/gsd:plan-phase --gaps`.
  </action>
  <verify>
    <automated>dotnet test src/Converj.Generator.Tests/Converj.Generator.Tests.csproj --no-build --nologo</automated>
  </verify>
  <done>
Full suite green. Test count matches the expected total from summing Plan 23-01 through 23-04 new tests. BACK-02 snapshot passes (or any expected drift is whitelisted by the user in Task 2). All five Phase 23 amended success criteria map to green [Fact]s.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews retrofit audit table and signs off on any BACK-01 deviations</name>
  <files>.planning/phases/23-composability/23-04-SUMMARY.md, .planning/phases/23-composability/23-03-SUMMARY.md</files>
  <what-built>
The retrofit audit for the broad overload rule (Plan 23-04) classified every collision-family diagnostic descriptor (CVJG0008/0011/0016/0022/0023/0040/0041/0043/0052) as narrowed, message-updated, or no-change. Plan 23-04 SUMMARY contains the complete audit table. Some existing tests may have been rewritten to reflect narrowed triggers — each rewrite documented.

Additionally, CVJG0053 was introduced in Plan 23-03 with a specific triggering set documented in Plan 23-03 SUMMARY.

This checkpoint asks the user to review both the audit table and the CVJG0053 triggering set and approve any deviations from a strict byte-identical reading of BACK-01.
  </what-built>
  <action>
Pause and wait for the user to review the retrofit audit table (Plan 23-04 SUMMARY) and the CVJG0053 triggering set (Plan 23-03 SUMMARY). Do NOT proceed to Task 3 (SUMMARY writing) until the user replies with an approval signal.

While waiting, the AI may answer clarifying questions about any row in the audit table — grep / open source files on request to confirm classifications, but MAKE NO CODE CHANGES in this task. If the user requests a revert or escalation, scope it here and halt the plan for a gap-closure plan via `/gsd:plan-phase --gaps`.
  </action>
  <how-to-verify>
1. Open `.planning/phases/23-composability/23-04-SUMMARY.md`. Read the retrofit audit table.
2. For each "narrowed to signature-aware" row:
   - Confirm the rationale matches your expectation (signature-distinctness is the right discrimination predicate).
   - Confirm any rewritten tests in the "Tests changed" column describe a legitimate behavior change (not a silent weakening).
3. For each "message updated only" row:
   - Confirm the new message is clearer without changing the trigger.
4. For each "no change" row:
   - Spot-check by asking the AI to point you at the emission site to verify the classification.
5. Open `.planning/phases/23-composability/23-03-SUMMARY.md`. Read the CVJG0053 triggering set.
   - Confirm: record primary-ctor positional property (required case, locked by CONTEXT.md).
   - Confirm: any secondary cases added by the analyzer are documented with rationale.
   - Reject any triggering condition that is likely to false-positive on legitimate user code.
6. If any deviation is unacceptable, request it be reverted or escalated to a gap-closure plan.

Approve by responding with: `approved` (implicitly signs off on all documented dispositions); OR `approved except {list}` (specify deviations to revert); OR describe concerns.
  </how-to-verify>
  <verify>
    <automated>MANUAL — User review of Plans 23-03 and 23-04 SUMMARY files required. No automated check applies to a human-sign-off gate.</automated>
  </verify>
  <done>
User has responded with `approved` or `approved except {list}`. Any `except` items are converted to gap-closure work (planned via `/gsd:plan-phase --gaps`) before the phase closes. If `approved`, Task 3 may proceed.
  </done>
  <resume-signal>Type "approved", "approved except {list}", or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Produce final Phase 23 SUMMARY and mark phase complete</name>
  <files>.planning/phases/23-composability/23-05-SUMMARY.md</files>
  <action>
Create the phase-closing SUMMARY at `.planning/phases/23-composability/23-05-SUMMARY.md`. Structure (follow the existing templates/summary.md convention where available):

```markdown
# Phase 23 — Composability: Summary

## Status
Complete. Closes v2.2 milestone Phase 23. Awaiting /gsd:verify-work before v2.2 Phase 24 begins.

## Requirements Delivered
- COMP-01: [link to tests demonstrating]
- COMP-02: [link]
- COMP-03 (amended — free composition): [link]

## Plan Outcomes
- 23-01: Docs amended; 5 Wave-0 test stubs created.
- 23-02: Core composability — AccumulatorBulkMethod + AccumulatorBulkTransitionMethod + ISelfReturningAccumulatorMethod marker; emission; pipeline wiring.
- 23-03: Property parity — AttributeUsage widened; CollectionPropertyInfo; analyzer extended; CVJG0053 introduced.
- 23-04: Broad overload retrofit — CVJG0052 narrowed; audit completed.
- 23-05: Regression gate — BACK-01 / BACK-02 verified; user sign-off obtained.

## Test Count
- Phase 22 baseline: 440 passing.
- Phase 23 additions: {N} new tests across the five new / updated test classes.
- Phase 23 total: {440 + N} passing.
- Deviations from BACK-01: {list or "none"; cross-reference Plan 23-04 audit table}.

## Architecture Highlights
- Model D operational: one AccumulatorFluentStep per collection parameter/property with two self-returning methods.
- Broad overload rule active: signature-distinct same-name methods coexist generator-wide.
- Property-backed accumulators use the v2.0 object-initializer path — no parallel storage type introduced.
- ISelfReturningAccumulatorMethod marker centralizes traversal-exclusion logic.

## Deferred to Phase 24
- MinItems enforcement (all MIN-* requirements).
- WithXs-unconditionally-satisfies-minimum semantics (Reading C, locked in CONTEXT.md).
- Distinct step types for minimum-not-met / minimum-met.

## User Sign-Offs
- Retrofit audit dispositions: {approval text}
- CVJG0053 triggering set: {approval text}
- BACK-02 snapshot: {byte-identical | drift-whitelisted with reason}

## Next
- Commit plan closure.
- Run `/gsd:verify-work 23` for phase verification.
- Proceed to Phase 24 planning after /gsd:verify-work signs off.
```

Populate every `{placeholder}` with actual values gathered during Task 1 and Task 2. Cross-reference Plan SUMMARYs for exact test names, commit hashes, and audit-table rows.

Do NOT update ROADMAP.md's Phase 23 `**Plans**` line here — /gsd:execute-phase's phase-completion step handles that.

Do NOT update STATE.md here — the orchestrator owns STATE.md transitions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/23-composability/23-05-SUMMARY.md','utf8'); const missing=[]; ['COMP-01','COMP-02','COMP-03','Model D','ISelfReturningAccumulatorMethod','CVJG0053','CVJG0052','broad overload','Deferred to Phase 24'].forEach(t=>{ if(!s.includes(t)) missing.push(t); }); if(missing.length){ console.error('Missing required sections:', missing.join(', ')); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <done>
Phase 23 SUMMARY exists at the expected path. Contains every required anchor phrase and every placeholder substituted with real values from Plans 23-01 through 23-04. User sign-offs recorded. Deferred items for Phase 24 enumerated for planner continuity.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test` full suite green.
2. `BackwardCompatibilitySnapshotTests` — byte-identical (or whitelisted drift with recorded reason).
3. Retrofit audit table reviewed and signed off by user.
4. Phase SUMMARY exists and contains all required anchor phrases.
</verification>

<success_criteria>
- Phase 23 closes with all three requirements (COMP-01, COMP-02, COMP-03 amended) delivered and observable via green [Fact]s.
- BACK-01 / BACK-02 compliance verified OR deviations explicitly user-approved.
- Plan 23-05 SUMMARY aggregates Plans 23-01 through 23-04 into a single phase-level completion artifact.
- Phase 24 handoff clean — MinItems deferrals documented.
</success_criteria>

<output>
Phase 23 complete. Orchestrator transitions STATE.md. Next step for the user: `/gsd:verify-work 23` to run phase-level verification, then `/gsd:plan-phase 24` for MinItems enforcement.
</output>
