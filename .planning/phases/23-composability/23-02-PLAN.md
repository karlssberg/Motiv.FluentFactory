---
phase: 23-composability
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/Converj.Generator/Models/Methods/AccumulatorBulkMethod.cs
  - src/Converj.Generator/Models/Methods/AccumulatorBulkTransitionMethod.cs
  - src/Converj.Generator/Models/Methods/ISelfReturningAccumulatorMethod.cs
  - src/Converj.Generator/Models/Methods/AccumulatorMethod.cs
  - src/Converj.Generator/Models/Steps/AccumulatorFluentStep.cs
  - src/Converj.Generator/SyntaxGeneration/AccumulatorStepDeclaration.cs
  - src/Converj.Generator/ModelBuilding/FluentMethodBuilder.cs
  - src/Converj.Generator/ModelBuilding/FluentStepBuilder.cs
  - src/Converj.Generator/FluentModelBuilder.cs
  - src/Converj.Generator.Tests/CollectionMethodComposabilityTests.cs
  - src/Converj.Generator.Tests/CollectionMethodBulkSetTests.cs
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "A parameter carrying both [FluentCollectionMethod] and [FluentMethod] generates BOTH AddX(T) and WithXs(IEnumerable<T>) on the same AccumulatorFluentStep"
    - "The preceding regular step exposes two transitions into the accumulator step: the Phase 22 parameterless BuildTarget transition AND a parameterised WithXs(IEnumerable<T>) transition"
    - "Both AddX and WithXs are self-returning, marked [MethodImpl(AggressiveInlining)], emit copy-constructor return"
    - "Interleaving AddX and WithXs on the accumulator step produces correctly accumulated results (GEN-03 branch independence preserved by struct value semantics)"
    - "Step-graph traversals (GetDescendentFluentSteps, MarkReturnsFromMethods, ResolveTargetTypeReturn) exclude both self-returning methods via a shared ISelfReturningAccumulatorMethod marker interface — no infinite loop"
  artifacts:
    - path: "src/Converj.Generator/Models/Methods/AccumulatorBulkMethod.cs"
      provides: "Self-returning WithXs method on AccumulatorFluentStep"
      min_lines: 50
    - path: "src/Converj.Generator/Models/Methods/AccumulatorBulkTransitionMethod.cs"
      provides: "WithXs entry transition from regular step into accumulator step"
      min_lines: 40
    - path: "src/Converj.Generator/Models/Methods/ISelfReturningAccumulatorMethod.cs"
      provides: "Marker interface for accumulator step-graph traversal exclusion"
      min_lines: 8
    - path: "src/Converj.Generator/SyntaxGeneration/AccumulatorStepDeclaration.cs"
      provides: "Emission of both AddX and WithXs methods on the accumulator step"
      contains: "AccumulatorBulkMethod"
    - path: "src/Converj.Generator/FluentModelBuilder.cs"
      provides: "Routing of [FluentMethod] on a [FluentCollectionMethod] parameter into AccumulatorBulkTransitionMethod + AccumulatorBulkMethod"
      contains: "AccumulatorBulkMethod"
  key_links:
    - from: "AccumulatorStepDeclaration.CreateAddMethods"
      to: "step.FluentMethods.OfType<AccumulatorBulkMethod>()"
      via: "Polymorphic method emission loop"
      pattern: "OfType<AccumulatorBulkMethod>"
    - from: "FluentModelBuilder (accumulator step construction)"
      to: "AccumulatorBulkMethod + AccumulatorBulkTransitionMethod"
      via: "When [FluentMethod] co-occurs with [FluentCollectionMethod], create bulk method pair"
      pattern: "AccumulatorBulkMethod\\s*\\("
    - from: "FluentStepBuilder (traversal exclusion)"
      to: "ISelfReturningAccumulatorMethod"
      via: "Marker-interface filter replacing Phase 22's AccumulatorMethod type check"
      pattern: "ISelfReturningAccumulatorMethod"
---

<objective>
Deliver the core composability feature: when a parameter carries both `[FluentCollectionMethod]` and `[FluentMethod]`, emit a single `AccumulatorFluentStep` exposing BOTH `AddX(T item)` (single-item, existing Phase 22) AND `WithXs(IEnumerable<T> items)` (bulk-append, new). Route the `[FluentMethod]` entry from the regular trie into a parallel bulk transition on the preceding regular step. Preserve Phase 22's traversal-exclusion guard by generalizing it via a shared marker interface.

Purpose: Realizes the user's "Model D" decision locked in 23-CONTEXT.md — one step per collection parameter, two self-returning methods, free composition. Addresses COMP-01 (attributes coexist), COMP-02 (both methods emitted), and COMP-03 amended (free composition via incremental `ImmutableArray<T>.AddRange` / `.Add`).

Output: Two new model types, one marker interface, extended emission, extended pipeline wiring, replaced placeholder tests in two test classes.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-composability/23-CONTEXT.md
@.planning/phases/23-composability/23-RESEARCH.md
@.planning/phases/23-composability/23-01-SUMMARY.md
@src/Converj.Generator/Models/Methods/AccumulatorMethod.cs
@src/Converj.Generator/Models/Methods/AccumulatorTransitionMethod.cs
@src/Converj.Generator/Models/Methods/IFluentMethod.cs
@src/Converj.Generator/Models/Steps/AccumulatorFluentStep.cs
@src/Converj.Generator/SyntaxGeneration/AccumulatorStepDeclaration.cs
@src/Converj.Generator/SyntaxGeneration/AccumulatorCollectionConversionExpression.cs
@src/Converj.Generator/FluentModelBuilder.cs
@src/Converj.Generator/ModelBuilding/FluentMethodBuilder.cs
@src/Converj.Generator/ModelBuilding/FluentStepBuilder.cs
@src/Converj.Generator/TargetAnalysis/CollectionParameterInfo.cs
@src/Converj.Generator.Tests/AccumulatorStepGenerationTests.cs
@src/Converj.Generator.Tests/AccumulatorNameOverrideTests.cs

<interfaces>
<!-- Existing Phase 22 shapes this plan composes with. Extracted — no codebase exploration needed. -->

From src/Converj.Generator/Models/Methods/IFluentMethod.cs (excerpt):
```csharp
internal interface IFluentMethod
{
    string Name { get; }
    IParameterSymbol? SourceParameter { get; }
    IFluentReturn Return { get; }
    ImmutableArray<FluentMethodParameter> MethodParameters { get; }
    ImmutableArray<FluentMethodParameter> AvailableParameterFields { get; }
    ImmutableArray<FluentTypeParameter> TypeParameters { get; }
    INamespaceSymbol RootNamespace { get; }
    OrderedDictionary<IParameterSymbol, IFluentValueStorage> ValueSources { get; }
    string? DocumentationSummary { get; }
    Dictionary<string, string>? ParameterDocumentation { get; }
}
```

From src/Converj.Generator/Models/Methods/AccumulatorMethod.cs (the pattern to parallel):
```csharp
internal class AccumulatorMethod : IFluentMethod
{
    public AccumulatorMethod(
        CollectionParameterInfo collectionParameter,
        AccumulatorFluentStep returnStep,
        INamespaceSymbol rootNamespace,
        ImmutableArray<FluentMethodParameter> availableParameterFields,
        OrderedDictionary<IParameterSymbol, IFluentValueStorage> valueSources) { ... }
    public string Name => CollectionParameter.MethodName;
    public CollectionParameterInfo CollectionParameter { get; }
    // Self-returns by making Return == returnStep (the same AccumulatorFluentStep).
}
```

From src/Converj.Generator/TargetAnalysis/CollectionParameterInfo.cs:
```csharp
internal sealed record CollectionParameterInfo(
    IParameterSymbol Parameter,
    ITypeSymbol ElementType,
    ITypeSymbol DeclaredCollectionType,
    string MethodName,        // Derived / override-controlled add method name
    int MinItems);
```

From STATE.md (Phase 22 Plan 04 decision, line 62): `AccumulatorMethod excluded from all step-chain traversals (GetDescendentFluentSteps, MarkReturnsFromMethods, ResolveTargetTypeReturn) — self-returning method causes infinite loop if not excluded.`
These exclusions live in `FluentModelBuilder.cs` (lines ~931, 941 per research) and `FluentStepBuilder.cs`.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce ISelfReturningAccumulatorMethod marker + AccumulatorBulkMethod + AccumulatorBulkTransitionMethod domain types</name>
  <files>
src/Converj.Generator/Models/Methods/ISelfReturningAccumulatorMethod.cs,
src/Converj.Generator/Models/Methods/AccumulatorBulkMethod.cs,
src/Converj.Generator/Models/Methods/AccumulatorBulkTransitionMethod.cs,
src/Converj.Generator/Models/Methods/AccumulatorMethod.cs
  </files>
  <behavior>
    - Marker interface `ISelfReturningAccumulatorMethod` exists, has no members, lives under `Converj.Generator.Models.Methods`.
    - `AccumulatorMethod` now implements BOTH `IFluentMethod` AND `ISelfReturningAccumulatorMethod` (adjust class signature; no body change).
    - `AccumulatorBulkMethod` implements BOTH `IFluentMethod` AND `ISelfReturningAccumulatorMethod`.
    - `AccumulatorBulkMethod.Name` returns the `[FluentMethod]`-configured bulk method name (typically the parameter name rendered via existing `FluentMethodAttribute` resolution — the caller passes it in via constructor, NOT derived internally).
    - `AccumulatorBulkMethod.Return` points at the same `AccumulatorFluentStep` instance passed into the constructor (self-return; GEN-01 parallel).
    - `AccumulatorBulkMethod.MethodParameters` contains exactly one `FluentMethodParameter` whose source type is `IEnumerable<ElementType>` — NOT the declared collection type and NOT the element type alone. The bulk method always accepts `IEnumerable<T>` for append-range semantics (23-CONTEXT.md locked decision).
    - `AccumulatorBulkTransitionMethod` implements `IFluentMethod` only (NOT the marker — it lives on the preceding regular step and is not self-returning).
    - `AccumulatorBulkTransitionMethod.MethodParameters` contains one `FluentMethodParameter` of type `IEnumerable<ElementType>` (the items being bulk-added during the transition into the accumulator step).
    - `AccumulatorBulkTransitionMethod` exposes a `CollectionParameter` property (to let the syntax generator find the target accumulator field) — mirrors `AccumulatorMethod.CollectionParameter`.
    - Unit tests in a new `Converj.Generator.Tests/DomainModel/AccumulatorBulkMethodDomainTests.cs` (co-located with other tests; placed at test-project root is fine) verify: (a) marker interface is implemented; (b) `MethodParameters.Single().SourceType` equals `IEnumerable<ElementType>` constructed from Roslyn `Compilation.GetSpecialType(SpecialType.System_Collections_Generic_IEnumerable_T).Construct(elementType)`; (c) `Return` is reference-equal to the injected accumulator step.
  </behavior>
  <action>
Create three new files in `src/Converj.Generator/Models/Methods/`:

(A) `ISelfReturningAccumulatorMethod.cs` — empty marker interface, XML doc references Phase 22 Plan 04 STATE.md decision and Pitfall 8:
```csharp
namespace Converj.Generator.Models.Methods;

/// <summary>
/// Marker interface implemented by every self-returning accumulator method on an
/// <see cref="Steps.AccumulatorFluentStep"/>. Used by <c>FluentModelBuilder</c> and
/// <c>FluentStepBuilder</c> to exclude these methods from descendant step traversals —
/// self-return would otherwise cause infinite recursion (see Phase 22 Plan 04 STATE.md
/// decision; Phase 23 RESEARCH.md Pitfall 8).
/// </summary>
internal interface ISelfReturningAccumulatorMethod
{
}
```

(B) `AccumulatorBulkMethod.cs` — parallel to `AccumulatorMethod.cs` with two changes:
  - Implements both `IFluentMethod` and `ISelfReturningAccumulatorMethod`.
  - `MethodParameters` holds a single parameter whose source type is `IEnumerable<ElementType>`. To build the constructed Roslyn type use: `compilation.GetSpecialType(SpecialType.System_Collections_Generic_IEnumerable_T).Construct(collectionParameter.ElementType)`. The `Compilation` must be threaded in via the constructor — ADD a `Compilation compilation` constructor parameter. (Do NOT hand-roll a `FluentType` for `IEnumerable<T>`; ask Roslyn for the symbol.)
  - Uses a private inner class (mirroring `AccumulatorMethod.ElementTypeFluentMethodParameter`) that overrides `SourceType` to the constructed `IEnumerable<ElementType>` — call it `BulkFluentMethodParameter`. Name derived from `CollectionParameter.Parameter.Name` (e.g., `items`) — this is the PARAMETER name on `WithXs(IEnumerable<T> items)`, not the method name. Method name is the caller-provided `bulkMethodName` ctor argument.
  - `Name` property returns the caller-provided `bulkMethodName`.

(C) `AccumulatorBulkTransitionMethod.cs` — parallel to `AccumulatorTransitionMethod.cs` but parameterised:
  - Constructor takes: `string name`, `AccumulatorFluentStep returnStep`, `INamespaceSymbol rootNamespace`, `ImmutableArray<FluentMethodParameter> availableParameterFields`, `OrderedDictionary<IParameterSymbol, IFluentValueStorage> valueSources`, `CollectionParameterInfo collectionParameter`, `Compilation compilation`.
  - Exposes `CollectionParameter` property (non-null) — this is what distinguishes it from the parameterless `AccumulatorTransitionMethod` and lets `AccumulatorStepDeclaration` (task 2) emit the correct field seed.
  - `MethodParameters` contains the single `IEnumerable<ElementType>` bulk parameter (same inner-class pattern as (B)).
  - Does NOT implement `ISelfReturningAccumulatorMethod` — it lives on the regular step, not the accumulator step.
  - `Return` points at the `AccumulatorFluentStep`.

(D) Modify `AccumulatorMethod.cs` class declaration to add the marker:
```csharp
internal class AccumulatorMethod : IFluentMethod, ISelfReturningAccumulatorMethod
```
No other change to `AccumulatorMethod`.

TDD: Create `src/Converj.Generator.Tests/DomainModel/AccumulatorBulkMethodDomainTests.cs` (new file — make the `DomainModel/` folder if it does not exist) with the xUnit tests described in Behavior. Use `CSharpCompilation.Create` to build a minimal Compilation for resolving `SpecialType.System_Collections_Generic_IEnumerable_T`. These are the only true unit tests in this phase — most assertions are source-gen output assertions in later tasks.

Per C# standards: XML doc headers on all public/internal types and members; use raw string literals for any test source; AutoFixture is NOT warranted here (we need real Roslyn symbols).
  </action>
  <verify>
    <automated>dotnet test src/Converj.Generator.Tests/Converj.Generator.Tests.csproj --filter "FullyQualifiedName~AccumulatorBulkMethodDomainTests" --no-build --nologo</automated>
  </verify>
  <done>
Three new source files + one updated `AccumulatorMethod.cs` + one unit-test file. `AccumulatorBulkMethodDomainTests` passes. Full solution still builds (`dotnet build` green; warnings-as-errors respected). `AccumulatorMethod` and `AccumulatorBulkMethod` are both `ISelfReturningAccumulatorMethod`. `AccumulatorBulkTransitionMethod` is NOT.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AccumulatorStepDeclaration emission + generalize traversal exclusions to ISelfReturningAccumulatorMethod</name>
  <files>
src/Converj.Generator/SyntaxGeneration/AccumulatorStepDeclaration.cs,
src/Converj.Generator/FluentModelBuilder.cs,
src/Converj.Generator/ModelBuilding/FluentStepBuilder.cs,
src/Converj.Generator/Models/Steps/AccumulatorFluentStep.cs
  </files>
  <behavior>
    - Generated accumulator step declaration contains BOTH `AddX(T item)` methods (existing Phase 22) AND `WithXs(IEnumerable<T> items)` methods (new), each `public`, each `[MethodImpl(MethodImplOptions.AggressiveInlining)]`, each returning a new `Accumulator_{Index}__{Root}` via the private copy constructor.
    - The `WithXs` method body assigns `this._{fieldName}__parameter.AddRange(items)` to the target field and forwards every OTHER accumulator field unchanged (parallel to the existing `AddX` body which assigns `this._{fieldName}__parameter.Add(item)`).
    - Step-graph traversal sites that previously filtered out `AccumulatorMethod` now filter out any `ISelfReturningAccumulatorMethod` — three sites per Phase 22 Plan 04 STATE.md decision: `GetDescendentFluentSteps`, `MarkReturnsFromMethods`, `ResolveTargetTypeReturn`. The filter changes from type pattern `is AccumulatorMethod` to `is ISelfReturningAccumulatorMethod`.
    - A test in `CollectionMethodBulkSetTests.cs` verifies: input source with `[FluentCollectionMethod, FluentMethod] IList<string> tags` produces a generated file containing both `public ... AddTag(string item)` and `public ... WithTags(global::System.Collections.Generic.IEnumerable<string> items)`, both on the `Accumulator_0__{Root}` struct, both self-returning.
    - No infinite loop during trie walking — existing tests do not hang.
  </behavior>
  <action>
(A) `AccumulatorStepDeclaration.cs`: In `CreateAddMethods` (or equivalent entry point that emits the single-item methods), extend to also emit `AccumulatorBulkMethod` instances. Approach:

```csharp
private static ImmutableArray<MethodDeclarationSyntax> CreateAddMethods(AccumulatorFluentStep step)
{
    var stepGlobalName = step.IdentifierDisplayString();

    var singleAdds = step.FluentMethods
        .OfType<AccumulatorMethod>()
        .Select(method => CreateAddMethod(step, method, stepGlobalName));

    var bulkAdds = step.FluentMethods
        .OfType<AccumulatorBulkMethod>()
        .Select(method => CreateBulkMethod(step, method, stepGlobalName));

    return [..singleAdds, ..bulkAdds];
}
```

Introduce `CreateBulkMethod(step, method, stepGlobalName)` mirroring `CreateAddMethod`. Key differences:
  - Parameter: `IEnumerable<{elementGlobalName}> items` — use `global::System.Collections.Generic.IEnumerable<...>` for fully-qualified emission (matches project convention — see `AccumulatorCollectionConversionExpression` style).
  - Body: identical copy-constructor return shape, except the target-field argument is `this.{fieldName}.AddRange(items)` instead of `this.{fieldName}.Add(item)`.
  - Reuse the existing private helper that builds copy-constructor arguments. Parameterise it so the target field's expression is the caller's choice (`.Add(item)` vs `.AddRange(items)`). Recommended helper signature: `BuildCopyConstructorArguments(step, targetField, ExpressionSyntax targetFieldAssignment)`. Factor the existing `BuildCopyConstructorArguments` call in `CreateAddMethod` to use the new overload.

(B) Traversal-exclusion generalization — three sites (per Phase 22 Plan 04 STATE.md line 62 and 23-RESEARCH.md Pitfall 8):
  1. `FluentModelBuilder.cs` — `GetDescendentFluentSteps` (research flags ~line 931): change `is AccumulatorMethod` → `is ISelfReturningAccumulatorMethod`.
  2. `FluentModelBuilder.cs` — `MarkReturnsFromMethods` (research flags ~line 941): same change.
  3. `FluentModelBuilder.cs` or `FluentStepBuilder.cs` — `ResolveTargetTypeReturn` self-loop guard (Phase 22 Plan 04 fix): same change.

If any site does not use a direct type pattern (e.g., uses `GetType() == typeof(AccumulatorMethod)`), convert to `is ISelfReturningAccumulatorMethod`. Grep-assisted audit before edit: search `src/Converj.Generator` for `AccumulatorMethod` usages where the context is a traversal/exclusion — leave NON-traversal usages alone (e.g., constructor calls, emission `OfType<AccumulatorMethod>()` in `AccumulatorStepDeclaration`).

(C) `AccumulatorFluentStep.cs`: Update the XML doc comment on `FluentMethods` property — it currently says `AccumulatorMethod instances (one per collection parameter) plus the terminal TerminalMethod`. Update to also mention `AccumulatorBulkMethod` instances (one per collection parameter that has composed `[FluentMethod]`). No code change.

(D) Replace placeholder in `CollectionMethodBulkSetTests.cs` with a record-replay source-gen-output test. Input code fixture:
```csharp
using System.Collections.Generic;
namespace Test;
[FluentRoot]
public static partial class Builder { }
public class Target
{
    [FluentTarget(typeof(Builder))]
    public Target(
        [FluentCollectionMethod, FluentMethod] IList<string> tags) { }
}
```

Expected generated output assertions (capture via record-replay — run once with a pin-the-output helper, commit exact expected text). The KEY behavioral anchors the expected output must contain (verify textually if record-replay produces a different-but-equivalent spelling):
  - `public global::Test.Accumulator_0__Test_Builder AddTag(string item)` with `[MethodImpl(MethodImplOptions.AggressiveInlining)]` and body returning `new Accumulator_0__Test_Builder(this._tags__parameter.Add(item))`.
  - `public global::Test.Accumulator_0__Test_Builder WithTags(global::System.Collections.Generic.IEnumerable<string> items)` with `[MethodImpl(MethodImplOptions.AggressiveInlining)]` and body returning `new Accumulator_0__Test_Builder(this._tags__parameter.AddRange(items))`.
  - The preceding regular step exposes the Phase 22 parameterless transition to the accumulator step.

NOTE: The "preceding regular step exposes a parameterised `WithTags(IEnumerable<string>)` transition" behavior is delivered by Task 3 — this test should ONLY pin the two `AddTag`/`WithTags` methods on the accumulator step itself. A separate test in Task 3 pins the transition.

BUILD ORDER: Task 3 must still be able to pass COMP-02 end-to-end. The two-task split keeps each task <60min and lets Task 2 ship a compilable (but not yet end-to-end COMP-01) state. Until Task 3 wires the bulk method into the step's `FluentMethods` collection, Task 2's test is fixture-level only — provide a temporary construction path in the test itself (via a hand-built model or a `SkipGeneratedSourcesCheck` fallback). Planner's note: if that proves too contrived, merge Task 2 and Task 3 into a single larger task after confirming with the user during execution.
  </action>
  <verify>
    <automated>dotnet test src/Converj.Generator.Tests/Converj.Generator.Tests.csproj --filter "FullyQualifiedName~CollectionMethodBulkSetTests" --no-build --nologo</automated>
  </verify>
  <done>
`AccumulatorStepDeclaration.CreateAddMethods` emits both method families in a single pass. Traversal exclusions use the marker interface — grep for `is AccumulatorMethod` in traversal contexts returns zero hits. Full suite (`dotnet test`) builds and runs; only the two new composability-targeted tests should change state (Placeholder → real [Fact]); BACK-01 remains intact (Task 3 completes COMP-01 end-to-end).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire bulk transition in FluentModelBuilder and FluentMethodBuilder; enable end-to-end COMP-01/02/03</name>
  <files>
src/Converj.Generator/FluentModelBuilder.cs,
src/Converj.Generator/ModelBuilding/FluentMethodBuilder.cs,
src/Converj.Generator.Tests/CollectionMethodComposabilityTests.cs
  </files>
  <behavior>
    - When a collection parameter carries `[FluentCollectionMethod]` AND `[FluentMethod]`, the `[FluentMethod]` configuration is redirected away from the regular trie: instead of producing a `RegularMethod` on a trie node, it produces an `AccumulatorBulkTransitionMethod` on the preceding regular step AND an `AccumulatorBulkMethod` on the accumulator step.
    - The `AccumulatorBulkMethod.Name` is resolved from the `[FluentMethod]` attribute's `MethodName` / default resolution (same logic the regular trie path currently uses — NOT the singularized `AddX` name).
    - When a collection parameter carries `[FluentCollectionMethod]` but NO `[FluentMethod]`, Phase 22's behavior is preserved exactly (single transition, single `AddX` method, no bulk method). BACK-01 holds.
    - `[MultipleFluentMethods]` composition: each template-generated method name becomes an additional `AccumulatorBulkMethod` + corresponding `AccumulatorBulkTransitionMethod` pair. Confirmed via a test fixture. Signature-distinct-by-template-design — no overload collision at this stage (broad overload rule in Plan 23-04 handles collisions; pre-Plan-23-04 behavior is: if two bulk methods would collide on the accumulator step by name, CVJG0052 fires as it does today; Plan 23-04 narrows that).
    - `CollectionMethodComposabilityTests.cs` pins: (a) COMP-01 combined-attribute acceptance, (b) COMP-02 both methods emitted on the accumulator step, (c) COMP-03 amended — a composed runtime fixture showing `AddTag("x").WithTags(bulk).AddTag("y")` produces `["x", ...bulk, "y"]`.
  </behavior>
  <action>
(A) `FluentModelBuilder.cs` — in the accumulator-step construction path (research flags lines 58-63 and 317-459 as the orchestration surface for accumulator wiring), after constructing `AccumulatorMethod`s for each collection parameter, ADD a sibling loop constructing `AccumulatorBulkMethod`s for each collection parameter whose backing parameter symbol ALSO carries a `[FluentMethod]` (or `[MultipleFluentMethods]` — iterate its expanded method set) attribute. Source of truth for the presence of `[FluentMethod]` on the parameter: inspect `IParameterSymbol.GetAttributes()` for the `FluentMethodAttribute` metadata name. Resolve the bulk method name using the same logic the regular trie uses (centralized in `FluentMethodBuilder` today).

Each bulk method pair emits:
  - One `AccumulatorBulkMethod` added to `accumulatorStep.FluentMethods`.
  - One `AccumulatorBulkTransitionMethod` added to the PRECEDING regular step's `FluentMethods` list (same list that currently holds the Phase 22 `AccumulatorTransitionMethod`).

Both carry the SAME `CollectionParameterInfo` and the SAME resolved method name.

(B) `FluentMethodBuilder.cs` — when building method candidates for the regular trie, filter OUT `[FluentMethod]` attributes that live on a parameter also carrying `[FluentCollectionMethod]`. These are redirected to the accumulator wiring in (A). Preserve existing behavior for every other parameter. Concretely: find the site where `[FluentMethod]` is enumerated and a `RegularMethod` is appended to the trie — add a skip condition:
```csharp
if (parameter.GetAttributes().Any(a =>
      a.AttributeClass?.ToDisplayString() == "Converj.Attributes.FluentCollectionMethodAttribute"))
    continue; // Redirected to accumulator bulk transition in FluentModelBuilder.
```

(C) Replace placeholder in `CollectionMethodComposabilityTests.cs` with three [Fact]s:

1. `COMP_01_both_attributes_accepted_on_same_parameter` — record-replay test: input source has `[FluentCollectionMethod, FluentMethod] IList<string> tags`, expected output contains BOTH `AddTag` on the accumulator step AND `WithTags` as a transition method on the preceding regular step. No diagnostic emitted.

2. `COMP_02_both_accumulator_and_bulk_methods_generated` — same fixture, assert generated file contains the accumulator step struct declaration with both `AddTag(string item)` and `WithTags(IEnumerable<string> items)` inside.

3. `COMP_03_free_composition_runtime_behavior` — compile the generated builder IN THE TEST and execute it via `Assembly.Load` on the verifier's output compilation. Call `Builder.Create().AddTag("x").WithTags(new[]{"y","z"}).AddTag("w").Create()` and assert the resulting target's `Tags` property equals `["x","y","z","w"]`. Use the existing verifier infrastructure if it exposes the compiled assembly; otherwise add a minimal helper that extracts the compiled assembly from `VerifyCS.Test.TestState.Sources` + `GeneratedSources` combined. Study `AccumulatorStepGenerationTests.cs` for existing runtime-exercise patterns; match them.

If runtime execution is not already supported by the verifier infrastructure, fall back to a source-gen assertion that the terminal method converts the accumulated `ImmutableArray<T>` via the existing 6-case conversion (`AccumulatorCollectionConversionExpression` unchanged). Note the limitation in the test XML doc and defer the runtime round-trip to manual verification sign-off.

(D) Additional fixture (optional but recommended per RESEARCH.md Open Question 5): `COMP_MultipleFluentMethods_composition` — input source with `[FluentCollectionMethod, MultipleFluentMethods(...)]` on the parameter; assert each template-generated bulk method name becomes a distinct transition + distinct bulk accumulator method. If `[MultipleFluentMethods]` routing breaks in the current `FluentMethodBuilder` skip path (i.e., (B) is too aggressive and strips template-generated methods too), refine the skip to apply only to the baseline `[FluentMethod]` entry and let the template expansion continue to pass through the accumulator redirect logic in (A). If this becomes non-trivial, mark the fixture `[Fact(Skip = "...")]` and record the limitation in the plan SUMMARY for Phase 24.

CQS discipline: keep the accumulator-wiring loop in `FluentModelBuilder.cs` as an orchestrator; extract the per-parameter bulk-pair construction into a well-named private helper (e.g., `CreateAccumulatorBulkMethodPair(parameter, collectionInfo, accumulatorStep, precedingStep)`).
  </action>
  <verify>
    <automated>dotnet test src/Converj.Generator.Tests/Converj.Generator.Tests.csproj --filter "FullyQualifiedName~CollectionMethodComposabilityTests|FullyQualifiedName~CollectionMethodBulkSetTests" --no-build --nologo</automated>
  </verify>
  <done>
`CollectionMethodComposabilityTests` emits three (or four, if `[MultipleFluentMethods]` fixture lands) [Fact]s — all green. `CollectionMethodBulkSetTests` still green. Full `dotnet test` — BACK-01 intact (440+ existing tests still pass), only the intentional new tests added. Phase 23 core user value (`AddTag`/`WithTags` freely composable) is delivered. Plan SUMMARY notes any deferred items for Plan 23-03/04/05.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build` green with warnings-as-errors.
2. `dotnet test` full suite: pre-plan baseline + new tests from this plan pass; BACK-01 holds (existing 440 tests unchanged).
3. Grep audit: `is AccumulatorMethod` in traversal contexts (`GetDescendentFluentSteps`, `MarkReturnsFromMethods`, `ResolveTargetTypeReturn`) returns zero hits — all replaced by `is ISelfReturningAccumulatorMethod`.
4. COMP-01/02/03 fixtures in `CollectionMethodComposabilityTests` demonstrate free composition behavior.
</verification>

<success_criteria>
- A developer using `[FluentCollectionMethod, FluentMethod]` on a single parameter receives both `AddX` and `WithXs` methods on the same `AccumulatorFluentStep`.
- `AddX` and `WithXs` freely compose — interleaved calls produce correctly appended results without branch-contamination (GEN-03 preserved by struct value semantics).
- `AccumulatorMethod` and `AccumulatorBulkMethod` share traversal-exclusion behavior via marker interface; no infinite loop.
- Phase 22 sole-`[FluentCollectionMethod]` behavior preserved byte-identically (the existing accumulator test suite continues to pass unmodified).
</success_criteria>

<output>
After completion, create `.planning/phases/23-composability/23-02-SUMMARY.md`. Record:
- Exact emission shape of `WithXs` (captured record-replay text for future regression).
- Whether `[MultipleFluentMethods]` fixture landed as a [Fact] or [Skip].
- Whether runtime round-trip in COMP-03 was executed or deferred to manual verification.
- Full traversal-exclusion site list with line numbers.
- Any discovered subtleties in `FluentMethodBuilder` redirect for routing Plan 23-03 and 23-04 work.
</output>
